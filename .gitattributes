# 合併策略：純附加型的清單檔用 union，兩邊的新增行都保留、不產生衝突。
#
# **為什麼需要這個**（2026-08-21，實測資料）：這類檔案的規則本來就是
# 「只附加、不改舊行」，但光靠規則約束不了合併行為——只附加避免的是
# *語意* 衝突（不會蓋掉別人的內容），git 仍然會在 *文字* 層衝突：兩個分支
# 都在檔案結尾加一行時，git 不知道該把誰排前面，一定跳 CONFLICT。
#
# 實測：把當時開著的 11 個 PR 依序模擬合併，5 個有衝突，其中 3 個
#（#94／#91／#97）就是這種「兩邊都在 RESULTS_LOG 結尾附加一行」。加上
# union 之後同一組合併衝突歸零，兩筆紀錄都保留。
#
# **union 的適用條件**（不要亂套到別的檔案）：
#   1. 檔案是「一行一筆、彼此獨立」的紀錄，行的先後順序不影響正確性
#      （這幾個檔案的每一行都自帶日期，順序亂了也讀得懂）
#   2. 兩邊加的內容都應該保留（不是二選一）
#   3. **絕對不要**用在程式碼、設定檔、或順序有意義的檔案上——union 會
#      把兩邊的行都留下，在 queue.txt 這種「順序＝執行順序」的檔案上會
#      直接產生錯誤的佇列，在 .py 上會產生語法錯誤卻不報衝突。
# 用根目錄錨定（開頭的 `/`）而不是裸檔名——裸檔名會套用到任何深度、
# 任何目錄下同名的檔案（例如未來若有 docs/WORK_BOARD.md 這種巢狀同名檔），
# git attributes 的比對規則預設就是這樣，不是這裡寫錯，是要明確排除掉
# （2026-08-21 CodeRabbit review）。目前專案裡沒有巢狀同名檔（已用 find
# 核對過），但錨定不影響現在的行為，純粹是防未來新增檔案時被誤套用。
#
# **`WORK_BOARD.md` 已從這個清單移出**（2026-08-24）：拆分後改成統一
# 大表格＋每個任務只有一行、狀態變動直接覆蓋儲存格的格式（見
# `CONTRIBUTING.md` 零之四），不再符合上面條件 1、2——同一個任務的
# 同一行，兩個分支可能各自改了不同欄位，這時應該讓 git 跳出正常的
# CONFLICT 交給人判斷該留哪一版，用 union 會把兩個版本的行都留下，
# 變成同一個任務出現兩筆矛盾的狀態，比明確衝突更難發現。
#
# **`WORK_BOARD_DONE.md` 也已從這個清單移出**（同日，稍晚）：一度想
# 讓它接手 `WORK_BOARD.md` 原本的純附加型歷史封存角色，但使用者當天
# 稍晚進一步要求這份文件只留精簡結論、不保留逐字過程——代表這份
# 文件現在也會被整段改寫（把舊條目濃縮成結論、拿掉重複內容），不再
# 符合條件 1（「彼此獨立、順序不影響正確性」的附加行）。跟
# `WORK_BOARD.md` 一樣，改動這份文件時若跟其他分支衝突，應該讓 git
# 跳出正常的 CONFLICT 交給人判斷，不該用 union 把兩個版本都留下。
# 目前只剩 `results/RESULTS_LOG.md` 是真正的純附加型逐次執行紀錄。
/results/RESULTS_LOG.md merge=union
