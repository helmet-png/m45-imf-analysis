# Kaggle 待辦佇列。格式：標籤|腳本.py|參數|逗號分隔的額外依賴檔|minimal(true/false)|指定帳號(可留空)
# kaggle_queue.py 每輪都會重新讀檔，執行中可以在末端追加步驟。
# 已完成的記在 logs/kaggle_queue_done.txt。

# ===== 2026-08-10：測試掛載時序問題的根本修法 =====
# 在 make_kernel() 生成的 notebook 第一格加了 _wait_input()，讓 kernel
# 自己原地輪詢等 /kaggle/input/ 掛好再動作，不必再靠外部重推整個 kernel
# 版本賭運氣。用最小的 smoke test 快速驗證這個修法有沒有真的解決問題
# （比等一個 40 分鐘的正式工作快很多）。
mount_fix_test|kaggle_smoketest.py|||true|helmetalbert
