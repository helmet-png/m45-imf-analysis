# 多星團驗證目前做到哪裡？（高中生版）

更新：2026-08-20（台灣時間；UTC 日期仍為 8/19）。這份是「目前進度報告」，不是宣稱已得到最終 IMF 的論文結果。

## 一句話結論

M45 的**傳統分析流程**可以搬到 NGC 3532 和 Praesepe；但是要說前向模型也
「通用」，證據還不夠。原因不是算力不夠，而是兩個星團的前向模型都出現了
「答案貼在搜尋範圍邊緣」的警訊，而且新版選擇函數需要的 Gaia 控制場資料暫時
被 Gaia 伺服器中斷連線擋住。

## 我們在驗證什麼？

IMF 可以想成「一個新生星團裡，不同重量的星原本各有多少」的分布。

- **傳統法**：先由顏色和亮度推估每顆星的質量，再統計數量。
- **前向模型**：先假設一個星團，模擬望遠鏡會看到什麼，再和真實資料比較。

前向模型理論上更完整，但它也更容易在資料或模型不相符時給出一個看似精準、
其實不可靠的答案。因此這次特別檢查它能不能換星團後仍正常工作。

## 已完成而且可使用的部分：傳統法

| 星團 | 年齡（約） | 高品質樣本 | 傳統法 alpha 的範圍 |
|---|---:|---:|---:|
| NGC 3532 | 238 百萬年 | 647 顆 | 1.508–1.534 |
| Praesepe | 346 百萬年 | 381 顆 | 2.150–2.283 |

這裡的 alpha 是描述「低質量星相對多不多」的斜率；兩個星團的數字不同，不代表
立刻能說它們出生時 IMF 不同。年齡、聯星、星團內部的質量分層與挑選樣本的方式
都會影響它。

傳統法的好消息是：五種處理聯星的方法給出的數字在各自星團內相當接近。這表示
程式的基本流程不是只為 M45 寫死的。

## 為什麼前向模型現在不能當結論？

前向模型有一個很重要的「紅燈」：如果最佳答案剛好卡在允許範圍的最邊緣，
就像考試答案一直選到選項表的最後一格。它通常代表模型仍缺少某些物理效果，
不是可靠的最佳答案。

| 星團 | 出現的紅燈 | 目前判斷 |
|---|---|---|
| NGC 3532 | 年齡與消光都貼到邊界 | 只能當診斷，不能引用 alpha |
| Praesepe | 聯星比例貼到 100% | 只能當診斷，不能引用 alpha |

我們已更新程式：**下一次成功重跑後**，只要出現這種非預期貼邊，輸出檔就會明確標成
`diagnostic_only`（只供找問題用），而不是誤寫成 IMF 結果。舊輸出檔是在這個保護
加入前產生，不能把它們當成新版格式的結果。

## 這次新補上的重要檢查

前向模型需要知道「望遠鏡和資料篩選會漏掉哪些星」。原本的版本只用星團成員來
估計這個效果；但成員星不一定涵蓋所有顏色和亮度，因此不夠嚴謹。

新版程式設計為加入星團附近的**控制場星**，並檢查：

1. 整體留下多少星；
2. 不同亮度區間有沒有偏差；
3. 特別暗的星（G>=17）中，紅星和藍星是否被不公平地留下或刪掉。

NGC 3532 的控制場查詢已嘗試兩次（5,000 顆與 1,000 顆上限），都在 Gaia
伺服器端中途斷線。因此沒有產生新結果，也沒有重複跑舊的前向模型。Praesepe
暫不啟動相同查詢，避免明知會失敗仍浪費資源。

## 下一步

1. 等 Gaia 服務可連線後，各星團只重建一次控制場與選擇函數。
2. 三個檢查都通過後，才重跑對應的前向模型。
3. 前向模型仍貼邊時，保留它作為「模型哪裡不夠」的證據，不把它硬解釋成 IMF。
4. 在論文中把「傳統法可移植」與「前向模型仍待驗證」分開寫，這比搶著報一個
   漂亮數字更科學，也更容易被老師或評審相信。

## 你可以怎麼對同學或老師說

> 我們已經證明同一套傳統分析流程可以套到兩個不同年齡的星團；但更複雜的前向
> 模型在兩個星團都出現警訊，所以我們沒有把它當成最終結論。現在的工作是補齊
> 控制場資料，確認望遠鏡的篩選效應沒有騙我們。這是在把研究做得更可靠，而不是
> 只是讓電腦一直算。

## 06:30 交接狀態（台灣時間）

- 這一輪沒有新增可用的控制場資料。NGC 3532 的控制場查詢曾以較小樣本重試，但 Gaia 公開服務仍在傳輸途中中斷；因此沒有開始 Praesepe 的同類查詢，也沒有進行需要控制場支撐的前向模型重跑。
- 這是「資料尚未取得」而不是「方法通過或失敗」。所以整體、星等分箱與 `G>=17` 紅藍兩組的選擇函數測試，仍全部是**待驗證**。
- 已安全保留並推送：可重現的程式修正、既有傳統法結果、前向模型的警告標記，以及本報告。下次 Gaia 服務可用時，最短而正確的接續順序是：NGC 3532 控制場 → 三項選擇函數測試 → Praesepe 控制場 → 對應測試 → 只對通過者做小型前向模型 smoke test。
