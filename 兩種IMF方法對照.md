# 傳統方法與前向模型：兩種求 IMF 的路徑

寫給論文的「分析方法」章節，也用來向不熟悉的人解釋我們為什麼選前向模型。

核心差別一句話：**傳統方法把觀測資料一步步「翻譯」成質量分布；
前向模型反過來，從假設的質量分布「生成」預測的觀測資料，再比對。**

---

## 一、傳統方法的流程與邏輯

### 步驟

**第一步：剔除雙星。**
用某種判準（RUWE 門檻、CMD 光度偏移、光譜徑向速度變化）判斷每一顆星
是不是雙星，把判定為雙星的移除，留下「乾淨的單星樣本」。

**第二步：擬合 isochrone 求年齡。**
把理論等時線疊在剩下的 CMD 上，調整年齡、消光、距離模數，
找出最貼合主序的那一條。傳統上這一步用肉眼判斷，現代版本改用加權卡方
（M67 那篇的權重是轉折點 60%、巨星 30%、主序星 10%，
因為年齡資訊幾乎全部集中在轉折點）。

**第三步：把亮度翻譯成質量。**
用第二步定出的 isochrone 所提供的質量-光度關係，
把每顆星的觀測星等反查成質量。

**第四步：擬合冪律。**
把質量分布擬合成 dN/dm ∝ m^(−α)，得到 IMF 斜率。

### 背後的邏輯

整條路徑是**逐步翻譯**：觀測星等 → 扣除雙星汙染 → 對應到理論模型 →
反查質量 → 統計質量分布。每一步都把資料往「真實物理量」推進一步。

這個邏輯的前提是**每一步都能被乾淨地分離**：先把雙星處理掉，
才能得到乾淨的主序去定年齡；先定好年齡，才有質量-光度關係可用；
先有質量，才能統計質量函數。

### 這個邏輯的三個弱點

**弱點一：雙星必須先被移除，但移除的是「偵測得到的」那一批。**
我們實測四種判定法的兩兩重疊最高只有 18%，代表每種方法都有各自的盲區。
移除偵測到的雙星之後，剩下的樣本仍混著三種方法都看不見的雙星，
而且**你已經以為清乾淨了，所以不會再修正**。

**弱點二：步驟之間是單向的，錯誤會往下傳而不會被發現。**
如果雙星沒清乾淨，主序會系統性偏亮，定出的年齡就偏了；
年齡偏了，質量-光度關係就偏了；質量偏了，α 就偏了。
而整條鏈裡沒有任何一步會回頭檢查上一步。

**弱點三：未解析雙星讓質量被高估。**
兩顆星的光加起來像一顆亮星，反查質量時得到的是一個偏大的值。
這會系統性地把質量函數往重的方向推，量到的 α 偏平。
arXiv:2603.15779 指出這個偏差**不隨樣本數縮小**（統計誤差以 1/√N 遞減，
偏差是常數），所以樣本越大只會「越精確地錯」。

---

## 二、前向模型的流程與邏輯

### 步驟

**第一步：猜一組參數。**
年齡、消光、雙星比例、IMF 斜率、金屬量、質量比分布 —— 六個數字。

**第二步：生成一整個虛擬星團。**
1. 依假設的 IMF 抽出四萬顆星的質量
2. 用年齡與金屬量查 isochrone，得到每顆星的絕對星等與顏色
3. 依雙星比例決定哪些是雙星，依質量比分布抽伴星質量，
   把兩顆星的**流量相加**再轉回星等（這就是未解析雙星的物理）
4. 加上距離模數與消光
5. 加上與真實觀測同等量級的測光誤差
6. 套用與觀測相同的星等篩選

**第三步：比較合成與觀測的 CMD。**
把兩張圖都切成 40×50 的格子，數各格有幾顆星，用 Poisson 概似
算出「這組參數解釋觀測資料的好壞程度」。

**第四步：換一組參數重來。**
用 MCMC 讓走者在六維參數空間中移動，往機率高的方向偏，
最後得到的樣本分布就是後驗機率分布。

### 背後的邏輯

不做翻譯，而是**模擬**。核心問題從「這批觀測資料對應什麼物理量」
變成「什麼樣的物理設定，會產生出跟我們看到的一模一樣的資料」。

這叫「前向」，是因為方向跟傳統方法相反：
傳統是資料 → 物理量（反推），前向模型是物理量 → 資料（正推），
再用比對來搜尋。

### 這個邏輯解決了什麼

**雙星從汙染變成參數。**
不需要判斷任何一顆星是不是雙星 —— 雙星比例是模型的一個數字，
由「整片點雲的形狀需要多少雙星才能複製出來」決定。
偵測不到的雙星也自動被計入，因為它們確實貢獻到了觀測 CMD 的形狀。

**步驟之間不再單向。**
六個參數同時被同一份資料約束，沒有誰先誰後、誰假設誰。
這消除了傳統方法「雙星→年齡→質量→α」那條錯誤傳遞鏈。

**能揭露參數之間的簡併。**
輸出是後驗分布而非單一數值，因此可以看到參數兩兩之間的相關性。
我們實測到年齡↔金屬量 +0.98、年齡↔消光 −0.96 —— 這些在傳統方法裡
完全看不見，因為它每一步只給一個數字。

### 這個邏輯的代價

**必須把觀測的所有效應都模擬進去。**
測光誤差、星等選擇、完整度 —— 漏掉任何一項，合成資料就與觀測不可比。
我們實測過：一度忘記對合成星團套用 G < 18 的星等切，暗端就會多出
觀測裡不可能存在的星。

**計算量大得多。**
傳統方法擬合一次 isochrone 是秒級；前向模型的一次 MCMC 是小時級
（六參數、48 走者、三萬步，在 ARM64 原生 Python 加八行程平行下約 53 分鐘）。

**概似函數的選擇會影響結果。**
我們用的 Poisson-Hess 概似把 2,000 個格子當成互相獨立，
在 1,078 顆星上產生過度自信的窄峰，導致誤差棒嚴重低估。
這是傳統方法不會遇到的問題（因為它根本不給嚴謹的誤差棒）。

---

## 三、兩者的比較

| | 傳統方法 | 前向模型 |
|---|---|---|
| 方向 | 資料 → 物理量（反推） | 物理量 → 資料（正推後比對） |
| 雙星的地位 | 要被剔除的汙染 | 模型的一個參數 |
| 偵測不到的雙星 | 殘留在樣本裡，無法修正 | 自動計入（因為影響整體形狀） |
| 步驟關係 | 單向，錯誤往下傳 | 同時求解，互相約束 |
| 輸出 | 每個量一個數字 | 後驗分布＋參數相關性 |
| 簡併 | 看不見 | 顯示為相關矩陣 |
| 計算量 | 秒到分鐘 | 小時 |
| 主要風險 | 步驟間的錯誤傳遞 | 觀測效應模擬不完整、概似函數選擇 |

### 實測到的差距

同一批 1,078 顆成員星：

| 方法 | α |
|---|---|
| 傳統（每顆星當單星指派質量） | 1.980 ± 0.069 |
| 前向模型（雙星內建） | 2.26 – 2.35 |

差約 0.3，是統計誤差的四倍以上。方向與 arXiv:2603.15779 的預測一致
（忽略雙星使斜率偏平）。

---

## 四、為什麼兩種都要做

**不是為了證明哪個比較好，而是為了量化方法選擇造成的影響。**

如果文獻上不同星團的 α 差異在 0.3 這個量級，而我們證明光是換一種
雙星處理方式就能造成 0.3 的差異，那麼「IMF 是否隨環境變異」這個問題
就必須先排除方法造成的部分。這正是教授在初步說明裡點出的核心爭議。

所以論文的邏輯應該是：
1. 用同一批成員星、同一套 isochrone，只讓雙星處理方式不同
2. 量出兩種方法各自給的 α
3. 把差距與文獻上「星團之間的 α 差異」相比
4. 據此討論：文獻上的變異，有多少可能是方法造成的

這個問法不需要我們解決 IMF 普適性的爭議，
只需要誠實地量出「方法本身能製造多大的差異」——
而這正是目前文獻缺口所在。
