# 明天（2026-08-20）計畫

寫於 2026-08-19 深夜，使用者去睡前交代「繼續跑，不要停」。這份文件是給
早上回來時的快速定位，不是新的規劃文件——實際工作方向仍以 `WORK_BOARD.md`
待認領工作表為準。

## 昨晚（2026-08-19）夜間自動處理摘要

- **PR 全部解衝突完畢**：#62、#63 已由你合併。#64/#65/#66/#67/#69/#70/#73
  七個目前都是 `mergeable: clean`，可以直接在 GitHub 上依序點合併，過程中
  若因為你合併順序而重新出現衝突，我會自動偵測並修好。
- **Kaggle 佇列半夜當機過一次**：`kaggle_queue.py` 在 22:36 左右不明原因
  終止（不是崩潰堆疊，比較像被外部中斷），本機的 `run_queue.py` 有自動
  重啟但 `kaggle_queue.py` 沒跟上。已手動重啟兩次：第一次先用應急版本，
  第二次換成 PR #66 修好的版本（`probe_kernel_status()` 那套更穩健的復原
  邏輯）。**過程中發現一次真的重複派工**（`verify_bprperr_off_v2_rep1`
  同時在 justinlan11 跟 account5/trumpren 兩個帳號上跑）——不影響結果正確性
  （兩邊用完全相同的種子公式，數值上會算出一樣的東西），只是浪費一個
  帳號的運算時數，沒辦法刪除重複的 kernel（權限被系統擋下）。**早上先
  檢查 `logs/kaggle_queue_done.txt` 有沒有出現兩筆 `verify_bprperr_off_v2_rep1`**，
  有的話代表兩邊都跑完了，只需要留一份結果、另一份可以直接忽略。
- **`kaggle_queue.py`／`restart_queue_on_boot.ps1` 在主機上的即時版本已經
  手動同步成 PR #66 修好的版本**（原本落後，這是造成昨晚問題的根本原因
  之一）。等 PR #66 合併後這會是正式狀態，不用再手動同步。
- 詳細過程與每一步判斷記在這次對話的逐字紀錄裡，這份文件只列早上要做
  的事。

## 早上進來後，建議依序做這幾件事

### 1. 健康檢查（5 分鐘）
```powershell
Get-Process python | Where-Object { (Get-CimInstance Win32_Process -Filter "ProcessId=$($_.Id)").CommandLine -match "kaggle_queue.py|run_queue.py" }
```
確認兩條佇列的行程都還活著。若 `kaggle_queue.py` 不在，用同樣的
`Start-Process -WindowStyle Hidden` 手動重啟一次（指令見 `restart_queue_on_boot.ps1`）。

### 2. 確認 Kaggle 進度
```powershell
Get-Content C:\Users\yutun\Documents\m45-imf-analysis\logs\kaggle_queue_done.txt -Tail 20
```
看 `verify_bprperr_off/on_v2_rep0-4`、`p6b_inject_lowmass_v2_t0-2_retry*`、
`p2_final2_v3_rep9_retry` 這幾組有沒有陸續完成。全部完成後：
- **headline**：`fit_real_p2final_v3_rep0.npz` ~ `rep9.npz` 十個檔案應該
  都齊了，依 offset 沿 axis=0 串接成 `results/fit_real_p2final_v3.npz`，
  更新 `RESULTS_LOG.md` 與 `LIMITATIONS.md` A1/A2——這是本次夜間工作最後
  一塊拼圖，串起來就是正式 headline 數字。
- **verify_bprperr_v2**：off/on 各 5 個分片（rep0-4）串接後，跟 B1 既有
  的 off/on 數字比較，確認乾淨對照下 alpha 是否還是幾乎不變（先前粗略
  比較是 2.420 vs 2.420）。

### 3. 繼續 PR 合併
剩下 7 個 clean 的 PR，昨晚已經逐一列給你連結。合併完後跑一次
`git pull` 到本機工作樹，確認 `fit_real.py`／`kaggle_queue.py` 等檔案
跟合併後的 main 一致（本機這幾天一直用工作樹手動同步多個未合併分支的
內容，全部 PR 合併完後這個手動同步的需求會自然消失）。

### 4. WORK_BOARD 剩下的工作（優先度由高到低）

- **D1（BHAC15 擬合比較）**：`d1_bhac_check` 已排進 `queue.txt`（本機
  8 核佇列），正式規模（`n_syn=40000`）跑完後看撞牆問題是雜訊還是真實
  簡併，見 `LIMITATIONS.md` D1 的完成條件。
- **D2（sensitivity_sweep）**：`membership_threshold` 掃描卡在原始 Gaia
  查詢 CSV 抓不下來（ESA TAP 服務對這個查詢會主動逾時取消，見
  `WORK_BOARD.md` D2 那行的完整診斷與可能繞過法）。`stars_per_cluster`
  需要 pyUPMASK 環境，這台機器還沒裝。兩者都不急，可以之後找空檔處理。
- **Coma Berenices Tier 1**：前置阻擋條件是 `LIMITATIONS.md` D8 的四個
  PR #11 正確性問題必須先修好，還沒人認領，是接下來最大的一塊新工作。
- **Hobart 對照鞏固工作**（D11/D12/D13）：`WORK_BOARD.md`
  「對照 Hobart et al. 2026 的鞏固工作」表，D14 已完成，D11/D12/D13
  還沒人做。

### 5. Kaggle token 輪替（一直沒處理，提醒一下）
之前建議過全部 7 組 Kaggle API token 因為在 Dropbox 同步的 JSONL 逐字稿
裡以明碼形式出現過，應該輪替。這件事一直沒做，風險持續存在，早上有空
可以處理（去 Kaggle 帳號設定頁重新產生 token，更新本機
`kaggle_accounts.json`）。

## 不用做的事

- 不要重跑 `radial_r1/r2/r3/rall`（PDMF→IMF 第 2 步四項都已有初步值）。
- 不要重新開始 `p2_final2_v3`（headline 正規來源固定在 Kaggle 分片，
  本機 `queue.txt` 那筆已經停用並寫明原因）。
- 不要因為看到某個項目「尚無認領工作」就假設沒人在做——先查一次開著的
  PR 列表，`LIMITATIONS.md` 開頭有這條提醒，這幾天已經因為忽略這條
  重工過一次。
