# 變更歷史

規則類文件（`CONTRIBUTING.md`、`AGENTS.md`、`README.md`、`WORK_BOARD.md`、
`WORK_BOARD_DONE.md`、`LIMITATIONS.md`、`QUEUE_ALERTS.md`、`STATE.md`、
`docs/reference/*`）只寫現在的規則／現在的狀態，不寫「為什麼改、什麼時候
改」。這些脈絡性內容統一搬到這裡，依來源文件分節、依日期排序。找某個
規則的緣由，先看規則文件本身有沒有連過來這裡的章節，再來這裡查對應
的日期。

這份文件本身是純附加型：新的變更紀錄加在對應章節尾端，不回頭改舊行。

---

## CONTRIBUTING.md

- 2026-08-11：`LIMITATIONS.md` 的撰寫格式與分級規則從 `CLAUDE.md` 搬到
  這裡，因為這是給所有人與所有 agent（不只 Claude）遵守的規則。
- 2026-08-12：新增「規劃類文件的結論預設只是初步判讀，不是定案」的
  規則——避免多 agent 協作時把彼此的暫定判斷誤當成已經拍板的決議。
- 2026-08-15：新增「工作前先開分支、走 PR」的多 agent 協作規則，因為
  repo 開始同時有多個 AI agent 與人類協作者工作。
- 2026-08-12：`WORK_BOARD.md` 認領規則新增。起因是 Codex 接續了 Claude
  稍早卡住的多星團驗證工作，兩邊動到同一個檔案，事後才發現是接續、
  不是重工——加這條規則避免同一件事再發生。
- 2026-08-21：`.gitattributes` 加 `merge=union` 處理純附加型檔案的合併
  衝突；同一天 CodeRabbit review 指出純集合比對抓不到「重複行被刪一份」
  的漏洞，`check_append_only.py` 改用 `Counter` 多重集合比對。
- 2026-08-21：查了當時開著的 11 個 PR，42 則 CodeRabbit 留言裡 41 則
  完全沒人回覆，因此加了兩個疊起來的硬關卡：`.coderabbit.yaml` 開
  `request_changes_workflow`，加上 GitHub branch protection 把
  `required_approving_review_count` 從 0 提高到 1（CodeRabbit 卡在
  Changes Requested 才會真的擋合併）。同一天訂正 `@coderabbitai
  resolve` 的用法說明（原本寫錯：這個指令必須以 PR 頂層留言送出，
  而且會一次 resolve 掉全部留言，不適合處理單一誤判）。
- 2026-08-22：上述硬關卡（`required_approving_review_count=1`）取消，
  改回 0——不是判斷錯了，是代價太高：CodeRabbit 免費方案審查額度
  遠比預期緊，一次處理十幾個 PR 就會整個打光，額度用完期間硬關卡
  等於「PR 永久卡住、誰都合併不了」，且每次 merge 都會讓其他 PR
  產生新衝突。改成「不強制擋，但清楚可見」：`request_changes_workflow`
  保持開著，但合併不再被卡住，由人自行判斷要不要處理 CodeRabbit
  的意見。同一天發現 CodeRabbit 的 commit status 即使額度用完、根本
  沒送出正式 Review，也照樣顯示「Review completed」綠燈，因此寫了
  `scripts/tools/coderabbit_status.sh` 拿 `commit_id` 跟 PR head SHA
  比對來判定是否真的審過。
- 2026-08-23：新增零之三「`QUEUE_ALERTS.md`」規則——Gate B/C 沒過時
  只印警告到 log 沒人會固定去看，改成寫進一份規定每個 agent 開工前
  都要讀的檔案。
- 2026-08-23：`WORK_BOARD.md` 從單一 append-only 大檔拆成
  `WORK_BOARD.md`（待辦）＋`WORK_BOARD_DONE.md`（已完成），因為累積
  到 449 行後歷史紀錄跟待辦事項混在一起，找不到「現在到底剩什麼」。
- 2026-08-24：`WORK_BOARD.md` 改成統一大表格＋狀態只有兩種，隨後又
  訂正成「一個任務一張小表格＋緊接的說明文字，交錯排列」，取代先前
  誤讀使用者提供的參考圖示做出的一大表格＋一大段說明格式。
- 2026-08-24：`WORK_BOARD_DONE.md` 拿掉「狀態」與「備註」欄，改成只
  留結論（產出了什麼、之後哪裡用、對專案的幫助），不再保留逐字過程
  紀錄；因此也從 `check_append_only.py` 的強制 append-only 檢查與
  `.gitattributes` 的 `merge=union` 清單移出——整理濃縮變成這份文件
  的正常維護動作，不該被擋下來。
- 2026-08-13：新增 `LIMITATIONS.md` 與 `WORK_BOARD.md` 雙向追蹤規則
  （零之一）。起因是這兩份文件過去各自維護，讀者得自己在腦中兜起來，
  常常兜錯——例如 A2 的說明段落曾經被編輯到只剩半句話，卻沒人發現。
- 2026-08-13：新增「新發現要順手同步 `PAPER_OUTLINE.md`」規則（零之二）
  ——這條規則常被忽略成「只改 `LIMITATIONS.md`／`WORK_BOARD.md`，沒
  回頭看 `PAPER_OUTLINE.md`」，導致論文大綱的敘述變成過期資訊。
- 2026-08-16：新增 `docs/PROJECT_TIMELINE.md` 定期更新規則（零之三）。
- 2026-08-24：新增本節規則——所有規則類文件禁止「因為 X 所以改 Y」
  這種帶日期的脈絡敘事，脈絡統一搬進這份 `CHANGELOG.md`。

## AGENTS.md

- 2026-08-11：建立，作為給 Codex 與其他非 Claude agent 的進入點，
  一句話摘要 `CONTRIBUTING.md` 的核心規則，避免每個 agent 各自重新
  發現一次同樣的協作規則。

## WORK_BOARD.md / WORK_BOARD_DONE.md

- 見上方「CONTRIBUTING.md」小節 2026-08-23／2026-08-24 各行——兩份
  文件的格式沿革跟 `CONTRIBUTING.md` 零之四規則的沿革是同一件事，
  不重複記錄。

## LIMITATIONS.md

- 2026-08-04：發現多筆早期分析誤把分箱格數當成獨立證據、誤算系統
  誤差倍數，A 級項目多次修正，詳見 `docs/reference/REFUTED.md`。
- 2026-08-10：發現 MIST 網格覆蓋不足造成的簡併問題，`p9b`／`p9b2`／
  `p9b3` 系列結果全部作廢，改用涵蓋更完整的新網格重跑。
- 2026-08-11：發現「傳統法 α 定義域跟前向模型不一致」是造成 0.3–0.4
  差異的主因，不是雙星效應——對齊擬合範圍後差異塌到 0.01–0.11。
- （更早期與更細的個別條目沿革，見 `docs/reference/REFUTED.md`，那份
  文件本身就是「曾經的說法 vs 實測結果」的結構化記錄，不需要在這裡
  重複。）
- 2026-08-11～20：A1（精修 bug）與 A2（金屬量先驗被寫死）的 headline
  重跑過程——`p2_final2_v3` 10 次重複因為單一 Kaggle kernel 跑不完
  免費 session 時數上限，改用 `--repeat-offset` 拆給多個 kernel／帳號
  分開跑，中途一度發現已經跑完的 5 個 kernel 放了一天多沒人拉回來，
  最後 10/10 次於 2026-08-20 到齊定案（alpha=2.382±0.068）。過程中
  另外發現並修好一個會讓「重跑」因為沒有 manifest 而被靜默判定為
  「沿用舊檔」的 bug（`checkpoint.check_manifest()`）。
- 2026-08-14：A4（MH 自由參數吸收系統偏差）用修好的精修 bug 與
  Kaggle `results/` 目錄 bug 之後的乾淨程式碼重跑確認，PARSEC／MIST
  年齡差距從舊數字的「50–63 對 100–126」修正為「54.1 對 108.0」。
- 2026-08-16～23：A5（PDMF vs IMF）的徑向 α(<r) 從單次無誤差棒的
  `_prelim`（r1=2.10、r2=2.43、r3=2.50、rall=2.43，發現 rall < r3
  不是單調）逐步補上 `--repeats 5 --refines 3,3` 的正式統計版，r2
  最後到齊（2026-08-23）。r3 vs rall 的非單調問題先用配對比較回答
  （未達顯著），2026-08-24 依 CodeRabbit review 訂正成四組比較需要
  做多重比較校正（Holm），r2−r1 的 nominal p=0.028 校正後不再顯著。
- 2026-08-13：B1（測光誤差模型）第一次 off／on 對照因為兩次呼叫用了
  不同的精修程度（一次在精修 bug 修正提交前啟動、一次在之後），不是
  乾淨對照；2026-08-20～22 用同一組種子、同精修程度重跑 `_v2` 版本
  才得到可信的配對比較結果。
- 2026-08-13：C3 查證分光雙星能否解釋 RV 離群比例時，先用
  `WORK_BOARD.md` 建議的 Sana+2012（大質量 O 型雙星校準）週期分布，
  發現物理上不對應 M45 的 F-K 型矮星母體，改用 Raghavan+2010 太陽型
  場星週期分布重算。
- 2026-08-19：C5（A_V 被壓到物理下限）第一版 `trunc_exp` 替代分布的
  location 公式有 bug，會讓兩種分布在同一組 (A_V, dav) 下的平均消光
  不同，混進「平均消光不同」這個混淆因子；CodeRabbit review（PR #63）
  抓到後訂正為解 `E[max(loc+Exp(dav), 0)] = A_V` 得到的正確公式。
- 2026-08-13：D8（PR #11 多星團驗證的四項程式正確性問題）發現、修正、
  合併後重跑驗證的完整過程，2026-08-23 結案，已整條搬到
  `RESOLVED.md`（原 D8）。2026-08-24 從 `LIMITATIONS.md` 移除同一條
  的重複殘留內容。
- 2026-08-18～21：D1（BHAC15 等時線）網格建置、發現需要對稱亮端
  截斷、煙霧測試規模與正式規模先後在同兩個維度（f_bin、alpha）撞牆，
  判斷比較像真實簡併而非蒙地卡羅雜訊，但只有單一 seed 驗證過。
- 2026-08-19～24：D2（membership_threshold 敏感度）從可行性驗證
  （原始 Gaia 查詢 CSV 缺失、卡在 ESA TAP 逾時）到第一批正式規模
  數字到齊，2026-08-24 CodeRabbit review 指出 `selection.npz` 沒有
  隨門檻重新迴歸這個但書。
- 2026-08-19～20：D12（亮端完整度）用 HIPPARCOS 交叉查證，發現範圍
  比原問題描述更大（G=4.0–5.2 之間的星也整批消失），根因追查卡在
  缺少原始 Gaia 查詢檔案。
- 2026-08-21：D14（system MF vs stellar MF）核對 `JointModel.
  synthesise()` 抽樣順序，確認並寫進文件；同日用蒙地卡羅實算出兩種
  定義差多少。
- 2026-08-23：D15（貼牆偵測邊界）全面複查時發現偵測用的邊界比先驗
  實際生效的邊界寬，修好後 2026-08-23 CodeRabbit review 進一步指出
  維度數不符時不該整批放棄交集，已改成逐維對應。

## QUEUE_ALERTS.md

- 2026-08-23：建立。Gate B／Gate C 沒過時原本只印一行警告到
  `logs/queue_runner8.log`，需要有人主動翻 log 才會發現；額度用完的
  CodeRabbit review 已經證明「訊號存在但沒人固定看」等於沒有，因此
  改成寫進這份檔案，並規定每個 agent 開工前都要讀，取代「靠人記得
  去查」。

## docs/reference/PREFLIGHT.md

- 2026-08-20：建立。起因是核對 33 筆佇列紀錄後發現 84% 的機時（86.6
  小時）是白跑的，其中 73% 來自 `multi_stage_best()` 的 `--refines 3`
  沒有真的精修、只回傳粗網格 argmax（`LIMITATIONS.md` A1）卻正常
  `exit 0`——這種「看起來正常但數字是錯的」失敗，遠比當掉／CornerError
  這種「大聲失敗」更難發現、代價更高，因此設計成三道關卡（A：解析度／
  設定／網格自我測試，B：意圖與輸出對帳、續傳能力，C：跑完後的量化
  簽章驗證）。
- 2026-08-20（同日）：檢查從「只在 `run_queue.py` 呼叫 `--preflight`
  時才發生」改成「焊進五支計算腳本自己的 `main()`」，因為 Kaggle、
  x64 協作機等環境常常直接執行腳本、完全不經過 `run_queue.py`，原本
  的檢查對這些路徑沒有保護力。
- 2026-08-20（同日稍晚）：B1／B2（意圖對帳、輸出衝突）從 `fit_real.py`
  專屬推廣到全部五支腳本，作法是讓 `preflight.workload_audit()`／
  `output_audit()` 只認一組共同形狀，每支腳本自己把旗標轉換成這組
  形狀——同一天也把 B3（續傳能力）補齊到另外四支診斷腳本，抽出共用
  模組 `scripts/tools/checkpoint.py`，過程中順便修掉一個 `NpzFile`
  沒關檔案控制代碼、在 Windows 上偶發 `PermissionError` 的 bug（改成
  `with` 讀取＋對 `os.replace()` 的 `PermissionError` 加重試）。
- 2026-08-23：Gate B／C 沒過的處理方式從「只印警告」改成「寫進
  `QUEUE_ALERTS.md`」，且 Gate C 不通過時狀態改記成 `gate_c_fail`
  （不是 `ok`），理由跟這份文件本身建立的理由一致——訊號存在但沒人
  固定去看等於沒有。

## docs/reference/CLOUD_WORKERS.md

- 2026-08-22：建立，作為 SSH worker（GCP／Oracle 等 VM）的手動設定
  說明，跟既有的 Kaggle worker 是同一個「worker」抽象的另一種實作。
- 2026-08-23：訂正建議的 Ubuntu 版本（原寫 24.04，實際應為 26.04
  LTS，24.04 是過時資訊）；VM 環境安裝建議改為推薦 venv，取代原本
  的 `--break-system-packages`（CodeRabbit review 建議）。
- 2026-08-23：實測踩到 GCP 帳號隔離的坑——瀏覽器主控台「SSH」按鈕
  預設登入帳號跟 `ssh_workers.json` 裡填的帳號如果名字不同，GCP 會
  建立兩個互相看不到彼此環境的獨立帳號，因此補上「一開始就決定好
  要用的使用者名稱」的操作建議。
- 2026-08-23：實測發現 `ssh_sync.py run` 不支援 `--minimal` 頂層旗標、
  且 `--args` 傳遞帶 `--` 開頭的值時空白分隔會被 argparse 誤判，訂正
  成必須用 `--args=值` 的等號寫法。
- 2026-08-23：新增集中式團隊派工說明——科展隊員不需要拿到任何真實
  憑證就能透過 PR 排隊，讓運算機器自動接工作。

## docs/reference/METHODS.md

穩健性檢驗的推翻過程（第 1 步／pyUPMASK 成員判定）：

- 第一輪只測設定的影響，在當時因為 F1 最高而選用的 P ≥ 0.99 下，得到
  「PCA 開關差 36.1%」「投影修正差 3.5%」等數字，一度誤判 PCA 是最
  關鍵的設定。
- 第二輪意識到沒有雜訊底線就無法宣稱任何效應是真的，同資料同設定
  只換亂數種子重跑三次，發現種子間差異達 31.7%——第一輪的三個數字
  全部埋在雜訊裡，不成立。
- 第三輪逐門檻查成員數才發現不穩定集中在尾巴：P ≥ 0.5 時七組變因
  全部落在 1,302–1,325（離散度 1.1%），P ≥ 0.99 時卻炸到 48%——
  科學結論對設定不敏感，只有門檻位置敏感。
- 第四輪追根因：0.5–0.7 之間只有約 20 顆星、0.7–0.9 只有 33–48 顆，
  但 0.999–1.0 擠了 1,092 顆，0.99 門檻正好卡在密度堆的山腳，機率
  小幅移動就整批跨過門檻。
- 第五輪讀 `modules/KDEAnalysis.py` 確認機率是 `1/(1 + L_field/L_memb)`
  概似比後驗，成員分布核心的星概似比趨近零、後驗飽和到 1——堆積
  反映的是「星團跟場星分得多開」，不是「對成員身分多有把握」。

過程中被推翻的具體結論：汙染率一度估「切完仍有九成九」，實測 81%；
recall 天花板一度用 `Nt/N` 推出 59%，但 `Nt` 是三維判準不是角距判準，
實測天花板 92.7%；「投影效應是內部彌散的 5.2 倍」是拿跨天區極差比
標準差，不對等，實際貢獻約 0.62 mas/yr，與內部彌散 0.78 同量級；
用 F1 對 HR23 挑機率門檻，等於用對照目錄調自己的超參數，且挑到了
方法最脆弱的點；「可與 Cantat-Gaudin 直接比較」不成立，兩者機率
構造不同、數值不可通約。共同根源：在建立雜訊底線之前就開始解讀差異。

## STATE.md

這份文件的本質就是特定時間點的交接快照（2026-08-13～15，x64 協作機，
交接給 Acer AI 16），2026-08-21 覆查時已確認大半內容過期。2026-08-24
簡化時，把仍然現行有效的環境備忘與 checklist 留在原檔，其餘屬於
「那次交接當下發生了什麼」的敘事（Kaggle 多帳號派工排查、PR 進度、
逐項核對結果）視為歷史事件本身，不搬進本檔重寫——需要回顧完整交接
過程請直接看 git 上 2026-08-15 前後對這份文件的版本。
