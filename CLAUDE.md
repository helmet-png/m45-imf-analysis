# M45 IMF 專案專屬規則

全域規則在 `~/.claude/CLAUDE.md`；這裡只放這個專案特有的規則。

## 多 agent／多人協作

這個 repo 有多個 AI agent（Claude、Codex 等）與多個人同時工作。協作規則
在 `CONTRIBUTING.md`（所有人與所有 agent 共用），開工前先讀。重點：

- `main` 一律走 PR；新工作先開分支 `claude/<主題>`。
- 每個新結果記進 `results/RESULTS_LOG.md`；commit 要標明身分。
- 開工前先讀 `QUEUE_ALERTS.md`：`run_queue.py` 的開跑前檢查或跑完驗收
  沒過時會記在那裡（零之三）。
- 規劃類文件（`docs/planning/`、`WORK_BOARD.md` 說明、PR 描述）裡的結論
  預設只是當下想法：語氣用「初步判讀」而非「結論是」，推理與數字照樣
  寫完整；發現別人或自己稍早的判斷站不住腳就直接改。`LIMITATIONS.md`
  的 A–D 分級與引用可信度標記照舊使用（零之二）。
- 你沒有 senior24／協調 VM 的 SSH 金鑰，只能請使用者在瀏覽器 SSH 分頁
  貼指令、貼回結果；指令寫法見零之五。
- 只改派工清單（`cloud_queue.txt` 等）的 PR，標題以 `[派工]` 開頭讓
  CodeRabbit 跳過；還改了其他檔案就不可以帶（零之一）。
- 編輯 `LIMITATIONS.md` 前讀 `CONTRIBUTING.md` 第五、六節。

## 發現有未知修改時

未知修改通常來自其他 fork 或 session，照常工作、不用追查來源。只有判斷
修改本身有問題時，才回報使用者哪裡可能有問題。

## 教學者角色（只在使用者明確指派時適用）

多數 session 做的是研究本身（跑計算、改程式、更新 `LIMITATIONS.md`／
`PAPER_OUTLINE.md`），這一節不適用於它們。只有使用者明確把對話指定為
教學者（例如說「教我」「跟我解釋」「準備向教授報告」）時才套用；不要
因為讀到這一節就限制自己不能改東西。

教學者根據其他 session 的最新進度，把研究內容教會使用者：

- **只指出問題，不動專案本身**（程式碼、`LIMITATIONS.md`、
  `PAPER_OUTLINE.md`、git 分支等）。發現結果過期、文件矛盾、bug、git
  狀態混亂，回報使用者，交給指派的負責 session 處理——自己修容易跟
  其他 session 正在做的事衝突。教學文件以外的檔案即使明顯過期或錯誤，
  也只在教學文件裡或回報時指出。
- **教學文件本身**（`docs/teaching/教學_*.md`）可以直接寫、直接訂正，
  也可以 commit。但優先權低於其他 session：commit 前若發現跟別人的
  工作有衝突或不確定（分支狀態、他人未 commit 的修改），先讓步並回報，
  不搶著 commit，更不用 `--force`、`reset --hard` 這類會覆蓋別人東西的
  操作。

## 教學文件撰寫原則

所有教學文件都照這些原則寫，範例是 `docs/teaching/教學_傳統法誤差核算.md`
與 `docs/teaching/教學_前向模型.md`。讀者是使用者本人，用來準備向教授
報告。

1. **讀者設定為高一升高二程度。** 不假設懂微積分、統計或天文物理。每個
   數學工具（積分、機率密度、最大概似估計、逆變換抽樣、貝氏先驗、
   Poisson 分布等）在用到時才引入：先用生活化類比建立直覺，再推導公式，
   最後對照程式碼確認一致。
2. **每個數學結論都對照程式碼。** 每條推導出的公式旁附對應的程式碼節錄
   （檔名＋函式名），讓讀者確認推出來的數學就是電腦實際在算的東西。
3. **誠實記錄走過的錯路。** 寫出實際犯過的方法錯誤（誤用文獻、誤判撞牆
   訊號、精修 bug 等）的「起因 → 追查 → 修正 → 對既有結論的影響」。
   還沒解決的標「待重跑確認」，不為了好看假裝已定案。
4. **結構統一**：開頭三分鐘摘要、完整推導主體、結果表格、文獻對照表、
   「這支程式／模型沒有回答的問題」，最後一節固定是「給教授報告的講稿
   大綱」（含教授可能問的問題與建議答法）。
5. **數字標明可信度。** 引用的數字若落在已知 bug 或未定案項目的影響範圍
   內，直接在數字旁標注，不分散到別處。
