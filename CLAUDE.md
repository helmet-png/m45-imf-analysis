# M45 IMF 專案專屬規則

（全域規則見 `~/.claude/CLAUDE.md`；這裡只放這個專案特有、其他專案用不到的規則。）

## 多 agent／多人協作（2026-08-11 起）

這個 repo 現在有多個 AI agent（Claude、Codex 等）與多個人同時工作。
**協作規則見 `CONTRIBUTING.md`（給所有人與所有 agent 讀，不是 Claude 專屬）**：
`main` 分支所有人都要走 PR、`results/RESULTS_LOG.md` 記錄每次新結果、
commit 要標明身分。開始任何新工作前先開分支 `claude/<主題>`。

## 教學者角色（僅當使用者明確指派時適用，不是每個 session 的預設角色）

**這一節不是給所有讀到本檔的 session／agent 的通用規則**——這個 repo
被多個人與多個 agent 共用，大多數 session 的工作是研究本身（跑計算、
改程式、更新 `LIMITATIONS.md`／`PAPER_OUTLINE.md`）。以下規則**只在
使用者明確把某個對話指定成「教學者」角色時才適用**（例如使用者說
「教我」「跟我解釋」「準備向教授報告」），不要因為讀到這一節就自己
套用「只能指出問題不能改」這個限制——那樣反而會讓正常的研究型 session
不敢做該做的事。

**教學者角色的行為**：根據其他 session 的最新進度，把研究內容教會
使用者。**只能指出問題，不要自己主動去改、也不要主動去編輯專案本身**
（程式碼、`LIMITATIONS.md`、`PAPER_OUTLINE.md`、git 分支操作等）。
發現任何問題（結果過期、文件矛盾、bug、git 狀態混亂）都要回報給
使用者，但不要自己動手修——主動修改容易跟別的 session 正在做的事
衝突、打亂對方的進度，交給使用者指派的負責 session 處理。

撰寫與維護**教學文件本身**（`教學_*.md` 這類，教學者角色自己的產出）
不受此限，可以直接寫、直接訂正內容。**也可以把教學文件 commit 上去**
（不只是留在本地），但優先權比其他 session 的動作低——commit 前
若發現任何跟其他 session 正在進行的工作（分支狀態、其他人的
uncommitted 修改）有衝突或不確定的地方，先讓步、回報使用者，不要
搶著 commit 或動用 `--force`／`reset --hard` 這類會覆蓋別人東西的
操作去清理衝突。教學文件以外的任何檔案，即使看到明顯過期或錯誤的
內容，也只在教學文件裡或回報時指出來，不要跨過去改。

## 發現有未知修改時

通常為因其他 fork 所導致的修改，不需要主動提問，或再耗費 token 去思考是誰修改的。但是，如果對這些未知的修改認為有問題，就回報讓使用者知道哪裡可能有問題。

## LIMITATIONS.md 待辦分類標準（2026-08-10，使用者質問後訂定）

**起因**：LIMITATIONS.md 累積了很多「待辦」「待修」「尚未」標籤的項目，
逐一體檢後發現裡面混了兩種完全不同的東西——一種是真正還沒發生的風險，
一種是已經確認存在的缺陷，只是被放進清單裡看起來就不緊急了。

**任何新限制要放進「待辦／待修／尚未」之前，先過這兩關**：

1. **修正它會不會波及已經跑完的一切、需要全部重跑？**
   判斷方式：這個問題是不是被**所有**（或大部分）產出數字的腳本共用
   （同一個誤差模型、同一個先驗設定、同一組寫死的網格常數）？如果是，
   修正它會同時動到所有引用它的既有結果，不是修好就結束，還要回頭
   逐一確認每個受影響的結果有沒有變。這種問題不能單純排進待辦佇列
   慢慢做，必須先評估波及範圍，優先處理或至少先量化「不修的代價」。

2. **它會不會影響研究方向或主要結論**（表 4 穩健性、alpha 的正負偏移
   方向、PDMF vs IMF 這種決定論文能宣稱什麼的用詞）？如果會，
   歸進 LIMITATIONS.md 開頭「依會不會改變論文方向分類」的 **A 類**，
   不能跟一般敏感度測試待辦混在一起排隊。

**「待辦」只能收「已知、但還沒發生的風險」**——例如：還沒做的敏感度測試
（`pca_dims`、`hess_color_range` 這類）、還沒跑的額外交叉驗證
（BHAC15 等時線比對）、還沒遇到但理論上可能發生的邊界情況。
這些的共同點是：**現有結果目前沒有被它污染的證據，只是還沒去證實
「不會污染」**。

**不能放進「待辦」簡單排隊的，是已經確認存在、正在現役影響結果的缺陷**
——例如「目前用 G 查 BP/RP 誤差會低估紅星誤差」「殘留場星污染 1% 是猜的
常數、從沒驗證過」，這兩個不是「未來風險」，是**現在跑的每一次擬合都在用
這個沒驗證過的假設**。這種問題只有兩個合法的歸宿：
  - 立刻定量估計它對現有結果的影響範圍（哪怕只是量級），把結論寫清楚；
  - 或明確標成「已知現役缺陷，優先度 X，原因 Y，目前受影響的結果清單」，
    不能只寫「待修」就當作跟其他排隊項目一樣不急。

**判準一句話**：待辦清單裡的每一項都要能誠實回答「如果我現在不理它，
會不會有哪個已經寫進論文的數字其實是錯的，只是我還沒發現」——
答案是「有可能」的，就不是待辦，是要立刻處理或立刻標成緊急的現役問題。

## 這條規則怎麼用

發現新的限制、或重新整理 LIMITATIONS.md 時，套用上面兩關去分類。
2026-08-10 逐一體檢的結果與重新分類記在 LIMITATIONS.md 對應章節裡
（維護規則第 2 條：訂正要回頭改原處，不能只在新地方補一筆）。

## 教學文件撰寫原則（2026-08-11 起，以後所有教學文件都比照辦理）

範例：`教學_傳統法誤差核算.md`、`教學_前向模型.md`。這兩篇是給使用者
準備向教授報告用的完整推導文件，以後任何新的教學文件（新方法、新
章節）都要遵守以下原則，不要自己另外發明格式：

1. **目標讀者設定為高一升高二程度**：不假設讀者已經懂微積分、統計、
   或天文物理背景知識。每一個要用到的數學工具（積分、機率密度、
   最大概似估計、逆變換抽樣、貝氏先驗、Poisson 分布等），在用到的
   當下才引入，先建立生活化的直覺類比，再推導公式，最後對照真正的
   程式碼確認公式與實作一致——不是只講結論、也不是只丟公式。

2. **每個數學結論都要能對照到程式碼**：文件裡的每一條推導出來的
   公式，都要在旁邊附上對應的程式碼節錄（檔名＋函式名），讓讀者能
   親眼確認「推出來的數學」跟「電腦實際在算的東西」是同一件事，
   不是兩套各自獨立的敘事。

3. **誠實記錄走過的錯路，不是只呈現漂亮的最終結果**：文件裡要包含
   自己犯過的方法錯誤（誤用文獻、誤判撞牆訊號、精修 bug 這類實際
   發生過的事），完整寫出「起因 → 追查 → 修正 → 對既有結論的影響」，
   跟 `LIMITATIONS.md` 的維護精神一致。已知還沒解決的問題要照實
   標注「待重跑確認」，不要為了文件好看而假裝已經定案。

4. **結構統一**：三分鐘摘要（給趕時間／準備報告用）開頭、完整推導
   主體、結果表格、文獻對照表、「這支程式／模型沒有回答的問題」，
   最後一節固定是「給教授報告的講稿大綱」（含教授可能會問的問題與
   建議答法）。

5. **數字要標明可信度狀態**：任何引用的具體數字，若正好落在已知的
   bug 或未定案項目的影響範圍內，要在數字旁邊直接標注（不要分散到
   讀者找不到的地方），讓讀者一眼就知道這個數字能不能直接拿去引用。
