# 教學：traditional_accounting.py 在算什麼

寫給高中程度讀者。承接《報告_完整說明.md》第五、六章的定義。
本檔說明 P1 這支程式的每一步、為什麼這樣設計、以及它推翻了什麼。

最後更新：2026-08-07

---

## 一、這支程式要回答的問題

論文的主表（表 3）要把兩種方法放在一起：

| 方法 | α |
|---|---|
| 傳統法（每顆星當單星、指派質量、擬合冪律） | 1.98 |
| 前向模型（雙星內建於模型） | 2.40 |

但**兩個數字的誤差棒必須用同一把尺量**，否則比較沒有意義。
前向模型那邊已經用注入回收量過（第七章）；傳統法那邊原本只有
「概似曲率」給的 ±0.069，而那把尺已被證明低估約 4 倍。

所以這支程式把**完全相同**的注入回收流程套到傳統法上。

---

## 二、傳統法在這裡的精確定義

文獻上最常見的四步（見報告第 5.1 節），程式實作於 `pipeline/step5_imf.py`：

**第 1 步（只有「CMD 剔除」變體才做）**：用色光圖偏移法判定雙星並移除。
判準是：比同顏色的單星主序亮超過 0.375 星等。
0.375 是等質量雙星偏移量 0.753 的一半，用以涵蓋質量比中等的組合。

**第 2 步**：取一條等時線的**單星主序**，建立「視星等 G → 初始質量」的對應。
這裡有一個技術細節：必須先把等時線切到**單調**的那一段。
轉折點以上，巨星比主序星亮但質量未必更大，
所以「同一個 G 對應唯一質量」不成立，反查會出錯。
程式的作法是從低質量往高質量掃，只保留 G 持續變亮的部分
（`main_sequence_mass_luminosity`）。

**第 3 步**：每顆觀測星（包含沒被剔除的雙星）都用這個對應查出質量。

**第 4 步**：對質量做冪律擬合。用的是**未分箱的最大概似估計**
（`mle_powerlaw`），不是分箱後的最小平方。理由有二：
分箱方式會左右斜率；而且每格的計數服從 Poisson 分布而非常態分布。

截斷冪律的最大概似估計，數學上是這樣：質量分布為

    ξ(m) ∝ m^(−α)，  m_lo ≤ m ≤ m_hi

正規化係數 C = (1−α) / (m_hi^(1−α) − m_lo^(1−α))，
於是對數概似為

    ln L = n·ln C − α·Σ ln(m_i)

程式對 α 做一維最佳化求最大值，再用概似的二階導數估標準誤。

---

## 三、注入回收怎麼套上去

**假資料**由前向模型生成（`make_fake`），參數是我們指定的已知值：

| 量 | 值 | 為什麼這樣設 |
|---|---|---|
| α 真值 | 2.35 | Salpeter 值，方便對照 |
| f_bin | 0.00 / 0.30 / 0.45 / 0.60 | 掃過整個範圍，看雙星比例的影響 |
| 差異消光 dav | 0.30 | 與真實世界一致的已知效應 |
| 測光品質選擇函數 | 開啟 | 同上 |
| 顆數 | 1,078 | 與真實觀測相同，統計雜訊量級才一致 |

每個情境跑 20 次不同的雜訊實現，取平均與散布。

**兩個刻意對傳統法有利的設定**（所以量到的偏差是**下界**，不是誇大）：

1. 傳統法直接拿到「真的」等時線（真實的年齡、消光、金屬量）。
   實務上它還得自己擬合這些量，誤差只會更大。
2. 統計散布用同一批 1,078 顆量，與前向模型完全同基準。

---

## 四、我一開始漏掉的對照組，以及它推翻了什麼

第一次跑只掃了 f_bin = 0.30 / 0.45 / 0.60，得到偏差 −0.24 到 −0.27，
我把它解讀成「這就是忽略雙星的代價」。

**但我漏了 f_bin = 0 的對照組。** 補上之後：

| f_bin 真值 | 偏差（0.30–2.50 M☉） |
|---|---|
| **0.00（完全沒有雙星）** | **−0.265** |
| 0.30 | −0.268 |
| 0.45 | −0.240 |
| 0.60 | −0.238 |

**沒有雙星時偏差一樣是 −0.27。** 所以那個 −0.24 幾乎與雙星無關。

### 真正的成因：兩個 α 不是同一個量

- **傳統法**在 0.30–2.50 M☉ 上擬合**單一**冪律。
- **前向模型**的 α 是 Kroupa 分段冪律裡 **m > 0.5 M☉ 那一段**的冪次；
  0.08–0.5 M☉ 那一段固定在 1.3，不參與擬合。

Kroupa 分段冪律在 0.5 M☉ 有轉折。把單一冪律硬套在跨越轉折的範圍上，
得到的是兩段的某種加權平均，必然比高質量端那一段平。

### 驗證：把擬合範圍對齊到 0.50–2.50 M☉

| f_bin | 0.30–2.50（原設定） | **0.50–2.50（對齊）** |
|---|---|---|
| 0.00 | −0.265 | **−0.059** |
| 0.30 | −0.268 | −0.106 |
| 0.45 | −0.240 | **−0.013** |
| 0.60 | −0.238 | −0.068 |

偏差從 −0.24 塌到 −0.01 至 −0.11（散布 0.07–0.12，所以這幾個值彼此相容）。
**診斷確認：那 −0.24 裡大約 0.2 是質量範圍定義不一致，只有約 0.02 是雙星。**

### 與文獻對照後發現我先前引用錯誤

arXiv:2603.15779 給出的雙星偏差是
**−0.054 至 −0.086（質量相加）／−0.011 至 −0.021（光度相加）**。
未解析雙星是**光度**相加，所以適用的是後者，量級 0.02。

我們實測的 0.02 與它一致。**先前我用這篇文獻去佐證 0.3–0.4 的差異，
是誤讀**：只記得「忽略雙星使斜率偏平」，沒有注意它的量級。

---

## 五、「CMD 剔除」變體告訴我們什麼

在對齊的質量範圍下（0.50–2.50）：

| f_bin | 全當單星 | CMD 剔除後 | 平均剔除顆數 |
|---|---|---|---|
| 0.00 | −0.059 | −0.015 | 14.7 |
| 0.45 | −0.013 | **+0.120** | 112.3 |
| 0.60 | −0.068 | **+0.090** | 140.7 |

兩個要注意的地方：

1. **f_bin = 0 時仍然剔除了 14.7 顆**。那是誤判：測光誤差與差異消光
   讓一些單星落在主序上方 0.375 星等以外。這是偽陽性率的直接測量。
2. **雙星比例高時，剔除反而讓 α 偏高（+0.12）**。因為 CMD 偏移法
   優先抓到質量比接近 1 的雙星（它們最亮），而那些雙星的主星偏重；
   把它們整組移除等於系統性移除重星，使剩下的分布偏陡。

**這是缺口二的直接量化**：單一偵測法的剔除不是「修正」，
它把一種偏差換成另一種，方向還相反。

---

## 六、真實資料的結果

| 等時線 | 變體 | α | 星數 | 剔除 |
|---|---|---|---|---|
| (8.00, A_V=0.20) | 全當單星 | 1.973 | 702 | 0 |
| (8.00, A_V=0.20) | CMD 剔除 | 1.951 | 638 | 83 |
| (8.10, A_V=0.30) | 全當單星 | 2.099 | 772 | 0 |
| (8.10, A_V=0.30) | CMD 剔除 | 1.981 | 661 | 135 |

**傳統法對等時線選擇的敏感度是 0.13**（1.97 對 2.10），
對照前向模型修正版的 0.04。這仍然是論文可用的對比。

注意這裡用的是 0.30–2.50 的原設定，所以這些數字與前向模型的 α 不可直接相減。
論文的表 3 必須改用對齊的 0.50–2.50 重算，或明確標示兩者定義域不同。

---

## 七、引用文獻

| 文獻 | 用在哪裡 |
|---|---|
| Salpeter 1955, ApJ 121, 161 | α = 2.35 的來源，注入真值 |
| Kroupa 2001, MNRAS 322, 231 | 分段冪律（0.3 / 1.3 / 2.3），本文的轉折點問題來源 |
| arXiv:2603.15779 | 雙星對 α 的偏差量級（光度相加 −0.011 至 −0.021），我們實測 0.02 與之一致 |
| Cordoni et al. 2023, A&A 672, A29 | 78 個疏散星團的光度雙星與質量函數；缺口一的來源（量了 f_bin 卻沒接回 MF） |
| arXiv:2604.20722 (Mikhnevich et al. 2026) | 含 M45 的八個星團、兩種光度雙星判定法比較；我們的缺口二要與它區隔 |
| Hunt & Reffert 2023, A&A 673, A114 | 成員對照目錄、差異消光 0.634 |
| Bressan et al. 2012（PARSEC）／Choi et al. 2016（MIST） | 兩套等時線，用於量模型系統誤差 |

---

## 八、這支程式**沒有**回答的問題

- 傳統法自己擬合年齡的誤差（我們直接給它真值，對它有利）
- 質量分層造成的偏差（在有限半徑內量必然偏平）
- 等時線本身的物理錯誤（注入回收看不到，見報告 8.3 節）
