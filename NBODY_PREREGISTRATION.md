# N-body（路線 C）正式模擬預先註冊

**狀態**：預先註冊，2026-09-05 凍結，**在 `petar_m45_grid.csv` 任何一列
從「校準／pilot」升格成「正式模擬」之前簽署**。

**2026-09-18 修正（Codex review）——跟另一份文件的關係，尚待唯一化**：
另一支並行分支（PR #198）獨立新增了 `docs/planning/NBODY_PREREGISTRATION.md`，
同樣自稱是 N-body 正式模擬的預先註冊，但凍結了不同的孔徑數字
（11.68 pc，取代這份文件第二節的 11.87/12.09 pc）與不同的比較方法
（透過 `observe_snapshot.py` 的完整退化鏈把模擬端推過跟觀測端相同的
選樣，而不是像這份文件第一節那樣直接拿 `petar_pdmf_analysis.analyze()`
的 component-star 真實質量斜率去比較 `radial_final_reruns` 的觀測前向
模型 α）。兩份文件不能同時當「必須遵守」的凍結規格——在任何
`priority=1` 正式模擬真正開始、兩邊合併之前，**這份文件的第一、二節
視為暫定，不是最終凍結版**；哪一份文件是 canonical、怎麼合併，留給
兩個分支都合併進 main 之後再處理，不在這裡搶著定案。

**為什麼需要這份文件（H13）**：A5 的徑向分析（`radial_final_reruns`）
是先跑出四組結果，才回頭決定要用 Holm 校正處理多重比較——這個順序本身
沒有錯（那組實驗設計得早，沒有預先想到會需要修正這麼多次比較），但
它讓「核心到 2° 的分層算不算顯著」這個問題的答案，某種程度上依賴事後
才選定的統計處理方式。N-body 模擬一次跑下去是數小時到數十小時的算力
投入，不像前向模型可以重跑，**如果統計量、孔徑、質量段、驗收標準都是
看到初步結果之後才拍板，跟看到結果之後才選擇對自己有利的檢定方式沒有
本質差異**。這份文件的目的就是在還沒有任何正式模擬結果之前，把這些
決定寫死，之後不能因為某個孔徑或某種統計量給出「比較好看」的結果就
回頭改。

修改這份文件本身不是禁止的——研究過程中發現預先寫的規劃有問題是正常
的，但**修改必須在看到任何正式模擬（`priority=1`）的結果之前**，且
修改本身要留下記錄（見下方「修改記錄」一節），不能悄悄改掉。已經看過
結果之後才想改統計量或門檻，只能在 Discussion 裡承認是事後探索性分析，
不能包裝成預先設計。

---

## 一、主統計量（凍結）

**主統計量是 α(<r) 剖面跟觀測基準線的配對比較**，理由與定義完全比照
A5 已經用過、且已知有效的做法（`radial_final_reruns` 的配對比較法），
不重新發明一套：

- **2026-09-18 修正（Codex review）**：不能直接拿
  `petar_pdmf_analysis.analyze()` 算出的 component-star 真實質量斜率去
  跟 `radial_final_reruns` 的觀測前向模型 α 比較——固定 0.5–2.5 M☉
  質量段與孔徑只統一了兩個維度，雙星解析（觀測端有未解析雙星合併、
  模擬端 `analyze()` 沒有）跟觀測選擇函數（觀測端有測光雜訊／品質
  選擇／召回率三層退化，模擬端 raw 質量函數沒有）這兩個定義差異還沒
  統一，量到的 α 差異可能只是定義不一致，不是動力學效應。主統計量必須
  先把模擬快照推過跟觀測端相同的退化鏈，才能拿去跟觀測基準線做配對
  比較——用 `scripts/nbody_petar/observe_snapshot.py`（每個投影方向
  各跑一次模擬→觀測退化），輸出的假星表再用觀測端同一支估計器
  （`step5_imf.mle_powerlaw`／`petar_pdmf_analysis.mle_powerlaw`，同一
  個實作）算 α，這樣兩邊比的才是同一個量。在這條鏈驗證前，直接比較
  `analyze()` 跟 `radial_final_reruns` 的作法只能當診斷用途，不能當
  主要驗收依據。
- 對每個 N-body 模擬，在固定的孔徑清單（見第二節）上算出投影 α，
  同一個模擬內用同一組隨機投影方向（`n_projections=32`，函式預設值，
  不臨時改動）。
- 跟觀測基準線比較時，用**配對比較**而非「誤差棒是否重疊」——因為
  不同孔徑的樣本互為子集，不獨立，A5 已經證明用重疊誤差棒判斷會
  誤導。
- 次要統計量（記錄但不當驗收依據）：`delta_alpha` 三個分解項
  （survival_selection／stellar_evolution_after_survival／
  finite_aperture，`analyze()` 已經算的三個效應）、`bunres`（不可解析
  雙星率，可跟 C&S Table 2 的 0.68±0.02 對照）、質量分層參數（若後續
  接上，比照 C&S 的 β 定義）。

## 二、孔徑（凍結）

沿用 `DEFAULT_RADII_PC = [2.0, 4.0, 8.0, 12.09, 20.0]`
（`petar_pdmf_analysis.py` 現有預設值），另外**必須**加入一個對齊
觀測樣本邊界的孔徑：

- **11.87 pc**（觀測樣本 r=5° 對應的物理半徑，見 `PDMF_TO_IMF_PLAN.md`
  第二節）——這是唯一一個要跟 `radial_final_reruns` 的 `rall`
  （全樣本，α=2.3844±0.0792）直接比較的孔徑，其餘孔徑只做形狀／
  趨勢比較，不當成主要驗收點。
- 12.09 pc（`analyze()` 現有預設值之一，取到小數點後兩位跟上面
  11.87 pc 刻意不同——沿用既有程式碼常數，不因為這份文件而動
  `DEFAULT_RADII_PC`，兩個數字都留著，差異就是「常數表舊值」跟
  「這次重新算的樣本邊界」，不要事後合併成一個）。
- Jacobi 半徑 12.96 pc（潮汐邊界，用來判斷分層是否在潮汐半徑內收斂）。

孔徑清單只能在本文件生效後、看到任何正式模擬結果前擴充（例如新增
更精細的格點），不能事後刪除已經列出的孔徑。

## 三、質量段（凍結）

跟前向模型 headline 的擬合範圍對齊，**0.50–2.50 M☉**——理由跟
`docs/reference/REFUTED.md`「傳統法與前向模型的 α 差」那條完全相同：
不對齊質量範圍就比較 α，量到的是定義域差異而不是動力學效應。次要
（不當主要驗收依據）：0.30–2.50 M☉，用來跟傳統法附錄敏感度測試
對照。

## 四、初始條件（凍結，指向已查證的數字）

`petar_m45_grid.csv` 的 `priority=1`（正式模擬）三列使用 Converse &
Stahler (2010) Table 1 查證過的初始條件：`n_systems=1215`、
`binary_system_fraction=0.95`、`half_mass_radius_pc` 圍繞 pilot 值
（見網格檔本身），詳細數字與查證方式見 `PDMF_TO_IMF_PLAN.md`
「Converse & Stahler (2010) 實際初始條件」一節（2026-09-05 訂正版）。
`priority=2` 的敏感度網格（`rh_low`／`rh_high`／`seg_low`／`seg_high`／
`bin_low`／`bin_high`／`plummer`）維持現有設計，不受本文件約束——那些
本來就是敏感度掃描，不是「正式驗收用的模擬」。

## 五、驗收標準（凍結）

比照 A5 已經用過的框架，**不是「p<0.05 就宣告成功」這種單一數字門檻**：

1. **多重比較一律用 Holm 校正**，比較組數＝孔徑清單長度減一（相鄰
   孔徑兩兩比較）加上「11.87 pc vs 潮汐半徑內外對照」這一組，跟 A5
   的處理方式一致，不要因為 N-body 這次「感覺應該顯著」就退回不校正
   的 nominal p。
2. **形狀一致性優先於單點顯著性**：核心到 11.87 pc 之間 α 是否單調
   上升、11.87 pc 到潮汐半徑之間是否趨緩，這個**形狀**要跟
   `radial_final_reruns` 觀測到的形狀（核心到 2° 上升、2° 外近乎持平）
   做定性比較，即使個別點的 Holm 校正後 p 值不顯著，形狀吻合本身也是
   有意義的交叉驗證證據，要在報告裡跟統計顯著性分開陳述，不要混為
   一談。
3. **N-body 沒有復現觀測到的梯度形狀，不代表 N-body 模擬失敗**——
   可能是初始條件本身需要調整（見 `PDMF_TO_IMF_PLAN.md` 對「不是照抄
   數字去配結果，是拿它們當起點」的說明），這種情況下正確的下一步是
   回頭檢視初始條件假設，不是調整驗收標準去遷就已經跑出來的結果。
4. **每個正式模擬結果發布前，下面「已知會混進解讀的因素」全部要在
   同一份報告裡列出**，不能只挑對結論有利的部分：
   - 沒有套用銀河潮汐場（`LIMITATIONS.md` D19／H3）；
   - 低質量段 IMF 冪次鎖在 mcluster 內建的 Kroupa 預設值，跟前向模型
     正在懷疑的假設相同（`LIMITATIONS.md` D19／H5）；
   - 沒有套用星等相關的觀測召回率，只有空間孔徑選擇
     （`LIMITATIONS.md` D19／H7）；
   - 初始雙星比例（0.95，原初）跟前向模型 headline 的 `f_bin`
     （0.568，現時觀測）是不同量，不能互相驗證（`LIMITATIONS.md`
     D13／H6）；
   - 產出的質量函數在完成 Δα 修正前不能稱 IMF，命名還要標明是四種
     相容定義（component／primary／system_total／photometric_beta_N，
     見 `pdmf_system_definition_bridge.py`）裡的哪一種，不能一律簡稱
     system PDMF（`petar_pdmf_analysis.analyze()` 目前輸出的是
     component-star，不是 system），且要標明 post-gas-expulsion
     （`PDMF_TO_IMF_PLAN.md`「路線 C 的用詞規範」／H15，2026-09-18
     同步修正）。

## 六、修改記錄（不要刪除舊行，只加新行）

| 日期 | 修改內容 | 修改時是否已有正式模擬結果 |
|---|---|---|
| 2026-09-05 | 初版凍結 | 否——`petar_m45_grid.csv` 仍在校準階段，尚無 `priority=1` 的完整輸出 |
| 2026-09-18 | Codex review：第一節主統計量改為必須先透過 `observe_snapshot.py` 退化鏈才能跟觀測比較（不能直接比 component-star 真實質量斜率跟觀測前向模型 α）；第五節「system PDMF」用詞改成要求標明四種相容定義；文首加註跟另一分支（#198）`docs/planning/NBODY_PREREGISTRATION.md` 的孔徑／方法衝突尚未唯一化 | 否——同上，尚無 `priority=1` 的完整輸出，這次修正是在看到任何正式結果之前 |
