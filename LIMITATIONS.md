# 已知限制與未解問題（唯一權威清單）

**這份文件的用途**：把所有已知的限制、盲區、未解問題、以及已被推翻的說法
集中在一處，避免換對話後遺失。原本這些散落在 `README.md`、`STATE.md`、
研究日誌第六節與各程式的 docstring 裡，換 session 就容易漏掉。

**維護規則**：
1. 發現新限制 → 先寫進這裡，再寫進別處。
2. 推翻舊說法 → 移到第七節「已推翻的說法」，**不要直接刪掉** ——
   刪掉的錯誤會復活，因為下一輪看不到它曾被否證。
3. 論文的「討論／觀測偏差探討」章節直接以本文件為底稿。

最後更新：2026-08-09

**依「會不會改變論文方向」分類（2026-08-09 全面重篩）**

| 類 | 意義 | 項目 |
|---|---|---|
| **A** | **威脅現有主張，必須處理** | 低質量段冪次（0.248，最大單一誤差項，且文獻分歧 0.53 有方向性）→ P6b 跑中；**MH 異常成因未明** → 可能推翻表 4 穩健性，檢驗待跑；PDMF ≠ IMF → 論文用詞必須正名 |
| **B** | 影響數字不改方向 | Hess 多項分布近似、測光誤差用 G 查 BP/RP、殘留汙染 1% 未量、切半實驗兩缺陷、MCMC 未收斂 |
| **C** | 已解決但文件曾過時（已於 08-09 修正） | G<20「待修」vs 已推翻；等時線「尚未做」vs P3 已完成；低質量段「(b) 優先」vs 已改判 (a) |

**C 類是最危險的一種**：文件自己前後矛盾時，下一輪讀到過時的那半會走錯方向。
維護規則第 2 條（推翻不刪除）必須配合「同時把原處標記為已撤回」才有效，
只在第七節新增而不回頭改原處，就會製造矛盾。

---

## 一、資料與樣本

| 限制 | 量化 | 狀態 |
|---|---|---|
| 5° 錐涵蓋不完整 | 只含 HR23 全天成員的 92.7%（1,595/1,721） | 接受。完整需 15°，樣本從 6,956 爆到十幾萬、要 8 小時，只多撈 116 顆 |
| G<18 截斷 | 只含 HR23 成員的 91.7%；G<20 可到 99.7% | ~~待修~~ **已撤回（08-07，見第七節）**：G=18 已對應 0.173 M☉、擬合下限 0.30 M☉ 只到 G=16.63，有 1.4 星等餘裕。G<20 不擴大可用質量範圍 |
| 視差前切 plx>4 | 誤殺 HR23 成員 **0 顆**，但清掉 97% 汙染星 | 接受，但論文必須兩句都寫：不偏誤成員判定，且不能宣稱是演算法分辨出來的 |
| 測光品質篩選 | 1,297 → 1,078（砍 219 顆），**不是隨機砍**：G≥17 紅星砍 59%、藍星砍 20% | 已建模（見第三節） |
| 與 HR23 用同一份 Gaia DR3 | 一致性**不構成獨立驗證** | 無法解決。真正的獨立驗證需徑向速度、鋰吸收線、自轉週期、光譜金屬豐度 |

---

## 二、成員判定（pyUPMASK）

- **KDE 後驗機率會飽和**。P = 1/(1+L_field/L_memb)，M45 與場星分離極好，
  一千多顆全擠進 0.999–1.0。**P 接近 1 不代表把握大，只代表星團跟場星分得開。**
- **P≥0.95 以上不穩**：P≥0.99 換個亂數種子成員數從 1,188 變 815（差 48%）。
  穩定谷地是 0.5–0.9，故取 0.7。
- **頻率版與 KDE 版數值不可通約**，也不可與 Cantat-Gaudin 的機率直接比較。
- **隱含 50/50 先驗**。自洽先驗收斂到 0.1867，但對 M45 只改變 3% 成員。
  **分離差的星團（如 NGC 2477，|pm| 僅 2.6 mas/yr）會顯著**，做第二個標的要重新評估。
- **pyUPMASK 原始碼的統計缺陷未修**：誤差重抽每顆星只抽一個亂數套用到
  pmRA/pmDE/Plx 三個維度，三者擾動完全相關。修了只差 1.3%（低於種子間變異），
  故保留原始行為以免偏離已發表方法。
- `probs_final = -1` 是離群遮罩，不是機率，統計時要排除。
- **20 顆真正的判定分歧**，其 pm 與視差跟公認成員無法區分。
- **成員判定本身的完整度完全沒有建模。** 前向模型只模擬了測光品質篩選，
  沒有模擬「pyUPMASK 會漏掉哪些成員」。

---

## 三、前向模型

### 已建模但有殘留問題

- **測光品質選擇函數**（`pipeline/selection.py`）：訊噪比建模成
  「星等的非參數函數 + 顏色線性項」，再套用 config 的三把刀。
  驗收通過（整體 +0.018、逐星等最大 0.048、G≥17 紅藍落差 −0.362 vs 觀測 −0.388）。
  **殘留**：BP/RP 流量超額那一刀（85 顆）只用星等相依的隨機移除近似，
  它的顏色相依（+0.014）被忽略。這是星雲汙染造成的，不是星本身的性質。
- **差異消光**：見下方「不可測量的參數」。

### 尚未建模

- **測光誤差模型仍用 G 去查 BP/RP 的誤差**（`_interp_err(g, errmodel, "e_bp")`）。
  同一個 G 之下紅星的 BP 暗得多，所以這會**低估紅星的 BP 誤差**。
  選擇函數那一側已用各波段自己的星等修好，但加雜訊這一側還沒改。**待修。**
- **isochrone 取最近格點，不內插**（刻意的：演化快速階段線性內插會造出假特徵）。
  代價是 `logage_step = 0.05` 就是年齡解析度的天花板，`fit_logage_step` 設更小沒用。
- **殘留場星汙染**只用 1% 的均勻離群成分描述，而那個 1% 是猜的常數，沒有量過。
  可拿來檢查它的數字：與 HR23 真正的判定分歧是 20 顆，20/1078 = **1.9%**，
  所以量級大致對，但應升格為自由參數或至少做敏感度測試。**待辦。**
- **把 Hess 圖各格當成互相獨立的 Poisson 是一個近似。** 嚴格來說各格的聯合分布是
  多項分布（總星數固定 1,078，格與格之間有負相關），多項分布等價於
  「一組獨立 Poisson 在總數已知的條件下」。當獨立 Poisson 等於多算一個自由度。
  格數遠大於 1 時影響很小，但它是近似，先前的文件沒有標示。
- **不含自轉、不含前主序光變與黑子。**
- **質量分層／動力學演化沒有建模。** 我們量到的是現時質量函數（PDMF），
  不是 IMF；在有限半徑內量必然偏平。

### 兩個 2026-08-10 新發現（使用者質問後查證，不是我主動找到的）

**一、「MIST 鎖住 MH 後找不到合理年齡」這個結論是錯的，成因是網格覆蓋不足，
不是模型行為。** P9b 系列（p9b/p9b2/p9b3）連續三次把 logage 搜尋下限
放寬（7.90→7.70→7.30）去追一個看似「模型想要更年輕」的訊號，我一度在對話中
宣稱這「本身就是決定性結果」。**這個結論從未寫進文件**（使用者發現後才查證），
而且查證後證實是錯的：

直接測試 `_isochrone(want, 0.0)`（MIST 網格）：要求 7.30、7.50、7.70、7.80
四個不同年齡，**全部回傳完全相同的等時線**（都是 7.80、1573 點、G 值逐位元相同）。
原因是 MIST 網格檔（`build_mist_grid.py --logage-lo 7.80 --logage-hi 8.50`）
本身只涵蓋 logAge 7.80–8.50，任何低於 7.80 的請求都被 `_isochrone()` 的
最近格點吸附邏輯導到同一個 7.80。這代表 7.80 以下是一整片數值完全相同的
簡併平坦區，argmax 在裡面報出的「最佳年齡」是搜尋軸邊界決定的，不是模型偏好。

**這是我自己的方法錯誤**：撞牆就反射性放寬搜尋範圍，卻沒有先確認底層
isochrone 網格資料本身涵蓋到哪裡。跟先前 f_bin/alpha「網格比先驗窄」
那幾次不同——那幾次網格範圍只是設太保守，先驗本身涵蓋得到；這次是網格
根本沒有那些資料，放寬搜尋軸只會擴大簡併區，不會找到新答案。

**真正的狀態**：MIST + 鎖住 MH=0.00 的真實最佳擬合年齡，可能就在網格邊緣
7.80 附近，也可能比 7.80 更年輕（無法確定，因為網格沒蓋到）。要回答
「MIST 在鎖住 MH 後會不會給出跟 PARSEC 一致的 alpha」這個 P9 的核心問題，
**必須先重新下載涵蓋更廣年齡範圍的 MIST 網格**（例如 7.30–8.50），
不能靠放寬搜尋軸解決。**P9b 系列的失敗紀錄全部作廢，不能引用。**

**已確認沒事的部分**：PARSEC 自己的網格同樣有「檔名寫 7.7-8.3、實際只到
8.25」的落差，而 COARSE 的 logage 軸上界從最初版本就是 8.40（超出 8.25），
但直接檢查 `p2_final2`（10 次重複）與 `p9a2`（PARSEC 鎖 MH，5 次）的實際
擬合年齡，全部落在 8.00–8.10 之間，安全地在真實涵蓋範圍內部，
沒有觸及 8.25 以上的簡併區。這兩批結果的 logage/A_V/f_bin/alpha
不受這個問題影響。

**二、`config.toml` 記載的金屬量高斯先驗（中心 −0.03、σ 0.10，取自 Gaia
GSP-Spec/GSP-Phot 交叉驗證）從未被用在任何一支「產出數字」的腳本上。**
逐一檢查後發現 `fit_real.py`、`final_imf.py`、`inject_lowmass.py`、
`injection_recovery.py`、`measure_overconfidence.py`、`profile_lowmass.py`、
`traditional_accounting.py` **全部**在開頭寫死
`cfg._data["joint_fit"]["mh_prior_sigma"] = 0.0`，退回均勻先驗。

對純診斷用途（量概似本身的行為、測搜尋演算法、切半實驗）這樣做是對的——
先驗會污染那些測試想看的東西。但 `fit_real.py` 的角色是**產出要寫進論文
的最終數字**（P2），這裡關掉先驗沒有同等的理由，很可能是從診斷腳本複製
設定時沒有重新檢視。**這代表 `p2_final2`（目前的 alpha=2.387±0.060）與
所有其他 fit_real.py 產出的數字，用的都是均勻先驗，不是文件記載的方法論。**

**目前查到的影響（尚未定案）**：p2_final2 的 MH 十次重複全部精確落在
0.000——與均勻先驗下純概似的峰值一致，且已經很接近高斯先驗的中心 −0.03，
代表**修正後的模型**（選擇函數 + dav）本身的金屬量估計可能已經不再需要
先驗去拉正（不像修正前的舊模型跑到 +0.18、需要先驗大力介入）。若真是如此，
補上先驗對 p2_final2 的 alpha 影響會很小；但這是推測，**沒有驗證過**。
正確做法是把 `fit_real.py` 的先驗設定改回讀 config（移除那行覆寫），
重跑一次 config C，比較 alpha 有沒有變，才能定案。**在此之前，p2_final2
的 alpha=2.387±0.060 不能當成最終數字使用。**

### 低質量段冪次被固定，而它佔了六成的樣本（2026-08-07 新增，嚴重）

前向模型的 `alpha` **只是 Kroupa 分段冪律裡 m > 0.5 M☉ 那一段的冪次**。
0.08–0.5 M☉ 那段固定在 1.3、0.01–0.08 固定在 0.3，兩者都不參與擬合。

實測樣本落點（用最佳擬合等時線指派質量）：

| 質量段 | 冪次 | 觀測星數 | 佔比 |
|---|---|---|---|
| 0.08–0.5 M☉ | **固定 1.3** | 641 | **59.5%** |
| 0.5 M☉ 以上 | 自由 alpha | 437 | 40.5% |

**六成的星落在假設而非測量的段裡。** Hess 圖概似是整張圖一起算的，
所以這 641 顆星在推動包括 alpha 在內的所有參數；若 1.3 對 M45 不對，
模型會用別的參數補償。

**這與「以 M45 金屬量接近太陽為由固定金屬量」是同一類錯誤**，
而那個的輪廓測試顯示代價是 alpha 偏移 0.40。低質量段冪次**從未做過輪廓測試**。

**輪廓測試結果（2026-08-08，P6 完成）**：

| 冪次 p | alpha 平均（3 次） | 散布 |
|---|---|---|
| 0.9 | 2.633 | 0.094 |
| 1.1 | 2.567 | 0.094 |
| 1.3（現行值） | 2.500 | 0.163 |
| 1.5 | 2.433 | 0.189 |
| 1.7 | 2.233 | 0.094 |

**跨度 0.400 = 統計誤差 0.144 的 2.8 倍，單調遞減，無任何一維貼牆。**
與金屬量那次（跨度 0.40、133 倍）是同一類病灶，但這次的參數是**直接寫死在
config 之外的常數**（Kroupa 分段冪律定義本身），不在 joint_fit 的先驗系統裡。

**結論：必須處理，不能維持現狀。** 兩個可能作法：
(a) 升格為第八個自由參數（風險：進一步稀釋本來就緊繃的識別力，
且 6 成樣本的段落若同時要解兩個冪次，可能與 alpha 產生新的簡併，
需要再做一次注入回收才能確認）；
(b) 查文獻給這段冪次的獨立量測值做成高斯先驗鎖住，仿照金屬量的處理方式。

~~(b) 優先，因為它不需要新一輪的簡併驗證。~~
**改判 (a) 優先（2026-08-09，`analyze_lowmass.py` 量化後）**：

對 P6 的 (p, alpha) 做加權線性迴歸：

    d(alpha)/d(p) = -0.495 +- 0.111   （4.5 sigma，趨勢真實）

傳播 Kroupa (2001) 的 alpha1 = 1.3 +- 0.5：

    -> alpha 的系統誤差 = 0.248 = 統計誤差 0.144 的 **1.7 倍**

**這推翻了 (b)**：文獻先驗的**寬度本身就是誤差來源**，鎖住不會讓誤差消失，
只會把它從「未量化」變成「已量化但依然很大」。要真正縮小，只能讓資料
自己約束 —— 我們有 641 顆星（59.5%）落在這段，理論上有約束力。
故改走 (a)，但比照 dav 先做注入回收驗證可辨識性（`inject_lowmass.py`，P6b）。

**文獻本身分歧 0.53，且對 M45 有方向性（2026-08-09，`check_imf_form.py`）**：

| 來源 | 在 0.08–0.5 M☉ 的等效冪次 | 對 alpha 的影響 |
|---|---|---|
| Moraux+2003（**Pleiades 本身**的測量） | **0.84** | 我們的 alpha 需 **+0.23** |
| Chabrier 2003（銀盤場星通用值） | 1.37 | 幾乎不動 |
| 我們目前固定 | 1.3 | — |

Moraux 量的正是 M45，比通用值更該採信 —— 這代表目前用 1.3
**可能系統性低估 alpha 約 0.2**，是有方向性的偏移而非單純的不確定度。

### 函數形式：冪律 vs 對數常態（2026-08-09 已檢驗，結論是可用冪律）

Moraux+2003 與 Chabrier 2003 都主張低質量段用**對數常態**而非冪律，
這一度威脅到「測冪次」這件事本身的意義（若真實形狀是彎的，
單一冪次就是不存在的量）。`check_imf_form.py` 解析檢驗結果：

| 對數常態參數 | 局部冪次跨度（0.08→0.5） | 硬套單一冪律的殘差（RMS） |
|---|---|---|
| Moraux（m_c=0.25, σ=0.52） | 0.21 → 1.48 | 8.6% |
| Chabrier（m_c=0.079, σ=0.69） | 1.00 → 1.73 | 4.9% |

**殘差 4.9–8.6%，而 641 顆星分成 5/10/20 箱的 Poisson 相對雜訊是
8.8%/12.5%/17.7% —— 殘差小於雜訊，這批資料分辨不出兩種函數形式。**

結論：用分段冪律**是安全的近似**，P6b 測的「等效冪次」是有意義的量。
但論文必須聲明這是近似，且說明判準（殘差 < Poisson 雜訊）。
**注意這個結論繫於樣本數**：樣本大幅增加（例如推廣到多星團合併分析）時
要重算，因為 Poisson 雜訊會隨 1/sqrt(N) 下降而殘差不變。

### 傳統法與前向模型的 alpha 定義域不一致（我們自己的實作不一致）

`config.toml` 的 `mass_min = 0.30` 讓傳統法在 0.30–2.50 M☉ 擬合**單一**冪律，
但前向模型的 alpha 是 m > 0.5 M☉ 那一段。兩者不可直接相減。
論文表 3 必須改用對齊的 0.50–2.50，或明確標示定義域不同。
**這是我們的內部不一致，不是文獻現象，也不是可發表的發現。**

### 不可測量的參數（必須放進模型，但不能報告其值）

- **差異消光 dav**：注入回收 S4 顯示 dav 會貼在網格上界（3 次裡 2 次貼 0.600）。
  一開始用截斷常態 `max(0, A_V + dav·z)` 更糟 —— 截斷讓實際平均變成兩者的
  混合函數（A_V=0.15/dav=0.30 實際給出 mean 0.210、sd 0.223），
  A_V 直接被推到 0 貼牆。已改對數常態（mean 恰為 A_V、sd 恰為 dav、恆正），
  A_V 的偏差從 −0.150 降到 −0.050 且離開牆，但 dav 自己仍不可測。
  **不放它，α 偏 +0.178；放了，α 偏差歸零。所以放，但 dav 與 A_V 都降級成 nuisance。**
- **q_gamma（質量比分布冪次）基本上不受資料約束**：對照組偏差 −0.089、
  散布 0.157–0.236，在 S2 與 S3 都同樣跳 +0.19。不該當測量值報。

### 角落解：A_V 被壓到物理下限（2026-08-05 新增）

dav 掃描（注入不同大小的差異消光、擬合時當作 0）實測：
**注入 dav ≥ 0.20 時，A_V 在 15 次擬合裡有 12 次被壓到 0.000**，
也就是消光的物理下限。

成因是對數常態在大 dav 時中位數遠低於平均值
（A_V=0.10、dav=1.20 時中位數只有 0.008，65% 的星 A_V < 0.02），
不含 dav 的模型表現不了這種偏斜，只能靠壓低平均值去配合大多數幾乎沒被紅化的星。

**兩個後果**：
1. dav ≥ 0.20 那幾點量到的不是「漏掉 dav 的代價」，
   而是「漏掉 dav 且 A_V 卡在物理下限時的代價」——
   模型少一個自由度可吸收落差，alpha 被迫承擔更多，數字偏大。
   **只有 dav = 0.10 那一點（A_V 未貼牆）是乾淨的。**
2. **對數常態這個分布形式本身仍是未經檢驗的選擇。**
   它解決了截斷常態的平均值污染，但強偏斜帶來了新的行為。

### 邊界有三種，補救方式完全不同（2026-08-05 新增）

先前的貼牆偵測把所有邊界一視同仁，訊息一律是「放寬範圍重跑」。**那是錯的。**

| 種類 | 例子 | 意義 | 補救 |
|---|---|---|---|
| **物理邊界** | A_V ≥ 0、f_bin ∈ [0,1]、dav ≥ 0 | 角落解，模型描述不了資料 | **放寬不可能**，要回頭檢查模型 |
| **網格邊界** | logage、MH 的範圍 | 受限於已下載的 isochrone 涵蓋 | 重新下載網格 |
| **搜尋邊界** | 我自己設的上下界 | 答案被我決定 | 放寬重跑 |

`injection_recovery.check_walls()` 已依此分類，物理邊界拋 `CornerError`、
其餘拋 `WallError`。`fit_real.py`（產出論文數字）設為直接中止，
診斷用的跑法只印警告。

---

## 四、概似函數與誤差

- **Poisson-Hess 概似過度自信約 4 倍**。三種方法：

  | 方法 | σ_α |
  |---|---|
  | 概似曲率（模型自己宣稱） | 0.030 |
  | 切半實驗 | 0.106 |
  | 注入回收（最乾淨） | 0.144 |

- **可辯護的 Hess 格子數區間**：下界由雙星序偏移 0.753 星等決定
  （星等方向至少 46 格）；上界由「模型每格合成星數不能少於約 20 顆」決定。
  n_synthetic = 40,000 時是 **500–2,000 格**，目前用的 2,000 正好在上緣。
- **不要平滑模型的 Hess 圖**。1 格 = 0.32 星等，跟雙星序偏移 0.753 同量級，
  平滑等於抹掉要測的特徵，實測 f_bin 被壓到 0.00。
- **共用亂數必須一次抽好全部**，光是每次重設種子不夠（改變 f_bin 會改變
  抽取數量讓串流岔開）。修正前後 f_bin 0.60→0.45、α 2.10→2.35。
- **MCMC 尚未收斂**：τ 822–1454、接受率 0.064、N/50 = 400 而 τ ≈ 1000。
  **誤差棒與相關矩陣都不可引用。**
- **切半實驗有兩個缺陷，尚未修**：
  1. 網格窗太窄，20 個半樣本裡 7 個（35%）貼牆，散布被截斷；
     MH 的窗只有 0.00–0.30，而 Gaia 光譜值 −0.03 根本在窗外。
  2. 估計量是 argmax，而概似曲面在 α 方向近乎平坦
     （Δα = 0.225 只差 ΔlnP = 1.0），argmax 在平台上遊走。
     應改用短鏈 MCMC 的後驗中位數。
- **金屬量的異常尚未解釋（2026-08-09 升級為 A 類：可能推翻主賣點）**：
  真實資料上 MH 跑到 +0.18，而 Gaia GSP-Spec 給 −0.020、GSP-Phot 給 −0.061。
  注入回收裡 MH 九次全部精確回收到真值 —— 模型對時量得準、對真實資料量不準，
  這是模型與真實星團不符的直接證據，但**成因還沒找到**。

  **為什麼這威脅到「修正後對等時線穩健」這個主賣點**：
  0.2 dex 的金屬量偏移不是小事，它代表模型正在用一個錯的金屬量去補償
  某個沒建模的效應。若那個效應是**等時線在低質量前主序段的系統偏差**，
  那麼 PARSEC 與 MIST 之所以在修正後給出相同的 alpha（0.000），
  可能不是因為「修正消除了模型差異」，而是因為**兩套模型在該段有共同的
  偏差、而 MH 這個自由參數在兩邊都吸收了同樣的量**。
  果真如此，表 4 的穩健性就是假象 —— 兩套模型一致不等於兩套都對。

  **可行的檢驗（尚未做）**：把 MH 固定在光譜值 −0.03 重跑 config C 的
  PARSEC 與 MIST 兩版。若兩者的 alpha 仍然一致，穩健性成立（MH 只是
  吸收了無害的差異）；若鎖住 MH 後兩者開始分歧，就證實先前的一致
  是靠 MH 自由調整換來的，表 4 必須改寫。
  這個檢驗成本不高（兩次擬合），**優先度高於 P4/P5/P7**。

---

## 五、測試方法本身的盲區

**這一節最容易被忘記，因為通過測試會讓人以為沒問題了。**

- **注入回收測試看不到物理錯誤。** 假資料是同一個生成模型做的，
  所以它檢驗的是**搜尋演算法與估計量**，不是天文物理。
  **PARSEC 等時線本身若有系統誤差，注入回收永遠測不出來** ——
  模型錯，假資料也跟著錯，兩邊一致所以測試會通過。
  唯一的辦法是換 MIST / BHAC15 重跑同一批真實資料，把差距當系統誤差項。
  ~~這一項尚未做，而且很可能是目前最大的未量化系統誤差。~~
  **已完成（08-09，P3）**：PARSEC-EDR3 / PARSEC-DR2 / MIST-DR2 三組跑同一批
  觀測，把濾光片效應與模型效應拆開 —— 濾光片 0.040（可忽略）、
  恆星演化模型效應在**舊模型** 0.240、在**修正模型** 0.000。詳見 PAPER_OUTLINE 表 4。
  **但這不代表等時線系統誤差已經歸零**：PARSEC 與 MIST 在低質量前主序段
  可能有**共同**的偏差（兩者都用相似的對流與大氣處理），兩套模型一致
  不等於兩套都對。BHAC15（專為低質量前主序設計）仍未測，
  而 M45 的年齡正是由這一段約束。
- **注入回收有 −0.050 的 α 偏差地板**，來自擬合模型那批固定的共用亂數
  （n_synthetic 有限造成的蒙地卡羅偏移）。比它小的效應無法宣稱。
  正在測試提高 n_synthetic 能否壓低。
- **切半實驗看不到系統誤差。** 兩半共用同一個模型，模型錯的部分兩半一起錯。
  它只能給統計誤差那一半。
- **與 HR23 的一致不構成驗證**（同一份資料）。

---

## 六、尚未做過敏感度測試的設定

`pca_dims` 本身（非開關）、`stars_per_cluster`、`clustering_method`、
`inner_loop_runs`、`hess_color_range` / `hess_mag_range`、
BP 訊噪比門檻 20 這個值本身、成員機率門檻對 IMF 的影響。

**isochrone 模型（PARSEC vs MIST vs BHAC15）完全沒測過**，
而 M45 的年齡主要由低質量前主序收縮軌跡約束 —— 那正是 PARSEC 已知有偏差的區段。

---

## 七、已推翻的說法（保留以免復活）

| 曾經的說法 | 實測結果 | 日期 |
|---|---|---|
| 「2,000 個格子被當成 2,000 份獨立證據，改用無分箱概似可解決」 | σ 對格子數的冪次是 −0.18/−0.22 而非 −0.50；無分箱 KDE 只有 1,078 項卻給出同樣窄的 σ_MH（0.0061 vs 0.0050）。**分箱是有損壓縮，無分箱是資訊量最大的那一端。** 改用無分箱只會更糟，還多引進帶寬（0.15→0.30 讓 α 峰位跑 0.63） | 08-04 |
| 「換格子數把中心值推動 24–30 倍統計誤差，系統誤差主導」 | 錯在兩處：把 8,000/32,000 格（每格只有 5 顆和 1.2 顆合成星、被取樣雜訊主導）算了進去；分母用了已知偏小的概似自宣稱 σ。正確計算是 **α 1.4 倍、MH 0.7 倍**，屬正常範圍 | 08-04 |
| 「差異消光跟雙星方向不同所以可解」 | 方向不同只是必要條件。實測跟 f_bin 確實分得開（偏差 0.000），但**跟 A_V 分不開** | 08-04 |
| 「PCA 是影響最大的旋鈕（36%）」 | 門檻假象。在 P≥0.5 只差 2.3% | 07-31 |
| 「A_V–α 相關係數 +0.66 是聯合擬合才看得到的簡併證據」 | 不同鏈長給 +0.66 / −0.05 / −0.22，正負都跳，是未收斂的產物 | 08-01 |
| 「RUWE < 1.4 可以當品質切」 | 會砍掉 12% 的真成員（168/1369）。RUWE 高的主因就是未解析雙星，而 M45 年輕、雙星比例高 | 07-31 |
| 「投影效應是內部彌散的 5.2 倍」 | 那是拿「跨天區極差」比「標準差」，不對等。實測貢獻 0.62 mas/yr vs 內部彌散 0.78 | 07-31 |
| 「以 M45 金屬量接近太陽為由固定金屬量」 | 那是基於對特定天體的知識而非資料的函數。套到全銀河會因金屬量的銀徑梯度製造假的空間趨勢 | 08-01 |
| 「用 F1 對 HR23 挑機率門檻」 | 等於用對照目錄調自己的超參數，而且挑到方法最脆弱的點 | 07-31 |
| 「汙染率切完仍有九成九」 | 實測 81% | 07-31 |
| 「recall 天花板 59%」 | `Nt` 是三維判準不是角度判準；實測天花板 92.7% | 07-31 |
| **「傳統法與前向模型的 α 差 0.3–0.4，是忽略雙星造成的」** | **注入回收的 f_bin=0 對照組推翻**：無雙星時偏差一樣是 −0.265。真正成因是**兩個 α 定義域不同** —— 傳統法在 0.30–2.50 M☉ 擬合單一冪律，前向模型的 α 只是 Kroupa 分段冪律 m>0.5 M☉ 那一段。把擬合範圍對齊到 0.50–2.50 後偏差塌到 −0.01～−0.11。**雙星的真實貢獻只有約 0.02**，與 arXiv:2603.15779 的光度相加預測（−0.011～−0.021）一致 —— 我先前引用該文獻時只記得方向、沒注意量級 | 08-07 |
| 「G<20 對 IMF 是必要的」 | 實測 G=18 對應 0.173 M☉，而擬合下限 0.30 M☉ 只到 G=16.63 —— **已有 1.4 星等餘裕**。G<20 不擴大可用質量範圍，只在想往 0.3 M☉ 以下延伸時才需要，而那個區間 BP 測光誤差 108 mmag 已超過品質切門檻 54 mmag | 08-07 |

---

## 八、目前作廢的數值

**α 的絕對值暫時不可引用。**（傳統法 1.980 ± 0.069、前向模型 2.26–2.35）
原因是模型漏了兩個效應，各讓 α 偏約 0.18 且**方向相反、剛好抵消**：

| 情境 | α 偏差 | 扣掉對照組地板 |
|---|---|---|
| S1 對照 | −0.050 | — |
| S2 漏掉差異消光（dav=0.30） | +0.128 | +0.178 |
| S3 漏掉測光品質選擇函數 | −0.228 | −0.178 |

**只修一個會比兩個都不修更糟。** 兩個都補上並重跑之前不能定案。

**「剛好抵消」已確認是巧合**（2026-08-05，dav 掃描）。−0.178 是資料決定的
（選擇函數由真實觀測量出，沒有可調旋鈕）；+0.178 則由我憑空挑的 dav = 0.30
決定，而偏差隨 dav 在合理範圍內變動約 3 倍：

| 注入 dav | alpha 偏差（已扣對照組地板） | A_V 狀態 |
|---|---|---|
| 0.10 | +0.067 | 正常 |
| 0.20 | +0.067 | 貼物理下限 |
| 0.30 | +0.200 | 貼物理下限 |
| 0.45 | +0.133 | 貼物理下限 |
| 0.60 | +0.200 | 貼物理下限 |

**這次掃描的品質不足以畫出曲線**，三個原因：alpha 網格解析度 0.0667
而整個效應只跨 1–3 格（表中五個值全是 0.0667 的整數倍）；
散布 0.000 是網格釘死不是精確；dav ≥ 0.20 有角落解污染。
目前只能說「偏差為正、量級 0.07–0.20」。

**細網格重測（2026-08-06，精修兩階、A_V 範圍 0–1.0、每點 4 次）改寫了上表**：

| 注入 dav | alpha 偏差（扣地板） | 散布 |
|---|---|---|
| 0.10 | +0.017 | 0.082 |
| 0.20 | −0.033 | 0.119 |
| 0.30 | **+0.017** | 0.156 |
| 0.45 | +0.067 | 0.128 |
| 0.60 | +0.100 | 0.087 |

dav=0.30 的偏差從先前的 +0.178 掉到 +0.017。兩者統計上相容（差約 1.3 倍
合併標準誤）—— 真正的教訓是**單次擬合散布 0.10–0.16，三四次的平均根本
定不住 0.1 量級的效應**。「+0.178 與 −0.178 剛好抵消」的敘事因此**降級**：
S2 的偏差量值本身不確定（0 到 0.2 之間），確定的只有
（一）它隨 dav 平滑上升、在小 dav 趨近 0；（二）它的大小由假設的 dav 決定，
與資料決定的選擇函數偏差 −0.178 相等仍是巧合。
S3（選擇函數 −0.178±0.031）未受此影響，其散布小、且該掃描的 A_V 都在內部。

**仍然穩固可引用的**：成員數 1,297 / 1,078、對 HR23 的 recall 1.000、
真正分歧 20 顆、四種雙星判定法兩兩重疊最高 18%、
質量分層 α(r) 從 1.77 升到 2.29（相對比較，系統誤差在環帶間大致抵消）。

---

## 九、M45 當標竿星團的固有限制

太年輕（約 100 Myr），轉折點附近星數極少，年齡主要不由轉折點約束，
而是由低質量前主序收縮軌跡約束 —— **那正是 PARSEC 已知有偏差的區段**。
挑第二、三個標竿星團時要補上轉折點星數足夠的。

M45 泡在反射星雲裡：HR23 給的差異消光 0.634 遠大於平均消光 0.102，
BP/RP 孔徑測光被汙染最嚴重的正好是星雲裡的成員星。
