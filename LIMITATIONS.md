# 已知限制與後果（唯一權威清單）

所有已知限制集中在這裡，依嚴重程度排序，不依類型排序。

**嚴重程度定義**

| 級 | 意義 |
|---|---|
| **A** | 已引用的數字可能是錯的，或現有主張可能不成立。必須處理 |
| **B** | 每次計算都在用的未驗證假設。影響數字大小，不改變結論方向 |
| **C** | 結構性限制，修不掉。論文必須聲明 |
| **D** | 已知風險，尚未驗證。目前沒有污染現有結果的證據 |

**固定格式**（新增限制一律照此寫，不要自創格式）：標題行寫編號與問題名稱，
內文只有兩段——`**問題**：` 描述問題本身，`**後果**：` 描述它導致什麼。
不寫追查過程、不寫修正過程、不寫方法論心得，那些屬於 `CLAUDE.md`。
已推翻的說法與作廢數值放 `docs/reference/REFUTED.md`。已經完整解決、
重新核對過確實解決的整條搬到 `RESOLVED.md`，不要留在這裡寫「已修正」。

**2026-08-19 全面複查時的提醒**：這份文件在 `main` 上的版本，跟現在
GitHub 上十幾個尚未合併的分支／PR 相比，進度已經有落差——不少「尚無
認領工作」實際上已經有人在查、甚至查完大半，只是還沒合併進來（本次
複查已在對應條目補上分支名稱與進度）。**條目標成「尚無認領工作」不
保證真的沒人做**，開始新工作前務必先查一次開著的 PR 列表
（`gh pr list`）跟分支名稱，不要花時間重工已經有人做完的部分。

最後更新：2026-08-19

---

## A 類

### A1 精修 bug 波及的結果尚未以完整精修（`--refines 3,3`）重跑（p2_final2_v3 9/10次，未合併、p6b_inject_lowmass_v2 已推送待查證完成、p6_lowmass_v2 否、p11_outlierfrac_v2 否、verify_bprperr_v2 否）

**問題**：`injection_recovery.multi_stage_best()` 修好前，`--refines` 給單一值
等於完全沒精修（回傳純粗網格 argmax），給 `3,3` 只精修一階。程式碼已修，
用舊程式碼跑出的結果尚未重跑。受影響清單：

| 等級 | 結果 |
|---|---|
| 完全沒精修 | ~~`P9a-redo`（α=2.440±0.180）~~ **已重跑，見 A4：α=2.387±0.108**、~~`P9c`（α=2.180±0.098）~~ **已重跑，見 A4：α=2.102±0.102**、`p6_lowmass`（0.248 系統誤差）、`P6b`（ratio 0.92）、`P11`、`P12`、**`verify_bprperr_off`（α=2.420±0.098，2026-08-13 新確認，見 B1）** |
| 只精修一階 | `p2_final`／`p2_final2`（headline α=2.387±0.060）、`item4_davsweep`、注入回收 S1–S4（σ_α=0.144 的來源）、**`verify_bprperr_on`（α=2.420±0.078，2026-08-13 新確認，見 B1）** |

**後果**：除了已於 2026-08-14 用修好程式碼重跑確認的 P9a-redo／P9c（見
A4），上表其餘數字的精確值仍不可引用。alpha 的實際解析度是 0.20（完全沒
精修）或 0.067（一階），不是宣稱的 0.022。**「質性結論可能仍成立」這句話
的適用範圍要明確縮小**：僅指「MIST 年齡明顯偏年輕」這類已由重跑確認的
具體結論（粗網格 logage 間距 0.10 不足以翻轉這個方向），**不包含 A4 原本
「兩套等時線互相驗證穩健」那個主張——那個已經被確認不成立，不是「可能
仍成立」的待驗證項目**。其餘未重跑項目的精確值待重跑確認。重跑清單見
`WORK_BOARD.md`。

**2026-08-21 發現一個會讓 A1 重跑靜默失效的 bug（已修）**：
`scripts/tools/checkpoint.py` 的 `check_manifest()` 原本對「沒有
manifest 的舊結果檔」印一行警告就「視為信任沿用」。但沒有 manifest
的舊檔正是最不該信任的那一種——那段時期正好涵蓋 `multi_stage_best()`
精修 bug 還在的時候。實際案例：`results/profile_lowmass.npz`
（2026-08-15 存、無 manifest）裡 15 次擬合的 alpha 只有 4 個相異值
（2.1／2.3／2.5／2.7），相異值最小間距正好 0.2 = COARSE 網格間距，
是「完全沒精修」的量化簽章；而 `p6_lowmass_v2` 就是為了取代它才排的。
舊邏輯會判定「既有結果已滿足重複次數」，讓那次重跑**跑完 exit 0、
什麼都沒算**，接著 A1 會被誤標成已解決、用的還是同一批壞數字。
已改成擋下來並列出三個選項（移開舊檔／換 --tag／自行核對後補 manifest），
沿用舊檔必須是人明確決定的動作。**注意這不影響目前正在本機跑的那次
`p6_lowmass_v2`**：它啟動於 2026-08-20 15:16，用的是還沒有這套續傳
機制的舊版程式碼，會照常算完 15 次並覆寫該檔。

**這一段修的是程式 bug，不是 A1 本身——A1 仍是現役缺陷、優先度最高**
（2026-08-21 CodeRabbit review 要求講清楚，確實該分開講）：
`checkpoint.check_manifest()` 修好之後，「重跑會被靜默跳過」這個**新增
的**風險消失了，但**受影響的舊結果一顆都還沒重跑**。具體說：
`results/profile_lowmass.npz` 那 15 次擬合（A3 的 0.248 系統誤差來源）
仍然是完全沒精修的數字、仍然不可引用，取代它的 `p6_lowmass_v2` 目前排在
本機佇列**最後一位**（2026-08-21 移動，見 `queue.txt` 內的說明）。
換句話說：**程式已修 ≠ 結果已可用**，讀到這一段不要把 A1 當成已解除。
A1 上方表格裡除了 headline（`p2_final2_v3`，已於 2026-08-20 定案）之外的
每一項，引用狀態都沒有改變。

**2026-08-19 headline `p2_final2_v3` 進度更新**：10 次重複裡已完成 9 次
（`rep0`–`rep8`，分散在多個 Kaggle 帳號，逐次拉回保存），只差 `rep9`
還沒派工。這 9 次結果目前只保存在分支 `claude/headline-rep5-8-preserve`
（PR 尚未合併進 `main`），還沒串接成單一 `fit_real_p2final_v3.npz`，
`RESULTS_LOG.md` 與這裡的頭條數字都還沒更新——**不要誤以為這個 PR
一旦合併就代表 headline 數字定案**，串接檔案與正式更新頭條數字是合併
之後才要做的下一步。

**2026-08-20 headline `p2_final2_v3` 正式定案**：`rep9`（原本被 Kaggle
取消，`rep9_retry` 重新派工）已完成，10/10 次重複到齊。10 個
`rep*.npz` 的 `C` 陣列已沿 axis=0 串接存成 `results/fit_real_p2final_v3.npz`
（見 `RESULTS_LOG.md` 2026-08-20 那行完整參數）。**正式 headline 數字：
alpha=2.382±0.068**（population std，取代上面 A1 表格裡「只精修一階」
分類下的舊 `p2_final`／`p2_final2`（α=2.387±0.060）——這兩個修正
（精修 bug＋金屬量先驗）都已套用在這批重跑，A1／A2 兩項限制對
headline 數字本身**已解除**，上方表格與問題描述保留作為歷史記錄，
不要刪除或覆寫。

### A2 `fit_real.py` 曾寫死關閉金屬量高斯先驗（重跑進度：headline `p2_final2_v3` 10/10 次已完成，見上方 A1 2026-08-20 更新）

**問題**：`config.toml` 宣告 MH 高斯先驗（中心 −0.03、σ 0.10，取自 Gaia
GSP-Spec／GSP-Phot），但 `fit_real.py` 曾經寫死
`cfg._data["joint_fit"]["mh_prior_sigma"] = 0.0`，退回均勻先驗。程式碼
已修（改回讀 config），但既有數字尚未用修好後的程式碼重跑確認。

**後果**：`p2_final2`（α=2.387±0.060）與所有先前的 `fit_real.py` 產出的
數字，用的都是均勻先驗，不是文件記載的方法論，不能當最終值。跟 A1 是
同一次重跑（`p2_final2_v3` 會一併套用兩個修正，不用分兩次跑），進度見
上方 A1 的 2026-08-19 更新。**2026-08-20 更新：10/10 次重複已到齊並
定案，正式數字 alpha=2.382±0.068，見上方 A1 2026-08-20 那段與
`RESULTS_LOG.md` 同日期那行，本項限制已解除。**

### A3 低質量段冪次被固定，該段佔 59.5% 樣本（p2_free_lowmass 是、p6_lowmass_v2 否）

**問題**：前向模型的 `alpha` 只是 Kroupa 分段冪律 m > 0.5 M☉ 那一段。
0.08–0.5 M☉ 固定 1.3、0.01–0.08 M☉ 固定 0.3，都不參與擬合。實測樣本落點：
0.08–0.5 M☉ 有 641 顆（59.5%），0.5 M☉ 以上有 437 顆（40.5%）。輪廓測試
（P6）得 d(alpha)/d(p) = −0.495 ± 0.111（4.5σ），傳播 Kroupa (2001) 的
1.3 ± 0.5 得 alpha 系統誤差 0.248。`fit_real.py` 從未呼叫
`enable_lowmass_fit()`，headline 數字用的仍是固定 −1.3。

**後果**：alpha 最大的單一系統誤差項（0.248 = 統計誤差 0.144 的 1.7 倍）。
Moraux+2003 對 Pleiades 本身量到的等效冪次是 0.84（文獻間分歧 0.53），
若採信該值，目前用 1.3 會系統性低估 alpha 約 0.23。
（0.248 這個數字本身受 A1 影響，待重跑確認。）

### A4 MH 自由參數吸收系統偏差，表 4 的穩健性主張不成立（p9a_redo_v2 已用修好程式碼重跑確認、p9c_redo_v2 已用修好程式碼重跑確認）

**問題**：真實資料上 MH 跑到 +0.18，Gaia GSP-Spec 給 −0.020、GSP-Phot 給
−0.061；注入回收裡 MH 九次全部精確回收到真值。把 MH 固定在兩套網格共有的
格點 0.00 重跑 config C：

| | logage（年齡） | alpha |
|---|---|---|
| PARSEC（`p9a_redo_v2-fixed`，10 次） | 8.033（108.0 Myr，符合公認年齡） | 2.387 ± 0.108 |
| MIST（`p9c_redo_v2-fixed`，10 次） | 7.733（54.1 Myr，過輕） | 2.102 ± 0.102 |

**2026-08-14 已用修好 `multi_stage_best()` 精修 bug 與 Kaggle `results/`
目錄 bug 後的乾淨程式碼重跑確認**（`results/fit_real_fixmh_parsec_redo_v2.npz`、
`results/fit_real_fixmh_mist_redo_v2.npz`）：兩套等時線的年齡差距從舊數字的
「50–63 對 100–126」變成**單一乾淨值「54.1 對 108.0」，剛好差一倍**；
alpha 差距 0.285，是兩者合併標準誤（√(0.108²+0.102²)≈0.148）的 1.9 倍。
**用詞訂正**：不能說「不是雜訊」——這只是描述性比值，正式當成獨立常態
誤差算雙側 p 值約 0.055，屬於邊緣證據，不足以單獨排除隨機波動；正確的
講法是「差異為合併標準誤的 1.9 倍，證據仍屬邊緣」，還沒做正式假設檢定，
也沒驗證誤差彼此獨立這個前提。真正支持「不能宣稱穩健」這個結論的，是
**年齡差距（54.1 對 108.0 Myr，差一倍，遠超任何誤差量級）這個更直接、
不需要統計檢定的證據**，alpha 的邊緣證據只是輔助佐證。舊表格（`P9a-redo`/
`P9c`，α=2.440±0.180／2.180±0.098）作廢，不要引用。

**後果（年齡差距這個結論不變，已用乾淨數字確認；alpha 機制解釋降級為
待驗證假說）**：不能宣稱「修正後的模型對等時線選擇穩健」——先前兩套
等時線一致，很可能是 MH 各自吸收不同系統偏差湊出的表面現象，年齡差
一倍是這個結論最直接的證據。**「`p2_final2` 的 MH=0.000 正在吸收 PARSEC
低質量前主序段偏差」這個機制判斷，目前只是待驗證的假說**——固定 MH 的
P9a/P9c 結果支持「等時線之間有差異、表 4 需要改寫」，但沒有直接診斷
`p2_final2`（headline，MH 自由）本身在做什麼，不能單獨證明是這個機制。
`p2_final2_v3-fixed` 這次重跑仍在執行，等它完成後才能補上直接證據或
排除這個假說。不能用「兩套等時線互相驗證」佐證 α=2.387。表 4 必須改寫，
這點是本節唯一已定案、不受上述保留態度影響的結論。

### A5 測到的是 PDMF，不是 IMF（radial_r1/r2/r3/rall 初步值皆是、四項的正式統計版 `radial_final_reruns` 否、第3步LIMEPY模型 是／跟觀測值對照 否、第4步放大半徑 否、第5步N-body正式模擬 否）

**問題**：質量分層與動力學演化完全沒有建模，且樣本取自有限半徑（5° 錐，
11.87 pc）。動力學年齡 τ = t/t_rh = 1.11（t_rh = 113 Myr），質量分層必定
已經發生。傳統法量到的 α(r) 梯度 5.5σ：核心與外圍的 α 差 0.515，是統計
誤差 0.144 的 3.6 倍、比目前最大的系統誤差 0.248（見 A3）還大，但這是
帶著未解析雙星偏平偏差的傳統法量出的（Liu, Shao & Li 2025, ApJL 982,
L34 發現 Pleiades 雙星比例隨半徑雙峰分布，而目前的前向模型假設全域
單一 f_bin）。目前樣本半徑（5°）剛好卡在 Jacobi 潮汐半徑（12.96 pc =
5.46°）附近，40 pc 的潮汐尾完全沒收進來。

**2026-08-16 用前向模型（會把雙星建模掉）補到四個累積半徑切片，但只是
單次重複＋單階精修的初步方向數字，沒有誤差棒**（`fit_real_radial_r1/
r2/r3/rall_prelim.npz`）：r1(0–1°)=2.10、r2(0–2°)=2.43、r3(0–3°)=2.50、
rall(全樣本)=2.43。r1→r3 遞增支持核心到外圍的分層方向（跟傳統法方向
一致），**但 rall 比 r3 低，不是單調**——這個非單調現象目前沒有解釋，
可能是單次無誤差棒造成的雜訊，也可能是真實訊號，`WORK_BOARD.md` 已列
`radial_final_reruns`（用 `--repeats 5 --refines 3,3` 補上誤差棒）為
判斷這件事的前提，尚未執行。

**後果**：測到的是現時質量函數（PDMF），且必然偏平，不能宣稱測到 IMF。
前向模型初步值方向上支持分層存在，但「5° 內是否已收斂」「非單調是否
為真」這兩個問題都要等統計穩健版本才能回答，不能拿初步值當定論引用。

---

## B 類

### B1 測光誤差模型用 G 查 BP/RP 誤差（verify_bprperr_v2 off 半邊 5/5 完成、on 半邊 0/5，尚不能下結論）

**問題**：`_interp_err(g, errmodel, "e_bp")` 用 G 星等查 BP/RP 誤差。同一個 G
之下紅星的 BP 暗得多，因此低估紅星的 BP 誤差。選擇函數那一側已改用各波段
自己的星等，加雜訊這一側還沒改。`use_native_bprp_err` 旗標與 A/B 比較
（`verify_bprperr_off`／`on`）已存在。

**2026-08-13 兩次都跑完了，但誠實記錄一個問題——不是乾淨的對照組**：
`verify_bprperr_off`（結果 `results/fit_real_bprperr_off.npz`，α=2.420±0.098）
於 2026-08-11 10:47:42 開始跑，**早於同一天 16:57:09 才提交的
`multi_stage_best()` 精修 bug 修正**（見 A1），這個長跑的 subprocess 在
啟動當下就把舊版程式碼載進記憶體，中途 git 更新程式碼不會影響它，所以
這批**是完全沒精修的粗網格 argmax**（5 次結果 2.500/2.500/2.300/2.300/2.500
全部精確落在 COARSE 的 0.20 間距格點上，符合零精修的特徵）。
`verify_bprperr_on`（結果 `results/fit_real_bprperr_on.npz`，α=2.420±0.078）
緊接著在 18:07:11 才開始，晚於修正提交時間，跑的是修好後的程式碼，
但因為呼叫時沒帶 `--refines 3,3`（`fit_real.py` 的 `--refines` 預設值是
單一值 `"3"`），只精修一階（解析度約 0.067，5 次結果裡有 2 次
2.367/2.433 對得上這個解析度，另外 3 次仍剛好落在粗網格點上——精修後
最佳點跟粗網格點重合是可能發生的，不是矛盾）。**兩次用的精修程度不一樣，
不是控制單一變因的乾淨對照**，α 中心值剛好都是 2.420（差 0.000）這件事
方向上支持「BP/RP 誤差模型選擇對 alpha 影響很小」，但不能當成已驗證的
結論——真正乾淨的比較要兩邊都用 `--refines 3,3` 重跑一次（`verify_bprperr_v2`），
兩次用完全相同的精修程度才能排除是精修差異造成的巧合。

**2026-08-20 `verify_bprperr_v2` off 半邊 5 個分片全部到齊並串接完成**
（`results/fit_real_bprperr_off_v2.npz`，見 `RESULTS_LOG.md` 同日期行）：
**α=2.384±0.079**（5 次重複，雙階精修 `--refines 3,3`，`--repeat-offset
0–4` 分成 5 個 Kaggle kernel 跑完後沿 axis=0 串接）。logage 五次完全
一致 8.033（106.2 Myr）、MH 五次完全一致 −0.017，散布只出現在 A_V／
f_bin／alpha／q_gamma／dav 上。**這個數字取代舊的 `verify_bprperr_off`
（α=2.420±0.098）**——舊的是精修 bug 修好前跑的純粗網格 argmax，不是
同一個精修程度，兩者不能混用或並排比較。**on 半邊（`--native-bprp-err`）
的 5 個分片還沒有任何一片完成，B1 的 A/B 對照結論仍不能下**：現在只有
一半的乾淨數字，沒有可比較的對照組。

**後果**：所有 `fit_real.py`／`injection_recovery.py` 的數字共用這個誤差模型。
方向上會讓紅星在 Hess 圖裡看起來比實際精確，模型可能過度信任紅端形狀。
**初步跡象顯示對 alpha 中心值影響可忽略（2.420 vs 2.420），但這個結論
本身還沒有乾淨對照支持，待 `verify_bprperr_v2` 確認**，不能直接引用
現有的 off/on 兩個數字當最終答案。
大小未量。

### B2 殘留場星汙染固定在 1%（p11_outlierfrac_v2 否）

**問題**：均勻離群成分固定 `outlier_frac = 0.01`，是猜的常數。唯一的交叉核對
是與 HR23 真正的判定分歧 20 顆（20/1078 = 1.9%），量級相符。敏感度測試
（`profile_outlierfrac`）跑過但結果受 A1 作廢。

**後果**：每次擬合都在用這個未驗證的常數。若重跑後 0.5%／2% 之間 alpha
明顯跳動，須升級為 A 類。

### B3 文獻解析修正量外推到未驗證的 alpha 區間（尚無認領工作，已用降級為案例討論處理，見後果）

**問題**：`ANALYTIC_CORRECTION = 0.016` 取自 Rosen (2026) 區間 −0.011～−0.021
的中點絕對值。該文獻只在 alpha = 1.60–2.30 驗證過，M45 的 alpha（2.387–2.460）
超出上限。文獻區間本身的寬度 0.01 也沒有傳播進誤差棒。

**後果**：文獻解析修正法已降級為案例討論，不採計進方法優劣排序，保留在表 3
只為示範族群層級解析修正的做法、以及盲目外推文獻係數的風險。

### B4 Hess 圖各格當成獨立 Poisson（尚無認領工作）

**問題**：各格的聯合分布嚴格來說是多項分布（總星數固定 1,078，格間負相關）。
當成獨立 Poisson 等於多算一個自由度。

**後果**：格數遠大於 1 時影響小，但這是近似而非精確。

### B5 LIMEPY 多質量模型的質量比例直接沿用觀測 PDMF（`limepy_radial_crosscheck` 2026-08-20 已完成，見下方更新）

**問題**：`scripts/diagnostics/limepy_multimass.py`（PDMF→IMF 第 3 步）用
694 顆星（3 個質量區間）擬合 King(g=1) 多質量平衡模型，各質量區間之間的
相對總質量比例（Mj）直接固定成觀測值算出的比例，只有整體 normalization
自由。King(g=1)／Woolley(g=0)／Wilson(g=2) 三種模型形狀都測過，reduced
chi^2 分別是 0.75／1.50／0.52——**Wilson 實際上擬合最好**，用 King 只是
天文文獻的慣例選擇，不是因為它是最佳擬合，M／r_t 等精確值會隨模型形狀
選擇小幅變動（量級不變：r0 2.10–2.67 pc、M 440–476 M_sun）。如果 PDMF 本身已經因為蒸發／質量分層扭曲（這正是 A5 想解決的
問題），這個比例會把扭曲也帶進模型，不是獨立驗證——這是每次跑這支
腳本都在用的假設，會影響推算出的「潮汐半徑外質量」大小，不改變方向
（確實有質量在樣本邊緣外，只是精確量不確定）。模型算出的「潮汐半徑」
r_t=19.26 pc 是 LIMEPY 內部的 phi(r)=0 半徑，跟 `dynamics_estimate.py`
用銀河潮汐力算出的 Jacobi 半徑（12.96 pc）是不同概念的量，兩者對不上
是預期內的事，不要混為一談。

**2026-08-20 `limepy_radial_crosscheck` 結果（見 `RESULTS_LOG.md` 同日期行）**：
模型預測的 alpha(<r) 在 r1/r2/r3/rall 四個半徑都比觀測值低約 0.3–0.5，
但兩者都隨半徑遞增，方向一致（支持分層存在）。這個系統性低估可能反映
「模型只用 3 個質量區間的粗略冪律擬合」跟「觀測端用 694 顆星個別質量
做 MLE」不是同一種估計量，也可能是模型本身（單一各向同性、質量比例
直接沿用觀測值）就不會重現觀測的絕對尺度，只能重現形狀趨勢——這兩個
可能性目前無法區分。比較用的觀測值仍是 `_prelim`（單次無誤差棒），
`radial_final_reruns` 出來前不能判斷這個 0.3–0.5 的差距是否統計顯著。

**已修好的 bug（誠實記錄，2026-08-13 CodeRabbit review 抓到）**：第一版
把 LIMEPY 算出的 `Sigmaj`（質量面密度，M_sun/pc²）直接跟觀測星數面密度
（顆/pc²）比較，單位不一致，卡方擬合沒有意義。修好後（除以該區間平均
星質量換成星數面密度）reduced chi^2 從 8.90（擬合明顯不好）降到 0.75
（擬合品質好），總質量從 534.9 M_sun 修正為 456.0 M_sun，潮汐半徑外
質量估計從 21.2 M_sun 修正為 14.4 M_sun（模型總質量的 3.2%，原本誤植
4.0%）。**現在文件裡引用的都是修正後的數字**。

**後果**：擬合品質本身現在已經好（reduced chi^2=0.75），但「潮汐半徑外
還有 14.4 M_sun」仍然只能當初步估計，不能直接引用為確定數字——Mj 比例
沿用觀測 PDMF 這個假設本身還沒有獨立驗證。要提升可信度需要用第 2 步的
α(<r) 觀測結果交叉驗證（第 2 步的 `radial_r1/r2/r3/rall` 還在
`queue.txt` 排隊，尚未完成），或嘗試讓 Mj 也一起自由擬合而非固定。

---

## C 類

### C1 注入回收測不到等時線的物理誤差（**現役缺陷．優先度 中高**，與 D1 同一件事、同一組完成條件，見 D1）

**問題**：假資料由同一個生成模型產生，檢驗的是搜尋演算法與估計量，不是天文
物理。PARSEC 等時線本身若有系統誤差，模型錯、假資料也跟著錯，測試會通過。

**後果**：只能靠換等時線重跑同一批真實資料來量。已測 PARSEC-EDR3／
PARSEC-DR2／MIST-DR2（P3）：濾光片效應 0.040、恆星演化模型效應在修正模型下
0.000。但 PARSEC 與 MIST 在低質量前主序段用相似的對流與大氣處理，兩者一致
不等於兩者都對，共同偏差測不到——這正是分支 `claude/bhac15-grid-c1-d1`
（尚未合併）在補的第三套獨立模型，進度見 D1。

### C2 切半實驗測不到系統誤差（尚無認領工作）

**問題**：兩半共用同一個模型，模型錯的部分兩半一起錯。

**後果**：切半法只能給統計誤差那一半。

### C3 與 HR23 一致不構成獨立驗證（rv_binary_investigate 是）

**問題**：用的是同一份 Gaia DR3 資料。

**後果**：一致性不能當成成員判定正確的證據。真正的獨立驗證需要徑向速度、
鋰吸收線、自轉週期或光譜金屬豐度。**2026-08-13 已用
`scripts/data_prep/gaia_radial_velocity.py` 拿 Gaia DR3 官方徑向速度做了
第一次獨立交叉核對**（結果見 `data/radial_velocity.csv`）：1,078 顆成員裡
499 顆（46.3%）有徑向速度，相對星團整體徑向速度（bulk_rv=5.343 km/s）的
中位偏差只有 +0.14 km/s（整體一致，是好消息），但**其中 56 顆（11.2%）
偏差超過各自誤差棒的 5 個標準差**，查過 Gaia 自己的 `non_single_star`
旗標（outlier 組命中率 1.8% 反而低於全體基準 4.4%）與 `rv_nb_transits`
（outlier 組中位數 12，全體中位數 14，沒有系統性偏低）都不能解釋——這不是
「RV 太吵」或「已知的天測雙星」造成的，是一個目前沒有解釋的真正開放問題。
**沒有證據顯示這會污染已引用的數字**（現有頭條數字的擬合完全不用 RV，
只用自行／視差／測光），所以留在 C 類（結構性限制／有待釐清，不升級到
A）。

**2026-08-13 查證分光雙星能不能解釋掉 11.2%（`check_rv_binary_
fraction.py`）**：這台機器連不上 SIMBAD／Vizier（SSL 憑證問題，見
D9／A6），查不到現成的 M45 分光雙星編目，改用
`WORK_BOARD.md` 建議的第二條路——`f_bin=0.45` 配週期分布做蒙地卡羅
估計，隨機軌道相位／傾角下算 RV 偏移超過各自誤差棒 5σ 的機率，乘上
`f_bin` 得到「雙星本身能解釋的離群比例上限」。**測了兩個週期分布，
答案方向不一致**：

| 週期分布 | 週期中位 | K1 中位 | 雙星本身能解釋的上限 | vs 觀測 11.2% |
|---|---|---|---|---|
| Sana+2012（O 型雙星校準，短週期為主） | 16.5 天 | 37.2 km/s | 27.2% | 綽綽有餘 |
| Raghavan+2010（太陽型場星，M45 F-K 矮星母體更貼近的參考） | ~300 年 | 2.1 km/s | 6.9% | 缺口約 21 顆（4.3 個百分點） |

**誠實結論**：`WORK_BOARD.md` 原本點名「Kroupa/Sana」，但 Sana+2012
是校準給大質量 O 型雙星的短週期分布，M45 樣本主要是 F-K 型矮星，物理上
不對應——用它會系統性高估近距雙星的比例，因此高估 RV 大幅偏移的機率。
改用跟 M45 母體更接近的 Raghavan+2010 太陽型場星週期分布（週期分布寬得
多，中位數 ~300 年，RV 半振幅小很多），雙星本身只能解釋掉觀測比例的
一部分，**留下約 21 顆（56 顆的 4 成左右）沒有被單純的軌道相位效應
解釋**，可能是 f_bin 被低估、簡化的圓軌道假設（忽略偏心軌道在近星點
附近更容易被抓到大偏移的效應）、或真的有一部分污染源。**11.2% 這個
數字不能簡單歸給「分光雙星就能解釋」，也不能簡單歸給「污染」，答案
高度依賴週期分布假設，不是一個穩定的結論**——這是誠實的部分結論，
不是「查不出來」，見 `check_rv_binary_fraction.py` 完整方法與數字。

### C4 dav 與 q_gamma 不可測量，只能當 nuisance（尚無認領工作）

**問題**：差異消光 `dav` 在注入回收 S4 貼在網格上界（3 次裡 2 次貼 0.600）。
質量比分布冪次 `q_gamma` 基本不受資料約束（偏差 −0.089、散布 0.157–0.236）。

**後果**：兩者都必須放進模型（不放 dav，α 偏 +0.178；放了偏差歸零），
但兩者的值本身不可報告。

### C5 A_V 在大 dav 時被壓到物理下限（**現役缺陷．優先度 高**；修復工作 `extinction_form_test` 進行中）

**為什麼標成現役缺陷而不是待辦敏感度測試**（2026-08-19 CodeRabbit PR #63）：
這一項**已經污染了產出的結果**——`item4_davsweep` 那條 dav 掃描曲線在
dav ≥ 0.20 的每一點都受影響，而那條曲線正是「+0.178 這個差異消光修正量是不是
只是曲線在 dav=0.30 的一點值」這個判讀的依據。它不是「還沒測過所以想測看看」，
是「已知現有數字偏大、但還沒量出偏多少」，所以不能跟一般待辦排在同一列。

**完成條件**（三項全過才可降級為一般待辦）：
1. 兩種 `dav_distribution` 各跑一次注入回收，涵蓋 dav ∈ {0.10, ..., 1.20}
2. 量出兩者的 A_V 系統誤差差值，寫進 `results/RESULTS_LOG.md`
3. 依該差值修正或作廢 `item4_davsweep` 在 dav ≥ 0.20 的既有判讀

**問題**：對數常態在大 dav 時中位數遠低於平均值（A_V=0.10、dav=1.20 時中位數
只有 0.008），不含 dav 的模型表現不了這種偏斜，只能壓低平均值去配合大多數
幾乎沒被紅化的星。實測注入 dav ≥ 0.20 時，A_V 在 15 次擬合裡有 12 次被壓到
0.000。

**後果**：dav ≥ 0.20 的掃描點量到的不是「漏掉 dav 的代價」，而是「漏掉 dav
且 A_V 卡在物理下限的代價」，數字偏大，只有 dav = 0.10 那一點乾淨。
對數常態這個分布形式本身仍是未經檢驗的選擇。`item4_davsweep` 的既有判讀
（+0.178 差異消光修正量）目前建立在單一分布形式上，還沒有跟替代分布比對過，
屬於已知覆蓋不足、還沒量出偏差多少的現役缺陷，不是單純的待辦敏感度測試。

**2026-08-19 進度（分支 `claude/pr54-reapply`，尚未合併）**：
`pipeline/joint_fit.JointModel` 已加可切換的替代分布 `dav_distribution`
（預設 `lognormal` 不變；新選項 `trunc_exp` 截尾指數分布，用均值校正過的
location 讓兩種分布在同一組 (A_V, dav) 下平均消光相同，避免混進「平均
消光不同」這個混淆因子，四百萬次抽樣驗證誤差 <0.002）。`injection_
recovery.py` 已接上 `--dav-distribution` 旗標並把它存進輸出 npz。**還沒
做的部分**：真正跑一次注入回收比較兩種分布在 dav ∈ {0.10,...,1.20} 下的
A_V 系統誤差差異，這步是 `injection_recovery.py` 等級的計算量，還沒人跑完。

**已加了可切換的替代分布，注入回收比較還沒跑**：`pipeline/joint_fit.JointModel`
新增 `dav_distribution` 屬性，預設 `"lognormal"`（行為與加入前逐位元相同），
可切成 `"trunc_exp"`（截尾指數分布：scale 恆為 dav，location 依下述均值校正
決定，對負值做真正的截尾——夾到 0）。用跟 `av_i` 同一組共用亂數 `z_av` 產生，
維持「同一組共用亂數 -> 概似是參數的確定性函數」的 MCMC 前提。

~~煙霧測試（n=20000）：A_V=0.15、dav=1.20（C5 系統誤差比較實際會掃到的極端
情況）平均 0.5029（比 A_V 高很多，符合預期）、58.3% 的星剛好落在 A_V=0。~~
**已作廢，見下一段修正**（2026-08-19）：那個「平均比 A_V 高很多符合預期」的
判讀是錯的。location 當時一律取 A_V−dav，在 A_V < dav 時被截到 0 的那部分
機率質量會把平均推高（實測 A_V=0.15、dav=1.20 時平均 0.50，偏離目標 0.35 mag）。
放在單一條分布裡看確實物理合理，但 C5 要量的是**分布形狀**造成的系統誤差——
兩種分布若在同一組 (A_V, dav) 下平均消光就不同，量到的差會混進「平均消光不同」
這個混淆因子，分不出哪一部分來自形狀。等於把 dav 掃描偷偷變成平均消光掃描。

**修正（2026-08-19，CodeRabbit PR #63）**：location 改為解 E[max(loc+Exp(dav), 0)] = A_V
得到的值——`A_V ≥ dav` 時仍是 `A_V − dav`（既有結果逐位元不變），`A_V < dav`
時改為 `dav·ln(A_V/dav)`，兩式在 `A_V = dav` 連續。修正後平均恆等於 A_V
（四百萬次抽樣驗證，六組 (A_V, dav) 誤差皆 < 0.002），A_V=0 的離散質量點仍
保留，形狀差異也仍在——被移除的只有平均值這個混淆因子。修正後同一組極端條件
（A_V=0.15、dav=1.20）重測：平均 0.150（= A_V，符合設計）、87.5% 的星落在
A_V=0（比修正前的 58.3% 更多，因為 location 從 −1.05 降到 −2.50）。這正是
`trunc_exp` 跟 `lognormal` 在大 dav 時該有的形狀差異：同樣的平均消光下，一種
把質量堆在 0、另一種攤成長尾。
`injection_recovery.py` 新增 `--dav-distribution` 與 `--tag` 旗標接上這個開關，
並把 `dav_distribution` 一起存進輸出 npz（否則事後無從判斷某個檔案是哪種分布
算的，而 C5 的重點正是兩者的差）。

**還沒做的部分**：跑一次注入回收，在 A_V=0.10、dav ∈ {0.10, 0.20, ..., 1.20}
這個已知會撞牆的掃描範圍，比較兩種分布下 A_V 系統誤差有沒有差異——這步需要
`injection_recovery.py` 等級的計算量，還沒人跑完。

### C6 五種傳統法變體的誤差不是同一把尺（尚無認領工作）

**問題**：全當單星／CMD 剔除／文獻解析修正三種可以用注入回收；RUWE 剔除／
Gaia NSS 剔除做不到——`make_fake()` 只產生合成 color/mag，不模擬 RUWE 或
Gaia 官方 non-single-star 目錄判定，這兩者是真實天測解算與官方目錄流程的
產物，沒有簡單的正向模型可以合成。這兩個變體改用 bootstrap 估計誤差。

**後果**：bootstrap 量的是「換一批重抽樣本會不會給出差不多的答案」，注入
回收量的是「對真實但未知的真值的偏差與散布」，兩者是不同的量。論文裡
RUWE／NSS 那兩欄的誤差棒不能跟其他三欄比大小，只能比中心值。

### C7 逐星雙星判定只有前向模型後驗機率有召回率（尚無認領工作）

**問題**：合成資料對答案的結果（`results/verify_binary_detection.npz`）：

| f_bin | CMD 偏移 精確率／召回率 | 前向後驗機率 精確率／召回率 |
|---|---|---|
| 0.30 | 0.958／0.076 | 0.860／0.659 |
| 0.45 | 0.974／0.076 | 0.892／0.718 |
| 0.60 | 0.980／0.078 | 0.863／0.791 |

CMD 偏移法精確率 >0.95 但召回率只有 ~0.08。RUWE／NSS 無法測（同 C6）。

**後果**：CMD 偏移法只挑走一小群質量比高、光度超額大的雙星，這群不是隨機
樣本，剩下的低質量比雙星仍混在「當作單星」那堆裡，兩種效應疊加造成系統性
過矯正（表 3 的 +0.120）。全專案唯一驗證過準確度的雙星比例是前向模型自由
擬合的 f_bin（偏差 +0.011、散布 0.031）；其他地方出現的雙星計數／比例沒有
共同真值可比，不能放在同一張表裡暗示是同一個量的不同估計。

### C8 成員判定的完整度沒有建模（尚無認領工作）

**問題**：前向模型只模擬測光品質篩選，沒有模擬 pyUPMASK 會漏掉哪些成員。

**後果**：選擇函數只涵蓋測光那一段，成員判定造成的不完整度完全沒有進入
概似函數。

### C9 pyUPMASK 後驗機率會飽和，高門檻不穩（尚無認領工作）

**問題**：P = 1/(1+L_field/L_memb)，M45 與場星分離極好，一千多顆全擠進
0.999–1.0。P≥0.99 換個亂數種子成員數從 1,188 變 815（差 48%），穩定谷地是
0.5–0.9。隱含 50/50 先驗（自洽先驗收斂到 0.1867，對 M45 只改變 3% 成員）。
`probs_final = -1` 是離群遮罩，不是機率。

**後果**：P 接近 1 不代表把握大，只代表星團跟場星分得開，門檻取 0.7。
頻率版與 KDE 版數值不可通約，也不可與 Cantat-Gaudin 的機率直接比較。
分離差的星團（如 NGC 2477，|pm| 僅 2.6 mas/yr）會顯著受影響，做第二個標的
要重新評估。

### C10 pyUPMASK 原始碼的統計缺陷保留未修（尚無認領工作）

**問題**：誤差重抽時每顆星只抽一個亂數套用到 pmRA／pmDE／Plx 三個維度，
三者的擾動完全相關。修了只差 1.3%（低於種子間變異）。

**後果**：刻意保留原始行為以免偏離已發表方法，差異小於既有雜訊。

### C11 MCMC 未收斂，該家族的誤差棒永遠不可引用（尚無認領工作）

**問題**：τ = 822–1454、接受率 0.064、N/50 = 400 而 τ ≈ 1000。

**後果**：`joint*`／`joint6p*`／`profile_mh`／`profile_qgamma`／`prior_sens`
這一整個家族的誤差棒與相關矩陣都不可引用。誤差預算改用注入回收
（σ_α = 0.144）與網格 argmax，該家族從未產出任何已引用的最終數字。

### C12 切半實驗的兩個缺陷不修（尚無認領工作）

**問題**：（一）網格窗太窄，20 個半樣本裡 7 個（35%）貼牆，散布被截斷；
MH 的窗只有 0.00–0.30，而 Gaia 光譜值 −0.03 在窗外。（二）估計量是 argmax，
而概似曲面在 α 方向近乎平坦（Δα = 0.225 只差 ΔlnP = 1.0）。

**後果**：切半法量出的 σ_α = 0.106 不可信、從未被引用。統計誤差一律用注入
回收的 0.144（三種方法：概似曲率 0.030、切半 0.106、注入回收 0.144）。

### C13 注入回收有 −0.050 的 alpha 偏差地板（尚無認領工作）

**問題**：擬合模型那批固定的共用亂數造成蒙地卡羅偏移（n_synthetic 有限）。

**後果**：比 0.050 小的效應無法宣稱。

### C14 isochrone 取最近格點，不內插（尚無認領工作）

**問題**：刻意的選擇——演化快速階段線性內插會造出假特徵。

**後果**：`logage_step = 0.05` 就是年齡解析度的天花板，`fit_logage_step`
設更小沒有用。

### C15 5° 錐涵蓋不完整（尚無認領工作）

**問題**：只含 HR23 全天成員的 92.7%（1,595/1,721）。完整需 15°，樣本從
6,956 爆到十幾萬、要 8 小時，只多撈 116 顆。

**後果**：接受這個不完整度。

### C16 G < 18 截斷（尚無認領工作）

**問題**：只含 HR23 成員的 91.7%。G = 18 對應 0.173 M☉，而擬合下限 0.30 M☉
只到 G = 16.63，有 1.4 星等餘裕。

**後果**：不影響目前的質量範圍。往 0.3 M☉ 以下延伸時才需要放寬，而那個區間
BP 測光誤差 108 mmag 已超過品質切門檻 54 mmag。

### C17 視差前切 plx > 4 的效果不能歸給演算法（尚無認領工作）

**問題**：誤殺 HR23 成員 0 顆，但清掉 97% 的汙染星。

**後果**：論文必須兩句都寫——不偏誤成員判定，且不能宣稱汙染是演算法分辨
出來的。

### C19 模型不含自轉、前主序光變與黑子（尚無認領工作）

**問題**：這三項都沒有建模。

**後果**：合成 CMD 的散布只來自測光誤差、雙星、差異消光三個來源，真實星團
的其他變異來源全部缺席。

### C21 M45 當標竿星團的固有限制（尚無認領工作）

**問題**：太年輕（約 100 Myr），轉折點附近星數極少，年齡主要不由轉折點約束，
而是由低質量前主序收縮軌跡約束——那正是 PARSEC 已知有偏差的區段。M45 泡在
反射星雲裡，HR23 給的差異消光 0.634 遠大於平均消光 0.102，BP/RP 孔徑測光
被汙染最嚴重的正好是星雲裡的成員星。

**後果**：年齡與低質量段的結果最容易受等時線系統誤差影響。挑第二、三個標竿
星團時要補上轉折點星數足夠的。

### C22 多星團 Tier 1 與 M45（Tier 2）不同深度，不能靠加誤差棒升級（PR#11升級Tier2 否，見D8）

**問題**：NGC 3532（~238 Myr）、Praesepe／NGC 2632（~346 Myr）的 Tier 1 與
M45 的差距：

| 項目 | M45（Tier 2） | Tier 1 | 補齊要重跑嗎 |
|---|---|---|---|
| 成員判定 | 自己跑 pyUPMASK＋敏感度測試 | 直接借 HR23 成員表（Prob≥0.7） | 要 |
| 測光誤差模型 | 自己建立 | 沒有建立 | 前向模型才需要 |
| 測光品質選擇函數 | 自己校準 | 沒有校準 | 要 |
| 前向模型／注入回收 | 全套，σ_α = 0.144 | 完全沒做，誤差是曲率或 bootstrap | 要（全新一輪工作） |
| 年齡／消光／金屬量 | 自己擬合 | 直接用 HR23 的 `logAge50`／`AV50`／`MOD50`，MH 固定 0.00 | 要 |
| 轉折點截斷驗證 | 已驗證安全 | 兩個星團都沒驗證 | 要 |

**後果**：Tier 1 的數字不能寫進論文表 3 當最終數字，只能用來看方法差異會不會
比 M45 明顯。升級到 Tier 2 是重新執行整套腳本，不是既有結果的延伸。

---

## D 類

### D1 BHAC15 等時線未測（**現役缺陷．優先度 中高**；網格已備妥，`fit_real.py` 比較還沒跑）

**為什麼標成現役缺陷而不是待辦敏感度測試**（2026-08-19 CodeRabbit PR #65）：
目前對外可講的「等時線模型系統誤差」只有 PARSEC 對 MIST 那一個差值，而這兩套
在低質量前主序段用相似的對流／大氣處理——它們一致，**不構成**「等時線模型不
影響結論」的證據，只構成「這兩套之間沒差」。M45 的年齡主要由這一段約束，
A4（表 4 穩健性）已經因為 PARSEC/MIST 年齡差一倍而被推翻，所以這一段的模型
選擇確實會動到結論方向。這是「現有誤差預算可能低估、還沒量出低估多少」，
不是「想多測一個模型看看」。優先度低於 C5（C5 已知既有數字偏大；這一項是
已知覆蓋不足）。

**完成條件**（前兩項過即可降級為一般待辦）：
1. `fit_real.py --grid bhac15_gaia_logt7.6-8.4.dat` 跑一次 config C，與 PARSEC
   同設定比較 alpha 與 logage
2. 結果寫進 `results/RESULTS_LOG.md`，並在 A4／C1 標註「三套模型」而非兩套
3. （不可能達成，需明確記錄為永久限制）BHAC15 質量上限 1.4 M_sun 蓋不過 M45
   的 2.50，單一太陽金屬量也鎖死 MH 維度——這個比較**只能檢驗低質量段**，
   結論措辭不得宣稱驗證全質量範圍

**問題**：BHAC15 專為低質量前主序設計，而 M45 的年齡主要由低質量前主序收縮
軌跡約束。已測的 PARSEC 與 MIST 在這一段用相似的對流／大氣處理。

**後果**：兩套模型一致不代表沒有共同偏差（見 C1）。現有結果沒有被污染的
證據，只是還沒證實「換了也一樣」。

**2026-08-18 網格已可用**：正確下載網址是
`https://perso.ens-lyon.fr/isabelle.baraffe/BHAC15dir/BHAC15_iso.GAIA`（該
服務跟 PARSEC 一樣不送中繼憑證，`pipeline/net.py` 已通用化支援多組憑證鏈，
用 `chain_name="ens_lyon"`）。新增 `pipeline/bhac.py`／
`scripts/data_prep/build_bhac_grid.py`，轉出跟 PARSEC/MIST 同格式的網格
（`logAge`／`MH`／`Mini`／`G_fSBmag`／`G_BP_fSBmag`／`G_RP_fSBmag`），已用
`pipeline.isochrones.load_grid()`／`isochrone_at()` 驗證讀取正常。**確認
質量範圍只到 0.015–1.4 M_sun**（BHAC15 官方設計範圍，不蓋過 M45 擬合上限
2.50 M_sun），且只有太陽金屬量一組（MH 這個維度形同鎖死），驗證過用
BHAC15 重跑只能檢驗低質量段，不能宣稱驗證全範圍。**還沒做的部分**：真正
用 `fit_real.py --grid bhac15_gaia_logt7.6-8.4.dat` 跑一次跟 P3
（`build_dr2_grid.py`）同樣的濾光片/模型效應分解比較，這步需要
`fit_real.py` 等級的計算量，還沒人跑完。

**2026-08-19 第一次嘗試：發現需要對稱亮端截斷，煙霧測試在該截斷下撞牆**。

先補上一個必要條件：BHAC15 質量上限 1.4 M_sun 對應視星等 G≈8.88（用實際
距離模數換算），合成星團因此生不出更亮的星；若不同時砍觀測端，那些
「只有觀測、模型必為零」的 Hess 格會給概似巨大懲罰，量到的會是「網格
涵蓋不足的代價」而不是「換模型的代價」。已在 `fit_real.py` 加
`--g-bright MAG` 旗標，兩邊同步截斷（PARSEC 對照組必須用同一個值再跑
一次才是同基準比較）。

用煙霧測試規模（`--n-syn 5000 --refines 3 --fix-mh 0.0 --g-bright 8.9`，
config C）先驗證流程能不能跑通：亮端截斷保留 1,017/1,078 顆，isochrone
正確展開（6 個年齡格點），但**搜尋在 f_bin（貼上界 0.950）與 alpha
（貼下界 1.500）同時撞牆**，`multi_stage_best()` 依設計中止並拋出
`WallError`，沒有產出可用數字。

**這個結果目前只能算初步線索，不是結論**——`n_syn=5000` 遠低於正式數字
用的 40000，蒙地卡羅雜訊可能大到讓搜尋面失真；也可能是砍掉亮端後樣本
只剩低質量段，f_bin 與 alpha 的簡併性變強，是真實效應。**兩者都還沒
排除**。完成條件第 1 項因此更新為兩步：(a) 先用正式規模
（`--n-syn 40000 --refines 3,3`）重跑，若撞牆持續出現，代表是真實的
簡併而非雜訊，需要判斷是放寬 `f_bin`／`alpha` 的搜尋範圍、還是接受
「亮端截斷後的 BHAC15 比較無法產出可信數字」這個結論；(b) 若不再撞牆，
照原計畫跟 PARSEC 同設定（同一個 `--g-bright`）比較 alpha 與 logage。
正式規模的重跑已排進 `queue.txt`（見下方，標記為診斷用途、非 headline）。

### D2 未做過敏感度測試的設定（**部分進行中，2026-08-19**，見下方）

**問題**：`pca_dims` 本身（非開關）、`stars_per_cluster`、`clustering_method`、
`inner_loop_runs`、`hess_color_range`／`hess_mag_range`、BP 訊噪比門檻 20
這個值本身、成員機率門檻對 IMF 的影響。

**後果**：這些設定的敏感度未知。

**2026-08-19 進度**：新增 `scripts/diagnostics/sensitivity_sweep.py`，示範
`membership_threshold`（重新套用 `baseline.dat` 門檻 -> 重跑第 2/3 步 ->
config C 前向模型量 alpha）與 `stars_per_cluster`（需重跑 pyUPMASK 聚類，
腳本先做可行性檢查）兩個影響面最廣的設定。流程本身已用小規模參數驗證可
執行，**但還沒有真正的敏感度數字**——完整掃描需要原始 Gaia 查詢 CSV，這台
機器當時沒有，補齊依賴（clone `github.com/helmet-png/gaia-dr3-export`）後
重抓卡在 `fetch_gaia.py` 第一步的 `SELECT COUNT(*)` 查詢——手動重現並拿到
完整錯誤內文，確認是 ESA 伺服器端主動取消：`SQL exception: ERROR:
canceling statement due to statement timeout`（等 183 秒後被砍），不是
本機網路或帳號問題，是這個查詢（5 度錐形 + G<=18 + parallax>=4，對 18
億列做未加速的 COUNT）在伺服端太重。`stars_per_cluster` 則受限於這台
機器沒有 `pyUPMASK/`，腳本確認會誠實回報做不到、不編造數字。詳細下一步
（含可能的繞過法）見 `WORK_BOARD.md` D2 那行。

### D3 主序轉折點截斷對老星團會變成真限制（尚無認領工作）

**問題**：`pipeline/step5_imf.main_sequence_mass_luminosity()` 只保留 G 星等
持續變亮的那一段（轉折點以上質量與星等不再一對一，`np.interp()` 反查會錯）。
M45 已驗證安全：config C 等時線（logage=8.033）涵蓋 0.09–5.03 M☉、轉折點約
5.03 M☉，分析範圍上限只到 2.50 M☉，1,078 顆觀測星裡 0 顆因此變成 NaN。

**後果**：質量範圍延伸到接近轉折點、或換成老很多的星團（Hyades／Praesepe
~600–650 Myr，轉折點降到 ~1.5–2 M☉）時，要先重新驗證截斷有沒有把觀測星
排除在外。見 `docs/planning/PLAN_多星團擴展.md`。

### D4 冪律近似的有效性繫於樣本數（尚無認領工作）

**問題**：Moraux+2003 與 Chabrier 2003 都主張低質量段用對數常態。解析檢驗
（`check_imf_form.py`）：硬套單一冪律的殘差 RMS 為 8.6%（Moraux 參數）與
4.9%（Chabrier 參數），而 641 顆星分成 5／10／20 箱的 Poisson 相對雜訊是
8.8%／12.5%／17.7%，殘差小於雜訊。

**後果**：目前用分段冪律是安全的近似，但論文必須聲明判準（殘差 < Poisson
雜訊）。樣本大幅增加（例如多星團合併分析）時要重算——Poisson 雜訊隨 1/√N
下降而殘差不變。

### D5 `p6b4` 有一筆結果貼到網格邊界，補測後這次沒有再貼牆（`p6b4_boundary_retest` 是）

**問題**：8 筆裡 7 筆的 logage=8.2、MH=0.05、f_bin≤0.55，安全落在 PARSEC 網格
真實涵蓋（7.70–8.25）內部；`p_true=1.3` 的第 3 次試驗 logage=8.3，超出真實
上界 0.05。

**後果**：核心「可辨識」結論由另外 7 筆撐起，方向不受動搖，但這一筆本身不可
信。定案前應補測一次 `p_true=1.3`。（整批結果另受 A1 影響。）

**補測完成**（`inject_lowmass.py --only 1.3 --trials 1 --tag _p6b4_retest`，
新增 `--only` 旗標只測單一真值）：跑了一次 `p_true=1.3`（單次試驗），
**logage=8.133，安全落在 [7.70, 8.25] 內部，沒有再貼到網格邊界**。回收值
p_recovered=1.367（偏差 +0.067）、alpha_rec=2.433（偏差 +0.083），量級跟
原本 7 筆一致。**結論**：這次補測沒有重現貼牆，比較支持「原本那筆是單次
試驗的隨機波動，不是系統性現象」的解讀——但只補測了 1 次，不是原本的完整
3 次，不能 100% 排除偶爾仍會貼牆的可能性。核心「可辨識」結論（p_recovered
跟隨 p_true、ratio ~0.92）不受影響，維持成立。（這筆結果本身仍受 A1 影響，
數字量級參考、不是最終值。）

### D6 中間結果檔案會被重跑覆寫（尚無認領工作）

**問題**：`inject_lowmass.py` 的 `np.savez` 固定寫到
`results/inject_lowmass.npz`，每次重跑覆蓋前一次。`p6b`／`p6b2`／`p6b3` 的
完整 8 維參數向量已被後續嘗試蓋掉，無法回溯查證。

**後果**：這幾次執行的參數落點永久遺失，只剩 log 裡的 alpha／dav／p_rec／lnP。
需要事後核對邊界時無資料可查。

### D8 PR #11（多星團驗證）有 4 個待確認的正確性問題（PR#11修正 程式碼查證完成，PR #11 尚未合併＋未重跑，現役缺陷維持）

**問題**：PR #11（`codex/ngc3532-praesepe-generalization`，NGC 3532／
Praesepe 多星團驗證，尚未合併）的程式碼審查發現：`cluster_forward_
validation.py` 的 `allow_wall` 把全部參數的貼牆偵測都關掉，角落解（例如
Praesepe f_bin=1.000）會被當成收斂解寫進結果；`prepare_cluster_tier2.py`
的選擇函數驗證沒有重現 M45 用的紅藍分色檢查，SNR 迴歸也只用星團成員擬合、
沒有另外抓場星樣本；`cluster_imf_tier1.py` 在 `--refresh`（新增星團的正式
流程）遇到任一顆星 NSS 為 null 時會 TypeError 崩潰。完整內容見
[PR #11 留言](https://github.com/helmet-png/m45-imf-analysis/pull/11#issuecomment-5264701703)。

**後果**：只影響 PR #11 的 NGC 3532／Praesepe 結果（未合併，不影響 M45
本身任何已引用數字），但現狀合併的話，α 值可能不可靠——尤其前兩點會讓
角落解／有偏差的選擇函數被當成有效的多星團驗證結果寫進去。

**2026-08-20 查證更新（僅程式碼查證，非實證結論——見下方限制）**：為了
評估 Coma Berenices Tier 1（見 `WORK_BOARD.md` 起步條件）能不能起跑，逐項
核對 PR #11 分支（`codex/ngc3532-praesepe-generalization`，commit `c631e73`）
目前內容，發現 Codex 在留言之後已針對下列 4 點各自寫了對應的程式碼修法
（不是這次新修的，是查證既有進度）——**這只確認「程式碼看起來已處理」，
不等於「問題已解決」，PR #11 仍未合併、也還沒重跑資料驗證，D8 維持現役
缺陷狀態，不降級、不結案**：
1. `cluster_forward_validation.py:112` — `allowed_wall = (6,) if len(axes) > 6 else ()`，
   只放行 dav（索引 6，已知不可辨識、貼牆是預期行為），其餘維度貼牆會被
   `check_walls()` 標成 `wall_hits`、寫進機讀輸出（`fit_once()` 回傳
   `bool(wall_hits)`），角落解不會再被當成有效解悄悄吃掉。
2. `cluster_imf_tier1.py:189` — `nss_values = np.asarray(mem["NSS"], dtype=float)`
   明確轉型，VizieR 的 `null` 會變成 `np.nan` 而不是 object dtype 的 `None`，
   `np.isfinite()` 不會再崩潰（程式碼註解也記錄了修法原因）。
3. `prepare_cluster_tier2.py:238`（`validate_selection()`）已有 `faint = g >= 17.0`
   後的紅藍分色存活率差檢查（`red_minus_blue_error`），重現了
   `build_selection.py` 講的關鍵檢查。
4. `prepare_cluster_tier2.py:104`（`fetch_control_field()`）已實作抓場星樣本
   （查詢星團周圍 1.5 度內、扣掉成員本身的 Gaia 星，至少 100 顆才視為有效），
   且確認有接進 `main()`（第 330、348 行）餵給 SNR 迴歸與 `validate_selection`。

**只用讀程式碼＋確認呼叫鏈有接上做的查證，沒有實際重跑 NGC 3532／Praesepe
驗證資料確認數值結果**（例如貼牆偵測改了之後 Praesepe 的 f_bin 最終數字
變成多少）——這一步是這則更新沒做到的部分，留給實際合併 PR #11、跑一次
Tier 1 流程時順便確認，確認前不能排除程式碼看似修好、實際仍有其他未發現
問題的可能。**Coma Berenices Tier 1 的起步前置條件（`WORK_BOARD.md`）視為
「查證通過、可以開始起手式」，但不是「D8 已結案」**——D8 要等 PR #11 合併
且跑過一次帶新資料的 Tier 1 驗證、確認角落解偵測與選擇函數檢查在真實資料
上如預期運作後，才能改標記為已解決。

### D10 config C／D 的 alpha 一致性只在假資料上驗證過，真實資料沒有直接比較過（尚無認領工作）

**問題**（2026-08-16，教學對話中使用者追問「dav 當 nuisance 放行會不會
讓 alpha 不準」時發現）：`fit_real.py` 的 config D（dav 上界放寬到 1.2，
對照 config C 的上界 0.6）設計目的就是要確認「dav 貼牆位置改變時 alpha
會不會跟著移動」——如果 C、D 給出幾乎一樣的 alpha，代表 alpha 不受 dav
牆的位置污染。`fit_real.py` 開頭註解說這件事「在假資料上確認過（alpha
偏差與對照組完全相同）」，但那是**注入回收用合成資料**做的驗證，不是用
真實觀測資料的 `config C` 和 `config D` 直接比較。查過 `RESULTS_LOG.md`
與現有 `LIMITATIONS.md`／`RESOLVED.md`，沒有找到一筆用真實資料同時跑
`--configs C,D`（其餘設定完全相同）並比較 alpha 的記錄——正式跑幾乎都
只用 `--configs C`，沒有同時排 D。

**後果**：目前對「alpha 沒被 dav 的貼牆位置污染」這件事的信心完全建立
在合成資料的注入回收結果上，真實資料是否也成立**假設如此、沒有直接
驗證過**。如果真實資料上 C、D 的 alpha 有明顯差異，代表現有 headline
數字可能受 dav 不可辨識這個已知問題間接污染，需要回頭檢視——這正是
D 類「不會，只是還沒去證實不會」的典型情況，不代表現有數字已知有錯。

### D11 低質量段沒有獨立於等時線模型的經驗質量-光度關係校驗（`empirical_ml_relation_test` 待認領）

**問題**（2026-08-19，核對 Hobart et al. 2026（arXiv:2607.17300，UQ 團隊對
Alpha Persei／Pleiades／Praesepe 的 IMF 論文，方法論與本專案高度重疊）
時發現）：我們目前唯二測過的質量估計來源（PARSEC、MIST）都是**理論
等時線**，共享類似的對流／大氣邊界處理（見 D1 已記錄的「一致不代表沒有
共同偏差」）。Hobart et al. 2026 額外用一條**完全獨立於任何等時線模型**
的經驗質量-光度關係——蒐集近期食雙星測光質量文獻（Torres et al. 2010、
Benedict et al. 2016、Iglesias-Marzoa et al. 2017 的彙編，方法沿用 Kroupa
et al. 1993 對 Popper 1980 樣本的做法），對 M_V 與質量做 B-spline 平滑
（滿足 Wahba 1990 的 GCV 判準）。他們發現：用這條經驗關係跟用等時線
算出的低質量段（<0.5 M☉）IMF 斜率明顯不同（Pleiades 兩段 vs 三段模型
的貝氏證據方向都翻轉），高質量段則三種方法一致——這代表**低質量段
的斜率結論對「用不用等時線」本身很敏感**，不只是對「哪一套等時線」
敏感。

**後果**：我們現有的 PARSEC vs MIST 比較（P9 系列）只能排除「特定
等時線程式碼的實作差異」，排除不了「所有理論等時線共享的低質量段
系統偏差」這個更大的風險。低質量段（mass_min=0.30 附近）alpha 的
可信度上限尚未知，屬於「已知風險、目前沒有污染現有結果的證據」的
D 類。待認領工作：蒐集公開食雙星質量-光度資料（同一批文獻來源，
Torres+2010／Benedict+2016／Iglesias-Marzoa+2017 均可公開取得），
仿 `pipeline/step5_imf.py` 的 `assign_masses()` 架構做一條獨立的
經驗質量估計路徑，跟 PARSEC／MIST 三方比較低質量段 alpha。

### D12 亮端完整度沒有獨立目錄交叉驗證（`bright_end_completeness_check` 2026-08-20 初步查證完成，發現範圍比原問題描述更大，見下方更新）

**問題**（2026-08-19，核對 Hobart et al. 2026 時發現）：`config.toml`
的 `g_bright_limit = 4.0` 只是「比這更亮測光不可靠」的下限截斷，沒有
獨立驗證這條線以上有沒有真正的星團成員被截掉。Hobart et al. 2026
用 HIPPARCOS（測光可測到 mV=-1.44 不飽和，比 Gaia 亮端更寬）交叉比對
三個星團的 Gaia 資料，確認飽和界線以上沒有遺漏亮星成員（並補上 V/B
測光確認這些星落在主序上）——這是他們唯一一個明確拿「Gaia 的前代
巡天」當完整度交叉驗證用的步驟，跟我們現有的 UKIDSS 等效物缺失
（見下方 C16 的低質量端延伸缺口）是同一類問題的亮端版本。

**後果**：M45 目前已知的最亮成員（Alcyone 等，G~2.85 附近）落在
`g_bright_limit=4.0` 之上，若這幾顆亮星因為飽和測光被 CMD 品質篩選
排除、或反過來因為壞測光被誤判成非成員，目前沒有獨立於 Gaia 自身的
方式能查出來。已知風險，沒有污染現有結果的證據（高質量段目前只有
個位數顆星，即使有問題影響的星數也有限，但這幾顆星恰好是對高質量段
alpha 貢獻最大的幾顆）。

**2026-08-20 查證更新**：用 VizieR TAP 查 HIPPARCOS（`I/239/hip_main`）
M45 天區（中心 56.60083/24.11389，5° 內）Hp<=6.0 的星，共 17 顆，扣掉
明顯離群的場星後剩 10 顆左右空間上聚集在星團核心（1.2° 內）——對應
到肉眼可見的「昴宿七姊妹」與周邊亮星。逐顆交叉查 Gaia DR3（同樣用
VizieR 的 `I/355/gaiadr3` 鏡像，ESA 官方 TAP 當下剛好在維護中連不上）：

1. **Alcyone（G≈2.90，最亮的成員）在 Gaia DR3 完全沒有視差／RUWE 解**
   （查到的紀錄 `Plx`/`RUWE` 兩欄都是空值）——這不是本專案 pipeline
   造成的，是 Gaia DR3 本身對這顆星沒有可用的天測解（太亮，很可能是
   飽和導致的資料層級限制），任何下游步驟都不可能把它排進成員表。
2. **意外發現，範圍比原問題描述更大**：`g_bright_limit=4.0` 照定義
   只該影響 G<4.0 的星，但實際 `cmd_members.csv` 裡最亮的成員是
   G=5.203287（即 HIP 17851／Gaia source_id 66529975427235712）——
   G=4.0–5.2 之間、confirmed 視差落在星團範圍（Plx≈7.1–9.5 mas，
   對照 M45 公認視差≈7.36 mas）、Gaia DR3 測光正常的星（例如
   source_id 65205373152172032，G=4.17，Plx=7.07）**也完全沒有出現在
   `cmd_members.csv`**，比 `g_bright_limit` 這條線本身多刷掉了將近
   1.2 星等的範圍。這些星的 RUWE 明顯偏高（2.3–4.1，符合「亮端這幾顆
   本身就是已知聯星/聚星」的天文學背景，例如 Atlas、Pleione），但
   `ruwe_threshold=1.4`（`config.toml`）查過用途只在
   `step4_binaries.py`／`traditional_accounting.py` 當**事後聯星旗標**，
   不是成員表或 CMD 篩選的排除條件，不能直接解釋為什麼這些星整顆消失，
   不是只被標記成聯星。
3. **根因尚未追到**：這台機器沒有 pipeline 第 1 步（成員機率指定）之前
   的原始 Gaia 查詢檔案（`data/m45_r5_g18_plx4.csv`，見 D2 進度說明，
   卡在 ESA TAP 的 COUNT 查詢逾時），無法直接重跑 `run_pipeline.py`
   第 1、2 步逐步確認這些星是在 pyUPMASK 成員機率判定被評低（例如
   高 RUWE 的聯星讓自行/視差量測不乾淨，混淆了以自行/視差為主的分群），
   還是在 `step2_cmd.py` 其他測光品質篩選（例如
   `phot_bp_rp_excess_factor`，亮星在這個量上容易因為飽和被標記異常）
   被排除。**下一步**：等 `data/m45_r5_g18_plx4.csv` 可用（或用這 8–10
   顆星的座標單獨小量查詢 Gaia 驗證它們在原始查詢裡的完整測光/RUWE/
   `phot_bp_rp_excess_factor` 欄位），逐步比對 `run_pipeline.py` 第 1、2
   步的中介輸出，確認具體是哪一關把它們濾掉。

**後果更新**：亮端不完整不是只有 `g_bright_limit=4.0` 這條刻意設計的
截斷造成，額外還有 G=4.0–5.2 這段目前查無明確原因就整批消失，範圍比
原本估計的更大。高質量段本來就只有個位數顆星在支撐 alpha，若這 1.2
星等的範圍其實是可以救回來的真實成員被誤判排除（不是本來就該排除的
飽和壞測光），高質量段的統計量會明顯增加，值得列為後續認領工作，但
在追到具體原因前不能判斷這是「應該排除的髒資料」還是「被誤判的真
成員」，維持 D 級（已知風險，尚未證實會污染現有結果）。

### D13 聯星比例只有前向模型一條觀測制約，沒有獨立的「凸出星」比例當第二條制約（尚無認領工作）

**問題**（2026-08-19，核對 Hobart et al. 2026 時發現）：我們的
`f_bin` 是聯合前向模型（Hess 圖概似）唯一由資料約束的自由參數，
另外三種逐星傳統判定法（RUWE、CMD 偏移、前向模型後驗機率）是**事後
交叉核對**，不是餵回同一個貝氏擬合當獨立制約。Hobart et al. 2026 的
作法不同：他們用「主序以上偏移 2.5σ_p 以上」定義的觀測「凸出星」比例
`f_SO`（跟我們的 `cmd_offset_threshold` 概念類似，見 `binary_offset_mag`
一節），把這個獨立測到的比例**跟** MF 三段斜率**一起**放進同一個似然
函數（他們論文 Eq. 13）用 NAUTILUS 巢式抽樣聯合求解，讓 `f_bin` 同時
被「凸出星有多少」與「MF 形狀本身」兩條獨立資訊制約，理論上能縮小
`f_bin` 與 alpha 之間的簡併。我們目前的架構讓 `f_bin` 只被 MF 形狀
（Hess 圖）一條資訊制約，凸出星比例完全沒有進到似然函數裡。

**後果**：`f_bin` 與各段 alpha 之間可能存在的簡併，目前沒有第二條
獨立觀測制約可以打破，只能靠前向模型本身的統計精度撐住。不影響現有
數字已知有錯，但代表 `f_bin` 的不確定度可能被低估（貝氏後驗只看到
模型內部的統計雜訊，看不到「本來可以用凸出星比例排除的簡併方向」）。
待認領工作範圍大，屬於架構層級的增修，建議先在 `docs/planning/` 開
獨立子計畫評估成本再動工，不建議直接動 `pipeline/joint_fit.py`。

### D14 system MF 與 stellar MF 的區分在文件與程式碼中未明確一致標註（**已完成**，2026-08-19）

**問題**（2026-08-19，核對 Hobart et al. 2026 時發現）：Hobart et al.
2026 明確區分「system MF」（單星＋聯星系統的主星，不含次星）與
「stellar MF」（樣本中每一顆恆星，含未解析聯星的次星），並在方法論
裡交代清楚模型內部用哪一個、報告數字用哪一個（他們的 `f_bin` 擬合
在 system MF 空間進行，再生成大量合成星團轉換成 stellar MF 報告）。
我們的 `fit_real.py`／`pipeline/joint_fit.py` 目前沒有在程式碼註解或
`PAPER_OUTLINE.md` 裡明確標註 `alpha` 這個量究竟對應哪一個定義——
`binary_fraction` 是自由參數，合成星團生成邏輯（`imf` 抽樣→依
`binary_q_gamma` 配對次星）隱含用的是 system MF 空間抽樣、stellar MF
是抽樣完才隱含產生的結果，這跟 Hobart et al. 2026 的架構方向一致，
但目前沒有在文件裡明講這件事，容易在論文寫作或跟其他文獻比較數字
時混淆兩種定義（例如拿我們的 alpha 直接對照一篇報告 stellar MF 的
文獻，數值上可能有小幅系統差異）。

**後果**：不影響任何已算出的數字（架構本身跟 Hobart et al. 2026 的
做法方向一致），純粹是文件缺口——待認領工作是在 `PAPER_OUTLINE.md`
與相關程式碼註解裡明確加一句話說明我們的 alpha 對應哪個定義，避免
之後寫論文或跟文獻比較時混淆。

**已完成（2026-08-19）**：逐行核對 `JointModel.synthesise()` 的抽樣順序後
確認：先抽 `n_syn` 個**主星**質量（冪次 = `alpha`），再獨立擲
Bernoulli(`f_bin`) 決定誰帶伴星，伴星質量 `m2 = q·m1`（`q ~ q^q_gamma`，
**不是抽自 IMF**），最後主星與伴星**流量相加**塌縮成同一個測光點。
所以合成星團的一筆是一個**系統**，`alpha` 是**主星質量分布**的冪次，
等價於以主星質量標記的 **system MF**；伴星從未進入被擬合的質量分布。
說明已寫進 `PAPER_OUTLINE.md` 3.4 節、`fit_real.py` 檔頭、
`pipeline/joint_fit.py` 抽樣段落三處。

**兩種定義差多少（實算）**：`scripts/analysis/system_vs_stellar_mf.py`
（本次新增，可重跑）用專案實際參數（alpha=2.35、q_gamma=−0.50、
q_min=0.10、量測窗 0.5–2.5 M☉）做四百萬次蒙地卡羅，把伴星也放進同一個
量測窗後重做截斷冪律 MLE：stellar MF 比 system MF **陡**，
f_bin=0.30 → +0.046、0.45 → +0.066、0.60 → +0.085、0.75 → +0.102。
我們擬合到的 `f_bin` 落在 0.45–0.62，所以量級是 **+0.05 到 +0.09**——
小於統計誤差 0.144，但與好幾項系統誤差同量級。**這個數字本身不是新的
系統誤差項**（我們報 system MF、內部一致），但拿去跟報 stellar MF 的
文獻並排時必須先做這個換算，否則會被誤讀成物理差異。

**順帶記下的已知簡化**：Bernoulli(`f_bin`) 那一步與 `m1` 獨立，等於假設
**雙星比例不隨質量變化**。Torres+2025 對 Pleiades 觀測到雙星比例隨半徑
是雙峰的，質量相依性同樣沒有理由預設為常數——這一項尚未量化，列在下面
的待認領工作，不在本次範圍內。

---

## 附：不需要做的事（避免重複提案）

**0.01–0.08 M☉ 那段不需要比照 A3 升格為自由參數**：G = 18 對應 0.173 M☉
（見 C16），0.08 M☉ 比這暗一大截，代表沒有任何一顆通過測光品質篩選的觀測星
落在 0.01–0.08 M☉。0.08–0.5 M☉ 值得升格是因為裡面有 641 顆星在推動概似；
這一段沒有星在推動概似，放成自由參數只會變成另一個 q_gamma（見 C4）——
不受資料約束、貼著先驗邊界遊走，沒有真正的估計值可報。這不是資源分配問題，
是資料本身沒有資訊量。
