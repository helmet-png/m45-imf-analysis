# 已知限制與後果（唯一權威清單）

所有已知限制集中在這裡，依嚴重程度排序，不依類型排序。沿革與已推翻的
修正過程見 [CHANGELOG.md](CHANGELOG.md)。

**嚴重程度定義**

| 級 | 意義 |
|---|---|
| **A** | 已引用的數字可能是錯的，或現有主張可能不成立。必須處理 |
| **B** | 每次計算都在用的未驗證假設。影響數字大小，不改變結論方向 |
| **C** | 結構性限制，修不掉。論文必須聲明 |
| **D** | 已知風險，尚未驗證。目前沒有污染現有結果的證據 |

**固定格式**（新增限制一律照此寫，不要自創格式）：標題行寫編號與問題名稱，
內文只有兩段——`**問題**：` 描述問題本身，`**後果**：` 描述它導致什麼。
不寫追查過程、不寫修正過程、不寫方法論心得，那些屬於 `CHANGELOG.md`。
已推翻的說法與作廢數值放 `docs/reference/REFUTED.md`。已經完整解決、
重新核對過確實解決的整條搬到 `RESOLVED.md`，不要留在這裡寫「已修正」。

條目標成「尚無認領工作」不保證真的沒人做——開始新工作前務必先查一次
開著的 PR 列表（`gh pr list`）跟分支名稱，不要花時間重工已經有人做完
的部分。

---

## A 類

### A1 精修 bug 波及的結果尚未以完整精修（`--refines 3,3`）重跑（headline `p2_final2_v3` 已解除；p6_lowmass_v3 否、p6b_inject_lowmass_v2 否、p11_outlierfrac_v3 否）

**問題**：`injection_recovery.multi_stage_best()` 修好前，`--refines` 給單一值
等於完全沒精修（回傳純粗網格 argmax），給 `3,3` 只精修一階。程式碼已修，
用舊程式碼跑出的結果尚未全部重跑。

**後果**：headline（`p2_final2_v3`，alpha=2.382±0.068）與 `verify_bprperr_off`／
`on`（見 B1，alpha=2.384±0.079／2.433±0.075）已用完整精修重跑確認，可以
引用。仍未重跑、精確值不可引用的：`p6_lowmass`（A3 的 0.248 系統誤差
來源，完全沒精修）、`P6b`（ratio 0.92）、`P11`、`P12`（完全沒精修或只
精修一階）。alpha 的實際解析度在這些未重跑項目上是 0.20（完全沒精修）
或 0.067（一階），不是宣稱的 0.022。已修好一個會讓重跑靜默失效的 bug
（`checkpoint.check_manifest()` 原本對沒有 manifest 的舊結果檔只印警告
就視為信任沿用，已改成強制擋下、要求人明確決定要不要沿用舊檔）。重跑
清單見 `WORK_BOARD.md`。

### A2 `fit_real.py` 曾寫死關閉金屬量高斯先驗（已解除，隨 A1 headline 重跑一併確認）

**問題**：`config.toml` 宣告 MH 高斯先驗（中心 −0.03、σ 0.10，取自 Gaia
GSP-Spec／GSP-Phot），但 `fit_real.py` 曾經寫死
`cfg._data["joint_fit"]["mh_prior_sigma"] = 0.0`，退回均勻先驗。程式碼
已修（改回讀 config）。

**後果**：headline（`p2_final2_v3`，alpha=2.382±0.068）已用修好的先驗
重新擬合並定案，本項限制對 headline 已解除。跟 A1 是同一次重跑，其餘
仍受 A1 影響的項目狀態見 A1。

### A3 低質量段冪次被固定，該段佔 59.5% 樣本（p2_free_lowmass 是、p6_lowmass_v2 否）

**問題**：前向模型的 `alpha` 只是 Kroupa 分段冪律 m > 0.5 M☉ 那一段。
0.08–0.5 M☉ 固定 1.3、0.01–0.08 M☉ 固定 0.3，都不參與擬合。實測樣本落點：
0.08–0.5 M☉ 有 641 顆（59.5%），0.5 M☉ 以上有 437 顆（40.5%）。輪廓測試
（P6）得 d(alpha)/d(p) = −0.495 ± 0.111（4.5σ），傳播 Kroupa (2001) 的
1.3 ± 0.5 得 alpha 系統誤差 0.248。`fit_real.py` 從未呼叫
`enable_lowmass_fit()`，headline 數字用的仍是固定 −1.3。

**後果**：alpha 最大的單一系統誤差項（0.248 = 統計誤差 0.144 的 1.7 倍）。
Moraux+2003 對 Pleiades 本身量到的等效冪次是 0.84（文獻間分歧 0.53），
若採信該值，目前用 1.3 會系統性低估 alpha 約 0.23。（0.248 這個數字
本身受 A1 影響，待重跑確認。）

### A4 MH 自由參數吸收系統偏差，表 4 的穩健性主張不成立（已用修好程式碼重跑確認）

**問題**：真實資料上 MH 跑到 +0.18，Gaia GSP-Spec 給 −0.020、GSP-Phot 給
−0.061；注入回收裡 MH 九次全部精確回收到真值。把 MH 固定在兩套網格共有的
格點 0.00 重跑 config C：

| | logage（年齡） | alpha |
|---|---|---|
| PARSEC（10 次） | 8.033（108.0 Myr，符合公認年齡） | 2.387 ± 0.108 |
| MIST（10 次） | 7.733（54.1 Myr，過輕） | 2.102 ± 0.102 |

兩套等時線的年齡差距一倍，遠超過任何誤差量級；alpha 差距 0.285，是
合併標準誤（≈0.148）的 1.9 倍，屬邊緣證據（雙側 p≈0.055），不足以
單獨排除隨機波動——但年齡差一倍這個更直接的證據已經足夠支撐下面的
結論。

**後果**：不能宣稱「修正後的模型對等時線選擇穩健」——先前兩套等時線
一致，很可能是 MH 各自吸收不同系統偏差湊出的表面現象，年齡差一倍是
這個結論最直接的證據。「`p2_final2` 的 MH=0.000 正在吸收 PARSEC 低質量
前主序段偏差」這個機制判斷，目前只是待驗證的假說——固定 MH 的結果
支持「等時線之間有差異、表 4 需要改寫」，但沒有直接診斷 `p2_final2`
（headline，MH 自由）本身在做什麼。不能用「兩套等時線互相驗證」佐證
α=2.387。表 4 必須改寫，這點是本節唯一已定案的結論。

### A5 測到的是 PDMF，不是 IMF（現役缺陷．優先度最高；解除條件＝跟 LIMEPY 多質量模型與 N-body 模擬的預測交叉驗證）

**問題**：質量分層與動力學演化完全沒有建模，且樣本取自有限半徑（5° 錐，
11.87 pc）。動力學年齡 τ = t/t_rh = 1.11，質量分層必定已經發生。目前
樣本半徑（5°）剛好卡在 Jacobi 潮汐半徑（12.96 pc = 5.46°）附近，40 pc
的潮汐尾完全沒收進來。

**後果**：測到的是現時質量函數（PDMF），不能宣稱測到 IMF。前向模型
（會把雙星建模掉）的正式統計版 `radial_final_reruns` 四組
（`--repeats 5 --refines 3,3`）已全部完成：

| 半徑 | alpha（5 次樣本標準差） |
|---|---|
| r1（0–1°） | 2.0644 ± 0.1193 |
| r2（0–2°） | 2.3889 ± 0.0981 |
| r3（0–3°） | 2.4244 ± 0.0924 |
| rall（全樣本） | 2.3844 ± 0.0792 |

四組共用同一組亂數種子（`--repeat-offset`），因此用配對比較（比獨立
誤差棒重疊更精確，因為 r3／rall 等組本身互為子集不獨立）：核心到 2°
（r2−r1）nominal p=0.028，但四個比較（三個相鄰＋一個頭尾對照）屬同一
假說家族，Holm 校正後 p 約 0.084–0.088，未達慣例的 0.05 門檻——**核心
到 0–2° 的分層是提示性訊號，還不能宣稱獨立確認的統計顯著**。0–2° 往外
（r3−r2、rall−r3）本身 nominal p 就不顯著（0.099、0.181），沒有偵測到
進一步變化，但檢定力有限，不能倒推「已經證實收斂」。這個形狀（核心到
2° 有分層訊號、2° 以外沒有再偵測到訊號）跟質量分層的物理圖像一致，
可以當成目前最好的估計，但正式的「收斂」結論需要更高統計檢定力才能
宣稱。

這個初步判讀還沒有跟 LIMEPY 多質量模型（B5，目前只用 `_prelim` 做過
探索性比對）或 N-body 模擬（尚未進行）的預測交叉驗證過，那兩步是解除
本條目「現役缺陷．優先度最高」狀態前要做的事。

---

## B 類

### B1 測光誤差模型用 G 查 BP/RP 誤差（已界定：影響為統計誤差的 0.34 倍，未達顯著）

**問題**：`_interp_err(g, errmodel, "e_bp")` 用 G 星等查 BP/RP 誤差。同一個 G
之下紅星的 BP 暗得多，因此低估紅星的 BP 誤差。`use_native_bprp_err` 旗標
提供切換做 A/B 比較。

**後果**：`verify_bprperr_v2` 兩邊都用 `--refines 3,3`、同一組種子做完全
控制單一變因的乾淨對照：

| | alpha | 樣本標準差 | SEM |
|---|---|---|---|
| off（用 G 查，現行行為） | 2.3844 | 0.0792 | 0.0354 |
| on（用各波段自己的星等） | 2.4333 | 0.0754 | 0.0337 |

配對比較：平均差 +0.0489、SEM 0.0227、雙側 p=0.097，未達 5% 顯著水準，
且只有統計誤差 0.144 的 0.34 倍。方向一致為正（5 次裡 3 次為正、2 次
為零，沒有負值），但檢定力有限，不能宣稱「已證實沒有影響」，只能說
「影響界定在統計誤差的三分之一量級以內」。所有 `fit_real.py`／
`injection_recovery.py` 的數字共用這個誤差模型，headline 數字不需要
因此修正，但論文若要宣稱「這個簡化無影響」，措辭必須是「影響小於
統計誤差的三分之一」而不是「無影響」。

### B2 殘留場星汙染固定在 1%（p11_outlierfrac_v2 否）

**問題**：均勻離群成分固定 `outlier_frac = 0.01`，是猜的常數。唯一的交叉核對
是與 HR23 真正的判定分歧 20 顆（20/1078 = 1.9%），量級相符。敏感度測試
（`profile_outlierfrac`）跑過但結果受 A1 作廢。

**後果**：每次擬合都在用這個未驗證的常數。若重跑後 0.5%／2% 之間 alpha
明顯跳動，須升級為 A 類。

### B3 文獻解析修正量外推到未驗證的 alpha 區間（尚無認領工作，已降級為案例討論處理）

**問題**：`ANALYTIC_CORRECTION = 0.016` 取自 Rosen (2026) 區間 −0.011～−0.021
的中點絕對值。該文獻只在 alpha = 1.60–2.30 驗證過，M45 的 alpha（2.387–2.460）
超出上限。文獻區間本身的寬度 0.01 也沒有傳播進誤差棒。

**後果**：文獻解析修正法已降級為案例討論，不採計進方法優劣排序，保留在表 3
只為示範族群層級解析修正的做法、以及盲目外推文獻係數的風險。

### B4 Hess 圖各格當成獨立 Poisson（尚無認領工作）

**問題**：各格的聯合分布嚴格來說是多項分布（總星數固定 1,078，格間負相關）。
當成獨立 Poisson 等於多算一個自由度。

**後果**：格數遠大於 1 時影響小，但這是近似而非精確。

> **這一條與 `RESOLVED.md` 記載的「原 B4：Hess 圖各格當成獨立 Poisson」
> 內容矛盾**（`RESOLVED.md` 稱該問題已於 2026-08-13 被證明是代數上完全
> 等價、非近似，見 `check_poisson_vs_multinomial.py`）——本次整理只做
> 格式精簡，沒有動科學結論，這個矛盾原樣保留、如實標記，交給看到這裡
> 的人查證後決定哪一版才對，不要自行判斷刪掉任一邊。

### B5 LIMEPY 多質量模型的質量比例直接沿用觀測 PDMF（探索性交叉比對已完成，正式版待 A5 的 `_final` 資料）

**問題**：`scripts/diagnostics/limepy_multimass.py`（PDMF→IMF 第 3 步）用
694 顆星（3 個質量區間）擬合 King(g=1) 多質量平衡模型，各質量區間之間的
相對總質量比例直接固定成觀測值算出的比例，只有整體 normalization 自由。
King(g=1)／Woolley(g=0)／Wilson(g=2) 三種模型形狀都測過，reduced chi²
分別是 0.75／1.50／0.52——Wilson 實際上擬合最好，用 King 只是天文文獻的
慣例選擇。如果 PDMF 本身已經因為蒸發／質量分層扭曲（即 A5），這個比例
會把扭曲也帶進模型，不是獨立驗證。模型算出的「潮汐半徑」r_t=19.26 pc
是 LIMEPY 內部 phi(r)=0 半徑，跟銀河潮汐力算出的 Jacobi 半徑（12.96 pc）
是不同概念的量。

**後果**：擬合品質好（reduced chi²=0.75，單位換算 bug 已修，總質量
456.0 M_sun，潮汐半徑外質量估計 14.4 M_sun，模型總質量的 3.2%）。模型
預測的 alpha(<r) 在四個半徑都比觀測值低約 0.3–0.5，兩者都隨半徑遞增、
方向一致（支持分層存在），但這個比較用的是 A5 的 `_prelim`（單次無
誤差棒）觀測值，還沒有換成 A5 已完成的 `_final` 誤差棒版本重做——這個
系統性低估是否統計顯著，仍是待做的正式交叉驗證（A5 解除條件的一部分）。
要提升可信度也可以嘗試讓質量比例自由擬合而非固定。

---

## C 類

### C1 注入回收測不到等時線的物理誤差（現役缺陷．優先度中高；與 D1 同一件事、同一組完成條件，見 D1）

**問題**：假資料由同一個生成模型產生，檢驗的是搜尋演算法與估計量，不是天文
物理。PARSEC 等時線本身若有系統誤差，模型錯、假資料也跟著錯，測試會通過。

**後果**：只能靠換等時線重跑同一批真實資料來量。已測 PARSEC-EDR3／
PARSEC-DR2／MIST-DR2（P3）：濾光片效應 0.040、恆星演化模型效應在修正模型下
0.000。但 PARSEC 與 MIST 在低質量前主序段用相似的對流與大氣處理，兩者一致
不等於兩者都對，共同偏差測不到——這正是 BHAC15 第三套獨立模型（見 D1）
在補的缺口。

### C2 切半實驗測不到系統誤差（尚無認領工作）

**問題**：兩半共用同一個模型，模型錯的部分兩半一起錯。

**後果**：切半法只能給統計誤差那一半。

### C3 與 HR23 一致不構成獨立驗證（rv_binary_investigate 是）

**問題**：用的是同一份 Gaia DR3 資料，一致性不能當成成員判定正確的證據。
真正的獨立驗證需要徑向速度、鋰吸收線、自轉週期或光譜金屬豐度。

**後果**：已用 Gaia DR3 官方徑向速度做第一次獨立交叉核對：1,078 顆成員裡
499 顆（46.3%）有徑向速度，相對星團整體徑向速度的中位偏差只有
+0.14 km/s（整體一致），但其中 56 顆（11.2%）偏差超過各自誤差棒的 5
個標準差，且無法用 Gaia 的 `non_single_star` 旗標或 `rv_nb_transits`
解釋。用跟 M45 母體較接近的 Raghavan+2010 太陽型場星週期分布做蒙地卡羅
估計，分光雙星本身只能解釋掉這 56 顆裡的一部分（上限約 6.9%，換算約
21 顆沒被單純的軌道相位效應解釋），可能是 f_bin 被低估、簡化的圓軌道
假設、或真的有一部分污染源——11.2% 這個離群比例的成因答案高度依賴
週期分布假設，不是一個穩定的結論。沒有證據顯示這會污染已引用的數字
（現有頭條數字的擬合完全不用 RV），留在 C 類。

### C4 dav 與 q_gamma 不可測量，只能當 nuisance（尚無認領工作）

**問題**：差異消光 `dav` 在注入回收 S4 貼在網格上界（3 次裡 2 次貼 0.600）。
質量比分布冪次 `q_gamma` 基本不受資料約束（偏差 −0.089、散布 0.157–0.236）。

**後果**：兩者都必須放進模型（不放 dav，α 偏 +0.178；放了偏差歸零），
但兩者的值本身不可報告。

### C5 A_V 在大 dav 時被壓到物理下限（現役缺陷．優先度高；已量出兩種分布形式的差異方向，正式掃描待跑）

**問題**：對數常態在大 dav 時中位數遠低於平均值（A_V=0.10、dav=1.20 時
中位數只有 0.008），不含 dav 的模型表現不了這種偏斜，只能壓低平均值去
配合大多數幾乎沒被紅化的星。實測注入 dav ≥ 0.20 時，A_V 在 15 次擬合裡
有 12 次被壓到 0.000。

**後果**：dav ≥ 0.20 的掃描點量到的不是「漏掉 dav 的代價」，而是「漏掉
dav 且 A_V 卡在物理下限的代價」，數字偏大，只有 dav = 0.10 那一點乾淨。
`item4_davsweep` 的既有判讀（+0.178 差異消光修正量）目前建立在單一
分布形式（對數常態）上，還沒有跟替代分布比對過。`pipeline/joint_fit.
JointModel` 已加可切換的替代分布 `dav_distribution`（新選項 `trunc_exp`
截尾指數分布，均值校正過確保跟 lognormal 在同一組 (A_V, dav) 下平均
消光相同，避免混進「平均消光不同」這個混淆因子），`injection_
recovery.py` 已接上 `--dav-distribution` 旗標。**還沒做的部分**：真正
跑一次注入回收，在 A_V=0.10、dav ∈ {0.10, ..., 1.20} 這個範圍比較兩種
分布下的 A_V 系統誤差差異，這步需要 `injection_recovery.py` 等級的
計算量，還沒人跑完。完成條件：(1) 兩種分布各跑一次覆蓋 dav ∈
{0.10,...,1.20} 的注入回收，(2) 量出兩者的 A_V 系統誤差差值，(3) 依該
差值修正或作廢 `item4_davsweep` 在 dav ≥ 0.20 的既有判讀。

### C6 五種傳統法變體的誤差不是同一把尺（尚無認領工作）

**問題**：全當單星／CMD 剔除／文獻解析修正三種可以用注入回收；RUWE 剔除／
Gaia NSS 剔除做不到——`make_fake()` 只產生合成 color/mag，不模擬 RUWE 或
Gaia 官方 non-single-star 目錄判定，這兩者是真實天測解算與官方目錄流程的
產物，沒有簡單的正向模型可以合成。這兩個變體改用 bootstrap 估計誤差。

**後果**：bootstrap 量的是「換一批重抽樣本會不會給出差不多的答案」，注入
回收量的是「對真實但未知的真值的偏差與散布」，兩者是不同的量。論文裡
RUWE／NSS 那兩欄的誤差棒不能跟其他三欄比大小，只能比中心值。

### C7 逐星雙星判定只有前向模型後驗機率有召回率（尚無認領工作）

**問題**：合成資料對答案的結果（`results/verify_binary_detection.npz`）：

| f_bin | CMD 偏移 精確率／召回率 | 前向後驗機率 精確率／召回率 |
|---|---|---|
| 0.30 | 0.958／0.076 | 0.860／0.659 |
| 0.45 | 0.974／0.076 | 0.892／0.718 |
| 0.60 | 0.980／0.078 | 0.863／0.791 |

CMD 偏移法精確率 >0.95 但召回率只有 ~0.08。RUWE／NSS 無法測（同 C6）。

**後果**：CMD 偏移法只挑走一小群質量比高、光度超額大的雙星，這群不是隨機
樣本，剩下的低質量比雙星仍混在「當作單星」那堆裡，兩種效應疊加造成系統性
過矯正（表 3 的 +0.120）。全專案唯一驗證過準確度的雙星比例是前向模型自由
擬合的 f_bin（偏差 +0.011、散布 0.031）；其他地方出現的雙星計數／比例沒有
共同真值可比，不能放在同一張表裡暗示是同一個量的不同估計。

### C8 成員判定的完整度沒有建模（尚無認領工作）

**問題**：前向模型只模擬測光品質篩選，沒有模擬 pyUPMASK 會漏掉哪些成員。

**後果**：選擇函數只涵蓋測光那一段，成員判定造成的不完整度完全沒有進入
概似函數。

### C9 pyUPMASK 後驗機率會飽和，高門檻不穩（尚無認領工作）

**問題**：P = 1/(1+L_field/L_memb)，M45 與場星分離極好，一千多顆全擠進
0.999–1.0。P≥0.99 換個亂數種子成員數從 1,188 變 815（差 48%），穩定谷地是
0.5–0.9。隱含 50/50 先驗（自洽先驗收斂到 0.1867，對 M45 只改變 3% 成員）。
`probs_final = -1` 是離群遮罩，不是機率。

**後果**：P 接近 1 不代表把握大，只代表星團跟場星分得開，門檻取 0.7。
頻率版與 KDE 版數值不可通約，也不可與 Cantat-Gaudin 的機率直接比較。
分離差的星團（如 NGC 2477，|pm| 僅 2.6 mas/yr）會顯著受影響，做第二個標的
要重新評估。

### C10 pyUPMASK 原始碼的統計缺陷保留未修（尚無認領工作）

**問題**：誤差重抽時每顆星只抽一個亂數套用到 pmRA／pmDE／Plx 三個維度，
三者的擾動完全相關。修了只差 1.3%（低於種子間變異）。

**後果**：刻意保留原始行為以免偏離已發表方法，差異小於既有雜訊。

### C11 MCMC 未收斂，該家族的誤差棒永遠不可引用（尚無認領工作）

**問題**：τ = 822–1454、接受率 0.064、N/50 = 400 而 τ ≈ 1000。

**後果**：`joint*`／`joint6p*`／`profile_mh`／`profile_qgamma`／`prior_sens`
這一整個家族的誤差棒與相關矩陣都不可引用。誤差預算改用注入回收
（σ_α = 0.144）與網格 argmax，該家族從未產出任何已引用的最終數字。

### C12 切半實驗的兩個缺陷不修（尚無認領工作）

**問題**：（一）網格窗太窄，20 個半樣本裡 7 個（35%）貼牆，散布被截斷；
MH 的窗只有 0.00–0.30，而 Gaia 光譜值 −0.03 在窗外。（二）估計量是 argmax，
而概似曲面在 α 方向近乎平坦（Δα = 0.225 只差 ΔlnP = 1.0）。

**後果**：切半法量出的 σ_α = 0.106 不可信、從未被引用。統計誤差一律用注入
回收的 0.144（三種方法：概似曲率 0.030、切半 0.106、注入回收 0.144）。

### C13 注入回收有 alpha 偏差地板（已縮小，支持蒙地卡羅雜訊假設）

**問題**：擬合模型那批固定的共用亂數造成蒙地卡羅偏移（n_synthetic 有限），
原始記載地板約 −0.050。

**後果**：把 `--n-syn` 提高到 4 倍（160,000）重跑後，alpha 偏差降到
−0.006（3 次跨試散布 0.048），遠小於原本的 −0.050——地板確實隨 n_syn
提高大幅縮小，支持蒙地卡羅雜訊假設，不是另有系統性成因。附帶觀察：
`q_gamma` 偏差 −0.181（散布 0.026）明顯比 alpha 大且沒有跟著縮小，可能
有獨立於樣本量的偏移機制，需要另外查證。

### C14 isochrone 取最近格點，不內插（尚無認領工作）

**問題**：刻意的選擇——演化快速階段線性內插會造出假特徵。

**後果**：`logage_step = 0.05` 就是年齡解析度的天花板，`fit_logage_step`
設更小沒有用。

### C15 5° 錐涵蓋不完整（尚無認領工作）

**問題**：只含 HR23 全天成員的 92.7%（1,595/1,721）。完整需 15°，樣本從
6,956 爆到十幾萬、要 8 小時，只多撈 116 顆。

**後果**：接受這個不完整度。

### C16 G < 18 截斷（尚無認領工作）

**問題**：只含 HR23 成員的 91.7%。G = 18 對應 0.173 M☉，而擬合下限 0.30 M☉
只到 G = 16.63，有 1.4 星等餘裕。

**後果**：不影響目前的質量範圍。往 0.3 M☉ 以下延伸時才需要放寬，而那個區間
BP 測光誤差 108 mmag 已超過品質切門檻 54 mmag。

### C17 視差前切 plx > 4 的效果不能歸給演算法（尚無認領工作）

**問題**：誤殺 HR23 成員 0 顆，但清掉 97% 的汙染星。

**後果**：論文必須兩句都寫——不偏誤成員判定，且不能宣稱汙染是演算法分辨
出來的。

### C19 模型不含自轉、前主序光變與黑子（尚無認領工作）

**問題**：這三項都沒有建模。

**後果**：合成 CMD 的散布只來自測光誤差、雙星、差異消光三個來源，真實星團
的其他變異來源全部缺席。

### C21 M45 當標竿星團的固有限制（尚無認領工作）

**問題**：太年輕（約 100 Myr），轉折點附近星數極少，年齡主要不由轉折點約束，
而是由低質量前主序收縮軌跡約束——那正是 PARSEC 已知有偏差的區段。M45 泡在
反射星雲裡，HR23 給的差異消光 0.634 遠大於平均消光 0.102，BP/RP 孔徑測光
被汙染最嚴重的正好是星雲裡的成員星。

**後果**：年齡與低質量段的結果最容易受等時線系統誤差影響。挑第二、三個標竿
星團時要補上轉折點星數足夠的。

### C22 多星團 Tier 1 與 M45（Tier 2）不同深度，不能靠加誤差棒升級（PR#11升級Tier2 否，見D16、D17）

**問題**：NGC 3532（~238 Myr）、Praesepe／NGC 2632（~346 Myr）的 Tier 1 與
M45 的差距：

| 項目 | M45（Tier 2） | Tier 1 | 補齊要重跑嗎 |
|---|---|---|---|
| 成員判定 | 自己跑 pyUPMASK＋敏感度測試 | 直接借 HR23 成員表（Prob≥0.7） | 要 |
| 測光誤差模型 | 自己建立 | 沒有建立 | 前向模型才需要 |
| 測光品質選擇函數 | 自己校準 | 沒有校準 | 要 |
| 前向模型／注入回收 | 全套，σ_α = 0.144 | 完全沒做，誤差是曲率或 bootstrap | 要（全新一輪工作） |
| 年齡／消光／金屬量 | 自己擬合 | 直接用 HR23 的 `logAge50`／`AV50`／`MOD50`，MH 固定 0.00 | 要 |
| 轉折點截斷驗證 | 已驗證安全 | 兩個星團都沒驗證 | 要 |

**後果**：Tier 1 的數字不能寫進論文表 3 當最終數字，只能用來看方法差異會不會
比 M45 明顯。升級到 Tier 2 是重新執行整套腳本，不是既有結果的延伸。

---

## D 類

### D1 BHAC15 等時線未測（現役缺陷．優先度中高；正式規模已跑但撞牆，做不出可信比較數字）

**問題**：BHAC15 專為低質量前主序設計，而 M45 的年齡主要由低質量前主序
收縮軌跡約束。已測的 PARSEC 與 MIST 在這一段用相似的對流／大氣處理，
兩者一致不代表沒有共同偏差（見 C1）。BHAC15 質量範圍只到 0.015–1.4
M_sun（不蓋過 M45 擬合上限 2.50 M_sun），且只有太陽金屬量一組（MH 維度
形同鎖死）——這個比較設計上就只能檢驗低質量段，不能宣稱驗證全質量
範圍。

**後果**：網格已建置可用。正式規模比較（`--n-syn 40000 --refines 3,3`，
對稱亮端截斷 `--g-bright 8.9`）撞牆：`f_bin=0.939` 貼上界、`alpha=1.500`
貼下界，跑 1,160 秒後被 `WallError` 攔下，做不出可信的比較數字。兩個
不同規模（煙霧測試與正式規模）撞的是同樣兩個維度、同樣方向，比較像
真實簡併（BHAC15 質量上限加上亮端截斷後，能用的質量區間被兩頭夾得很
窄，資料撐不出 alpha 與 f_bin 的獨立約束）而不像蒙地卡羅雜訊，但目前
只有單一 seed 的一次執行，未經多次重複驗證，不算定案。完成條件第 1 項
（跟 PARSEC 同設定比較 alpha 與 logage）目前做不出可信數字——不是還沒
做，是做了但結果被邊界決定而非被資料決定。後續選項（尚未決定）：放寬
f_bin／alpha 搜尋範圍再試、拿掉或放寬亮端截斷（代價是失去跟 PARSEC
對照組「只差等時線」這個單一變因）、或接受「亮端截斷後的 BHAC15 比較
做不出可信數字」本身就是結論。

### D2 未做過敏感度測試的設定（部分進行中，membership_threshold 已有初步數字但有選擇函數未重新迴歸的但書）

**問題**：`pca_dims` 本身（非開關）、`stars_per_cluster`、`clustering_method`、
`inner_loop_runs`、`hess_color_range`／`hess_mag_range`、BP 訊噪比門檻 20
這個值本身、成員機率門檻對 IMF 的影響，這些設定的敏感度未知。

**後果**：`membership_threshold`（成員機率門檻）第一批正式規模掃描
（0.6 vs 0.7）已有數字：

| threshold | 成員數 | 測光品質篩選後 n_obs | alpha |
|---|---|---|---|
| 0.6 | 1,308 | 1,087 | 2.367 |
| 0.7（現行預設） | 1,297 | 1,078 | 2.367 |

alpha 完全沒變（跨度 0.000）。**但有重要但書**：`sensitivity_sweep.py`
重新套用門檻時刻意沿用同一份 `selection.npz`（用現行 0.7 門檻樣本迴歸
出的選擇函數係數），沒有對每個 threshold 重新迴歸——如果選擇函數係數
本該隨門檻變化，這個結果會系統性偏低估敏感度。正確講法：在固定
selection 係數的條件下，0.6–0.7 這個範圍沒有偵測到 alpha 變化；
`membership_threshold` 是否真的不敏感，要等 selection 係數也隨每個
threshold 重新迴歸、重跑一次才能確認。剩下三個門檻（0.5、0.8、0.9）
已排入佇列，但同樣沿用同一個 `selection.npz`，同樣的但書適用。
`stars_per_cluster` 需要重跑 pyUPMASK 聚類，尚待原始 Gaia 查詢 CSV
補齊依賴後才能測。

### D3 主序轉折點截斷對老星團會變成真限制（尚無認領工作）

**問題**：`pipeline/step5_imf.main_sequence_mass_luminosity()` 只保留 G 星等
持續變亮的那一段（轉折點以上質量與星等不再一對一，`np.interp()` 反查會錯）。
M45 已驗證安全：config C 等時線（logage=8.033）涵蓋 0.09–5.03 M☉、轉折點約
5.03 M☉，分析範圍上限只到 2.50 M☉，1,078 顆觀測星裡 0 顆因此變成 NaN。

**後果**：質量範圍延伸到接近轉折點、或換成老很多的星團（Hyades／Praesepe
~600–650 Myr，轉折點降到 ~1.5–2 M☉）時，要先重新驗證截斷有沒有把觀測星
排除在外。見 `docs/planning/PLAN_多星團擴展.md`。

### D4 冪律近似的有效性繫於樣本數（尚無認領工作）

**問題**：Moraux+2003 與 Chabrier 2003 都主張低質量段用對數常態。解析檢驗
（`check_imf_form.py`）：硬套單一冪律的殘差 RMS 為 8.6%（Moraux 參數）與
4.9%（Chabrier 參數），而 641 顆星分成 5／10／20 箱的 Poisson 相對雜訊是
8.8%／12.5%／17.7%，殘差小於雜訊。

**後果**：目前用分段冪律是安全的近似，但論文必須聲明判準（殘差 < Poisson
雜訊）。樣本大幅增加（例如多星團合併分析）時要重算——Poisson 雜訊隨 1/√N
下降而殘差不變。

### D5 `p6b4` 有一筆結果曾貼到網格邊界，補測後沒有再貼牆（已補測）

**問題**：8 筆裡 7 筆的 logage=8.2、MH=0.05、f_bin≤0.55，安全落在 PARSEC 網格
真實涵蓋（7.70–8.25）內部；`p_true=1.3` 的第 3 次試驗 logage=8.3，超出真實
上界 0.05。

**後果**：核心「可辨識」結論由另外 7 筆撐起，方向不受動搖。補測
`p_true=1.3`（單次試驗）：logage=8.133，安全落在 [7.70, 8.25] 內部，
沒有再貼到網格邊界，回收值 p_recovered=1.367（偏差 +0.067）、
alpha_rec=2.433（偏差 +0.083），量級跟原本 7 筆一致，支持「原本那筆
是單次試驗的隨機波動」的解讀，但只補測 1 次，不是完整 3 次，不能
100% 排除偶爾仍會貼牆的可能性。核心「可辨識」結論（p_recovered 跟隨
p_true、ratio ~0.92）不受影響。（這筆結果本身仍受 A1 影響，數字量級
參考、不是最終值。）

### D6 中間結果檔案會被重跑覆寫（六支計算腳本已全部有 `--tag`）

**問題**：固定檔名的 `np.savez` 每次重跑會覆蓋前一次的完整參數向量，
`p6b`／`p6b2`／`p6b3` 的完整 8 維參數向量已被後續嘗試蓋掉，無法回溯
查證，這部分是永久遺失。

**後果**：六支計算腳本（`fit_real.py`、`inject_lowmass.py`、
`injection_recovery.py`、`inject_massdep_fbin.py`、`profile_lowmass.py`、
`profile_outlierfrac.py`）現在都支援 `--tag` 避免覆寫（含路徑分隔符
檢查，避免 `--tag` 把輸出寫到 `results/` 之外），預設空字串維持原檔名。
**`--tag` 是「可以避免覆寫」，不是「自動避免」**——不帶 `--tag` 重跑
同一支腳本仍然會覆寫同名檔案；真正會擋下來的是
`checkpoint.check_manifest()`（設定不同時中止），但那只擋得住「設定
不同」，擋不住「設定完全相同、只是想保留前一次結果」，要保留就要自己
給 `--tag`。

### D10 config C／D 的 alpha 一致性只在假資料上驗證過，真實資料沒有直接比較過（尚無認領工作）

**問題**：`fit_real.py` 的 config D（dav 上界放寬到 1.2，對照 config C
的上界 0.6）設計目的是確認「dav 貼牆位置改變時 alpha 會不會跟著移動」
——如果 C、D 給出幾乎一樣的 alpha，代表 alpha 不受 dav 牆的位置污染。
這件事只在注入回收的合成資料上驗證過（alpha 偏差與對照組完全相同），
沒有找到一筆用真實觀測資料同時跑 `--configs C,D` 並比較 alpha 的記錄。

**後果**：目前對「alpha 沒被 dav 的貼牆位置污染」這件事的信心完全建立
在合成資料的注入回收結果上，真實資料是否也成立假設如此、沒有直接
驗證過。如果真實資料上 C、D 的 alpha 有明顯差異，代表現有 headline
數字可能受 dav 不可辨識這個已知問題間接污染，需要回頭檢視。

### D11 低質量段沒有獨立於等時線模型的經驗質量-光度關係校驗（現役缺陷．優先度中高；可行性評估已完成，見下方）

**問題**：目前唯二測過的質量估計來源（PARSEC、MIST）都是理論等時線，
共享類似的對流／大氣邊界處理（見 D1）。Hobart et al. 2026 額外用一條
完全獨立於任何等時線模型的經驗質量-光度關係（食雙星測光質量文獻彙編
＋B-spline 平滑），發現用這條經驗關係跟用等時線算出的低質量段
（<0.5 M☉）IMF 斜率明顯不同，高質量段則三種方法一致——低質量段的
斜率結論對「用不用等時線」本身很敏感，不只是對「哪一套等時線」敏感。

**後果**：我們現有的 PARSEC vs MIST 比較（P9 系列）只能排除「特定等時線
程式碼的實作差異」，排除不了「所有理論等時線共享的低質量段系統偏差」
這個更大的風險。可行性評估（`docs/planning/PLAN_D11_經驗質光關係_
可行性評估.md`）確認前置查證分兩階段：先做覆蓋範圍查證（質量精度換算
需要 `dM/dM_V` 而非 `dM/dG`，本機網格缺 V 波段，需另外下載；低質量段
樣本集中在 BP−RP≈2.4–3.5，任何 Gaia→V 轉換與食雙星彙編都必須在這個
顏色區間有驗證過的精度），通過後才進行消光處理一致性、經驗關係的
金屬量覆蓋、`dM/dM_V` 誤差傳播三項——五項查證全部完成後才決定是否
投入蒐集食雙星資料與實作。待認領工作見 `WORK_BOARD.md`。

### D12 亮端完整度沒有獨立目錄交叉驗證（現役缺陷；初步查證已完成，範圍比原問題描述更大，根因待追）

**問題**：`config.toml` 的 `g_bright_limit = 4.0` 只是「比這更亮測光不
可靠」的下限截斷，沒有獨立驗證這條線以上有沒有真正的星團成員被截掉。

**後果**：用 HIPPARCOS 交叉查證發現兩件事：(1) Alcyone（G≈2.90，最亮
成員）在 Gaia DR3 完全沒有視差／RUWE 解，這是 Gaia 本身的資料層級限制，
不是 pipeline 造成的；(2) 範圍比原問題描述更大——G=4.0–5.2 之間、視差
落在星團範圍、Gaia 測光正常的星也完全沒有出現在成員表裡，比
`g_bright_limit` 這條線本身多刷掉了將近 1.2 星等的範圍，這些星的
RUWE 明顯偏高（2.3–4.1，符合亮端已知聯星/聚星的天文學背景），但 RUWE
目前只在後續步驟當事後聯星旗標，不是成員表或 CMD 篩選的排除條件，
不能直接解釋為什麼整顆消失。根因尚未追到（受限於缺少原始 Gaia 查詢
檔案，見 D2），需要逐步比對 pipeline 第 1、2 步的中介輸出才能確認是
被 pyUPMASK 成員機率判定濾掉，還是被測光品質篩選濾掉。高質量段本來
就只有個位數顆星在支撐 alpha，若這 1.2 星等的範圍其實是可以救回來的
真實成員，高質量段的統計量會明顯增加，維持 D 級（已知風險，尚未證實
會污染現有結果）。

### D13 聯星比例只有前向模型一條觀測制約，沒有獨立的「凸出星」比例當第二條制約（尚無認領工作）

**問題**：我們的 `f_bin` 是聯合前向模型（Hess 圖概似）唯一由資料約束的
自由參數，另外三種逐星傳統判定法（RUWE、CMD 偏移、前向模型後驗機率）
是事後交叉核對，不是餵回同一個貝氏擬合當獨立制約。Hobart et al. 2026
的作法不同：他們用「主序以上偏移 2.5σ_p 以上」定義的觀測「凸出星」
比例 `f_SO`，把這個獨立測到的比例跟 MF 三段斜率一起放進同一個似然
函數聯合求解，讓 `f_bin` 同時被兩條獨立資訊制約，理論上能縮小 `f_bin`
與 alpha 之間的簡併。我們目前的架構讓 `f_bin` 只被 MF 形狀一條資訊
制約，凸出星比例完全沒有進到似然函數裡。

**後果**：`f_bin` 與各段 alpha 之間可能存在的簡併，目前沒有第二條
獨立觀測制約可以打破，只能靠前向模型本身的統計精度撐住。不影響現有
數字已知有錯，但代表 `f_bin` 的不確定度可能被低估。待認領工作範圍大，
屬於架構層級的增修，建議先在 `docs/planning/` 開獨立子計畫評估成本再
動工，不建議直接動 `pipeline/joint_fit.py`。

### D14 system MF 與 stellar MF 的區分已在文件與程式碼中明確標註（已完成）

**問題**：`fit_real.py`／`pipeline/joint_fit.py` 曾經沒有在程式碼註解
或 `PAPER_OUTLINE.md` 裡明確標註 `alpha` 這個量究竟對應 system MF
（單星＋聯星系統的主星）還是 stellar MF（樣本中每一顆恆星）。

**後果**：已逐行核對確認：`JointModel.synthesise()` 先抽 `n_syn` 個
主星質量（冪次＝alpha），再獨立擲 Bernoulli(f_bin) 決定誰帶伴星，
伴星質量不抽自 IMF，最後主星與伴星流量相加塌縮成同一個測光點——所以
alpha 是主星質量分布的冪次，等價於 system MF；伴星從未進入被擬合的
質量分布。已寫進 `PAPER_OUTLINE.md` 3.4 節與相關程式碼註解。兩種定義
差多少（`scripts/diagnostics/system_vs_stellar_mf.py` 實算）：stellar
MF 比 system MF 陡，f_bin=0.30→+0.046、0.45→+0.066、0.60→+0.085、
0.75→+0.102；我們擬合到的 f_bin 落在 0.45–0.62，量級是 +0.05 到
+0.09（小於統計誤差 0.144，但與好幾項系統誤差同量級）——這個數字
本身不是新的系統誤差項（我們報 system MF、內部一致），但拿去跟報
stellar MF 的文獻並排比較時必須先做這個換算。順帶記下的已知簡化：
Bernoulli(f_bin) 與 m1 獨立，等於假設雙星比例不隨質量變化，這一項
尚未量化（Torres+2025 對 Pleiades 觀測到雙星比例隨半徑是雙峰的，
質量相依性同樣沒有理由預設為常數）。

### D15 貼牆偵測曾經用搜尋軸自己的邊界，比先驗實際生效的邊界寬（偵測本身已修好；是否統一三層邊界待決）

**問題**：`injection_recovery.py` 的 `COARSE` 常數（六參數聯合擬合的
粗網格）A_V 軸與 logage 軸的名目範圍曾經比 `config.toml` `[joint_fit]`
實際生效的先驗邊界（`av_max`＝0.6、`logage_max`＝8.30）更寬，但兩邊
沒有同步——`multi_stage_best()` 的貼牆偵測（`check_walls()`）原本直接
取搜尋軸本身的邊界，沒有跟先驗邊界取交集，若某次真實擬合的最佳解被
先驗壓在邊界，貼牆偵測會誤判「沒貼牆」，讓一個被先驗邊界決定的角落解
悄悄當成收斂解通過。

**後果**：逐一核對過現有會受影響的每一批結果（headline、
`radial_final_reruns` 已完成的組）：A_V 全部遠低於先驗的 0.6，從未真正
貼到這道被誤判的牆，目前沒有任何已引用的數字受這個偵測漏洞污染。
偵測本身已修好（`bounds` 改成跟 `model.bounds` 逐維取交集，只會讓
判定邊界變緊、不會變寬，對既有結果數值逐位元不變）。尾端維度（例如
呼叫端傳了 `extra_axis` 但當次模型沒開對應自由度的情況）理論上仍可能
重演這個問題，但目前所有實際呼叫端的維度都對得上，暫不受影響。還沒
決定的是設計層面的問題：COARSE 的 A_V／logage 軸名目上比先驗寬，究竟
該把 `config.toml` 的先驗邊界放寬到跟 COARSE 一致，還是該把 COARSE
收回去跟先驗一致，兩者對「dav 貼牆是否連帶壓縮了 A_V 的可能性」這個
問題的診斷力不同，需要人決定。

### D16 NGC 3532 暗端紅藍選擇函數未通過（現役缺陷；優先度中）

**已驗證事實**：以 `main` commit `d9a09f4`、已保存 Gaia 成員與控制場重跑，
G≥17 的 `red_minus_blue_error=-0.199`，超過容許的 0.10；整體與星等分箱雖
通過，總 gate 仍為失敗。前向模型依規則沒有執行。

**科學後果**：目前不能引用 NGC 3532 的前向 alpha，也不能用它主張 M45
pipeline 已具跨星團普適性。這不是放寬門檻可以解決的問題。

**下一步**：檢查暗端紅、藍樣本的控制場代表性、SNR 與測光品質分布；只有
重新校準後用同一門檻通過，才能解鎖前向模型。

### D17 Praesepe 前向模型 `f_bin=1`，僅能作診斷（現役缺陷；優先度中）

**已驗證事實**：選擇函數三項驗收通過，但合併後 B 設定兩次擬合均
`f_bin=1.000` 貼物理上界，有效擬合數為 0；程式正確標為
`diagnostic_only` 並跳過注入回收。

**科學後果**：不得引用這兩次 alpha 作 Praesepe IMF，也不能用不含選擇函數
的 A 設定單次有效結果替代正式 B 結果。

**下一步**：先畫分星等／顏色的真實 CMD－最佳模型殘差，定位是哪一段主序
把聯星比例推向上限，再決定是否修改星團深度、旋轉、污染或雙星模型。

---

## 附：不需要做的事（避免重複提案）

**0.01–0.08 M☉ 那段不需要比照 A3 升格為自由參數**：G = 18 對應 0.173 M☉
（見 C16），0.08 M☉ 比這暗一大截，代表沒有任何一顆通過測光品質篩選的觀測星
落在 0.01–0.08 M☉。0.08–0.5 M☉ 值得升格是因為裡面有 641 顆星在推動概似；
這一段沒有星在推動概似，放成自由參數只會變成另一個 q_gamma（見 C4）——
不受資料約束、貼著先驗邊界遊走，沒有真正的估計值可報。這不是資源分配問題，
是資料本身沒有資訊量。
