# 下一輪對話的開場 prompt

把下面整段（從「===」到結尾）複製貼上當作新對話的第一句話。

===

我在做高中天文專研：用 Gaia DR3 資料求疏散星團 M45 的初始質量函數（IMF）。
這是接續前一輪的對話，請先讀完下列檔案再開始，不要憑猜測動手。

## 必讀（照順序）

1. `C:\Users\Alber\Claude\m45_membership\STATE.md`
   一頁現況：目標、關鍵數字、三條不能違反的原則、目前卡住的地方、待辦清單。

2. `C:\Users\Alber\Claude\m45_membership\研究日誌_2026-07-31_08-01.md`
   完整過程與所有數字，含**我犯過並更正的十項錯誤**。這節最重要，
   別重蹈。

3. `C:\Users\Alber\Claude\m45_membership\兩種IMF方法對照.md`
   傳統 IMF 方法與前向模型的流程、邏輯、優缺點對照。

4. `C:\Users\Alber\Claude\m45_membership\README.md`
   每個參數的作用、改動後果、預設值與理由、兩個數值陷阱
   （共用亂數、不要平滑 Hess 圖）。

5. `C:\Users\Alber\Claude\m45_membership\config.toml`
   所有可調參數集中在這裡，附中文說明。

## 教授給的資源（研究背景與規格）

資料夾：`C:\Users\Alber\Downloads\台東高中天文專研.2026-20260720T145252Z-1-001\台東高中天文專研.2026`

- `論文與報告\天文論文結構化格式.docx` — **論文必須照這個格式寫**。
  它的「討論」章節明確要求探討「觀測到的 IMF 是否受未解析雙星影響而
  高估整體質量」與「質量分化是動力學演化還是原初特徵」，
  正好對應我們做出結果的兩處。也要求所有數值附 16–84 百分位不確定度。
- `師大計劃端\初步說明.20260611.docx` — 教授的研究定位說明。
  核心爭議：IMF 是否具宇宙普適性，還是隨金屬量與環境變異。
- `lectures\IMF.20260613.pdf`、`lectures\GAIA_OC.20260517.pdf` —
  講義（圖片式投影片，沒有文字層，要讀得用視覺方式）。
- `papers\` — Salpeter 1955、Kroupa 2002、Kroupa 2024 等原始論文。
- `20260709.note.docx` — 教授的作業指示。

專案分三個 Track，我們負責 **Track A：聯星解構**
（CMD 幾何框選與光度拆解，對比聯星修正如何系統性改變斜率 α）。
另兩組做 Track B（重力動態、弛豫時間）與 Track C（星系環境、α 與銀心距離）。
教授的指示是**先把一個星團做好驗證可行**，再推廣到 10 個星團。

## 研究目標：填補兩個已查證的文獻缺口

**缺口一**：Cordoni et al. 2023（A&A）對 78 個疏散星團量出雙星比例
（15%–60%），但**該文的質量函數計算完全沒用上這個結果** ——
所有偵測到的星仍當單星做星等轉質量。斷點在「算出雙星比例」與
「用它修正 IMF」之間。

**缺口二**：**沒有任何論文把不同的雙星判定法擺在同一個星團上互相比較**。
Cordoni 2023 明講只用 CMD 光度法且未與其他方法驗證。
我們已做四種（前向模型、RUWE、CMD 偏移、Gaia NSS），
實測兩兩重疊最高只有 18%。

## 目前的進度

**已完成**
- 五步 pipeline 全部跑通（成員判定 → CMD → 年齡 → 雙星 → IMF）
- 成員數 1,297（P≥0.7，G<18）；測光品質篩選後 1,078
- 對 Hunt & Reffert 2023：recall 1.000（930 顆全中），真正分歧僅 20 顆
- 四種雙星判定法的比較表（缺口二的核心結果）
- α：傳統法 1.980±0.069，前向模型 2.26–2.35，差約 0.3（缺口一的核心結果）
- 質量分層：α 從中心 1.77 升到外圍 2.29（最穩固的結果，跨所有重跑一致）
- 六參數聯合擬合（年齡、消光、雙星比例、α、金屬量、q_gamma）已實作
- ARM64 原生 Python + 8 行程平行，比原本快 13.9 倍

**卡住的地方（下一輪的第一要務）**
概似函數把 CMD 切成 2,000 個格子、當成 2,000 份獨立證據，
但實際只有 1,078 顆星。這讓概似「過度自信」，後果是：
高斯先驗完全推不動後驗（先驗懲罰 −2.2 對上概似差異數百）、
MCMC 接受率只有 0.064、自相關時間隨鏈長增長（261 → 1025）。

正在做的驗證：切半實驗量高估倍數（初步 α 約 1.5x），
但 10 次切分裡有 4 次 α 落在 2.68、遠離其他值，
懷疑是兩階段網格搜尋卡在假峰。已排全掃驗證，**結果未出**。

**根本修法（尚未實作）**：改用無分箱概似 ——
對每顆觀測星直接取模型密度的對數再相加，恰好 1,078 項，
證據不會被重複採計。模型密度用合成星團的核密度估計。
代價是引入帶寬這個新的任意參數，需用輪廓測試量其影響。

## 我犯過的錯（別重蹈）

十項完整記錄在研究日誌第六節，這裡列最關鍵的四項：

1. **在建立比較基準之前就解讀結果** —— 十項錯誤裡有八項源於此。
   先報 PCA 效應才量雜訊底線、先報誤差棒才做輪廓測試、
   先宣稱相關係數才檢查鏈是否收斂。
2. **用對照目錄調自己的超參數** —— 曾用 F1 對 HR23 挑機率門檻，
   結果挑到方法最脆弱的點（P≥0.99 換個種子成員數差 48%）。
3. **以「M45 金屬量接近太陽」為由固定金屬量** ——
   那是基於對特定天體的知識而非資料的函數。
   套到全銀河更糟：金屬量有銀徑梯度，全部固定太陽值會讓偏移量
   與銀心距離相關，憑空製造假的空間趨勢。
4. **繞過問題而非修根本** —— 加均勻背景項、換高斯先驗、
   用外部資料約束金屬量，這些都是症狀端修補，
   沒有動到「2,000 格當獨立證據」這個病根。

## 工作方式的要求

- **先確定方法沒有邏輯問題，再產出最終數據。** 不要為了交出一個數字
  而繞過方法學問題。
- **長時間任務一律用 8 核平行。** 網格搜尋、MCMC 都可以。
  注意模型必須用 `Pool` 的 initializer 只送一次，否則序列化開銷
  會把平行效益吃光（實測從 1.2x 變 5.0x）。
- **平行沒生效時先診斷是四個原因裡的哪一個**（記憶檔
  `python-multiprocessing-pickle-trap` 有完整對照表）：
  沒平行化／物件每次重送／負載不均／行程被回收。
  診斷關鍵是**數有幾個行程在忙**，不是只看整機百分比。
  特別注意負載不均：網格搜尋不要按某個維度切工作（那維格點常只有個位數），
  要把所有組合攤平成線性索引、切成遠多於行程數的小塊。
- **長時間任務要用 `Start-Process` 啟動脫離式行程**，不要用一般的背景執行。
  一般背景工作會在對話工作階段結束時被回收，只留下孤兒工人空轉 ——
  症狀是 CPU 先衝到 100% 然後莫名掉回 10%。範例：
  `Start-Process -FilePath $py -ArgumentList "-u","script.py","--procs","8"
   -WorkingDirectory <專案目錄> -RedirectStandardOutput "logs\x.log"
   -RedirectStandardError "logs\x.err" -WindowStyle Hidden`
  注意 `Start-Process` 的重導向會用系統 ANSI 編碼寫檔，中文會變亂碼，
  讀取時要指定編碼或改用純 ASCII 輸出。
- **可量化的問題先算出數字再下結論**，不要用感覺。
- 每次改動都要回報做了什麼、為什麼。

## 環境（別重新診斷）

- ARM64 原生 Python：`%LOCALAPPDATA%\Python\pythoncore-3.14-arm64\python.exe`
  比 x64 模擬快 13.9 倍（`gaussian_kde` 單項 38 倍）
- **astropy 在 ARM64 上裝不起來**（相依 pyerfa 無 ARM64 wheel、本機無編譯器），
  已用 `pipeline/table_compat.py` 取代，兩邊算出的 lnP 完全相同
- pyUPMASK 仍需 x64（用到 astropy 的 RipleysKEstimator）
- PARSEC isochrone 服務的憑證鏈缺中繼，需先跑一次 `setup_ca.ps1`

## 專案檔案清單

程式（`C:\Users\Alber\Claude\m45_membership\`）：
- `config.toml` — 所有參數
- `run_pipeline.py` — 五步循序流程（`--steps 1 2 3 4 5`）
- `run_joint.py` — 六參數聯合擬合 MCMC
- `fetch_gaia.py` / `prep.py` — 抓資料與前處理
- `run_variant.py` — 跑單一組 pyUPMASK 變因
- `validate.py` / `disagree.py` / `investigate.py` — 對 HR23 交叉驗證
- `compare_variants.py` / `compare_prob_methods.py` / `compare_depth.py` — 各種對照
- `profile_test.py` — 輪廓測試（量固定某參數的代價）
- `measure_overconfidence.py` — 切半實驗（量概似高估倍數）
- `verify_twostage.py` — 驗證兩階段網格搜尋有無卡假峰
- `prior_fix.py` — 修正 pyUPMASK 的等機率先驗
- `gaia_astrophys.py` — 取 Gaia 的獨立金屬量與消光估計
- `final_imf.py` — 最終 IMF（**尚未可用，等方法學確定**）
- `plots.py` / `plot_step3.py` / `plot_step45.py` — 作圖
- `benchmark_arch.py` — x64 與 ARM64 的效能對照
- `pipeline/` — config、net、isochrones、table_compat、step2–5、joint_fit

文件：
- `STATE.md`、`研究日誌_2026-07-31_08-01.md`、`兩種IMF方法對照.md`
- `README.md`、`METHODS.md`、`COMPARISON.md`
- `簡報補充文字.md` — 給簡報用的待補文字

資料：`data/`（Gaia 原始檔、成員表、HR23 對照表）、
`isochrones/`（PARSEC 網格，最大 94 MB）、`results/`、`figs/`、`logs/`

## 這一輪希望你做的

1. 先讀完上列必讀檔案
2. 確認 `verify_twostage.py` 的驗證結果（在 `logs/verify_bal.log`），
   判定兩階段搜尋能不能用。
   判讀方式：比較「兩階段」與「全掃」兩者的 lnP。
   差值接近 0 → 兩階段找到的就是全域最佳，先前那批切半結果有效；
   全掃的 lnP 明顯較高 → 兩階段卡在假峰，那批出現 alpha=2.68 的樣本作廢，
   「高估倍數 1.5」的結論也不成立，需改用別的搜尋策略重測
3. 實作無分箱概似，重測高估倍數，確認降到 1 附近
4. 方法學確定後才產出最終 IMF 與誤差棒
5. 對傳統法做**完全相同**的誤差核算，兩者放同一張表比較
   （這是缺口二的核心，也是回答「前向模型有沒有優勢」的唯一嚴謹方式）
