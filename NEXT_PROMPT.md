# 下一輪對話的開場 prompt

把下面整段（從「===」到結尾）複製貼上當作新對話的第一句話。

===

我在做高中天文專研：用 Gaia DR3 資料求疏散星團 M45 的初始質量函數（IMF）。
這是接續前一輪的對話，請先讀 GitHub 上的文件再開始，不要憑猜測動手，
也不要預設任何舊結論是對的——上一輪剛做完一次全專案審查，推翻了好幾個
先前以為沒問題的結果。

## 專案已經開源到 GitHub（私有 repo）

**`github.com/helmet-png/m45-imf-analysis`**（同學可受邀 collaborator）。
本機資料夾是 `C:\Users\Alber\Claude\m45_membership\`，兩邊已同步。
`results/` 資料夾整個進版控（每次跑完計算都要 commit + push，不必等我要求）；
`isochrones/`、`kaggle_work/`、`kaggle_results/`、原始 Gaia 下載檔
不進版控（可重建，見 README.md「環境建置」）。

**規則：先讀 GitHub 上最新版本的以下三份文件，它們才是目前的真實狀態**，
不要相信任何比它們舊的記憶或摘要：

## 必讀（照順序，全部在 repo 根目錄）

1. **`LIMITATIONS.md`** —— 全專案已知限制、已推翻說法、以及
   **2026-08-10 全面審查**的完整記錄。這是目前最重要的檔案。
   維護規則：發現新限制先寫這裡；推翻舊說法要**同時回頭把原處標記撤回**，
   不能只在「已推翻」那節新增——上一輪就是因為沒做到這點，製造過
   內部矛盾（G<20、等時線系統誤差、低質量段處理方針都曾經自相矛盾過）。

2. **`docs/planning/PAPER_OUTLINE.md`** —— 論文範圍凍結文件，含：
   - 誤差預算表（目前排序：低質量段冪次 0.248 最大、測光選擇函數 0.18、
     統計誤差 0.144、雙星處理只有 0.02 最小）
   - **「全專案結果審查表」**：按方法論家族列出所有跑過的計算，逐一標記
     受哪個已知問題影響。**這張表本身也還沒逐一查證完every一列**，
     標記「未查」的地方要當成待辦，不是當成沒問題。
   - 待辦清單（P1–P10 + 新增項），優先序已排好

3. **`docs/teaching/教學_傳統法誤差核算.md`** 與**《docs/reports/報告_完整說明.md》**（八章）——
   給高中生程度的完整方法論教學，前者聚焦 P1（傳統法核算），後者是
   從資料到方法的完整推導，含每一次程式執行的動機與試錯過程。

## 上一輪（2026-08-10）做了什麼、發現了什麼

**起因**：使用者質問「先驗跟網格沒對上的問題，難道只可能影響到 p2_final2
和 p9a2 這兩批數據嗎？」——之前只抽查了兩批就下結論，範圍太窄。

**做的事**：對全部跑過的計算做系統性重查（先用自動掃描抓候選、再逐一
人工核對，因為自動掃描本身有假警報）。

**發現的三件事**（都寫進了 LIMITATIONS.md 與 docs/planning/PAPER_OUTLINE.md，這裡只列
結論，細節去讀文件）：

1. **P9 的 PARSEC 與 MIST 結果全部作廢**：
   - PARSEC 版（`p9a2_fixmh_parsec`）5 次裡有 2 次 `f_bin` 精確撞到修好前的
     舊上界 0.750，α=2.460±0.233 這個數字不可信。
   - MIST 版連續三次「撞到年齡下限」被誤判為「模型的決定性訊號」，
     查證後發現是 **MIST 網格檔本身只涵蓋 logAge 7.80–8.50**，任何更低的
     搜尋請求都被吸附到同一條 7.80 等時線形成簡併平坦區——是網格覆蓋
     不足的假象，不是模型行為。**這個錯誤結論一度差點被我當真寫進文件**，
     虧使用者質問才發現。

2. **`fit_real.py` 等全部七支「產出數字」的腳本都寫死 `mh_prior_sigma=0.0`**，
   從未使用 `config.toml` 記載、有 Gaia 光譜佐證的高斯先驗（中心 −0.03）。
   `p2_final2`（目前的 α=2.387±0.060）因此**不能當最終數字**，先驗補上後
   α 會不會變沒有驗證過。

3. **直接核對後確認沒事的關鍵數字**：`S3F`（σ_α=0.144 的來源）、
   `p6_lowmass`（0.248 系統誤差的來源）、表 4 的核心證據
   （`item3b/c_mistfit`、`p3b_dr2fit`）。這些的 f_bin/alpha/logage
   都安全落在各自網格真實涵蓋範圍內部。

## Kaggle 多帳號並行基礎設施（已建好，可用）

`kaggle_sync.py` / `kaggle_accounts.py` / `kaggle_queue.py`。兩個真實帳號
已登記在 `kaggle_accounts.json`（不進版控，helmetalbert + teammate2/
alex981113）。`kaggle_queue.py` 支援多帳號同時派工（每輪迴圈對每個閒置
帳號指派新工作，不是排隊等）。**已知限制**：dataset 掛載延遲不穩定，
自動重試機制（60/120/240/480 秒遞增）有時仍會在額度內失敗——
上一輪派出的兩個 Kaggle 工作（`p9a_more_reps`、`p6_lowmass_ext_retry`）
最後都失敗了，屬已知風險非新問題，需要時就重推。跨專案通用寫法記在
`~/.claude/kaggle-compute-howto.md`。

## 立刻要做的待辦（優先序，細節見 docs/planning/PAPER_OUTLINE.md 待跑清單）

1. **P9a-redo**：用已修好的 f_bin 範圍（0.15–0.95）重跑 PARSEC 鎖 MH 版
2. **P9c**：重新下載涵蓋更廣年齡範圍（例如 7.30–8.50）的 MIST 網格，
   才能重新回答 P9 的 MIST 版
3. **P10**：移除 `fit_real.py` 裡 `mh_prior_sigma=0.0` 那行覆寫，改回讀
   config 的高斯先驗，重跑 config C 比較 α 有沒有變——**這個沒做完，
   P2 的最終數字就不能定案**
4. **P6b 重跑**：目前這組的結論實質上是空的（撞牆、scoping bug 陸續發生），
   需要一次乾淨的完整重跑
5. 檢查本機 `queue.txt` 有沒有還在跑或卡住的項目（`p6b4_inject_lowmass`
   上一輪結束時還在執行中，先確認它跑完了沒有）

## 工作方式要求（沿用不變）

- 先確定方法沒有邏輯問題，再產出最終數據，不要為了交出數字繞過問題
- 長時間任務一律用 8 核平行（本機）或多帳號並行（Kaggle）
- 用 `Start-Process` 啟動脫離式背景行程，別用一般背景執行（工作階段結束
  會被回收）
- **搜尋範圍撞牆時，先查底層資料（isochrone 網格）到底涵蓋到哪裡，
  再決定要放寬搜尋軸還是要重下網格**——這是上一輪犯的核心錯誤，
  兩者是不同的問題、不同的修法
- 每次改動都要回報做了什麼、為什麼；每次算出新結果都要主動同步寫進
  `docs/planning/PAPER_OUTLINE.md`／`LIMITATIONS.md`，不必等我要求
- 每次跑完計算都要 commit + push 到 GitHub，不必等我要求
- 可量化的問題先算出數字再下結論，不要用感覺；抽查範圍要說清楚查了
  什麼、沒查什麼，不要用兩筆樣本就推廣到全部
