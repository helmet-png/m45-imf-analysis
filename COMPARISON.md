# 方法對照與完整參數表

給論文方法章節用。第一部分是三套方法逐項對照；第二部分是我這條 pipeline 用到的
每一個變數與其實際數值，附檔案位置可回查。

---

## 一、與前人方法的逐項對照

| 項目 | 本研究（pyUPMASK） | Hunt & Reffert 2023 (HR23) | Cantat-Gaudin & Anders 2020 |
|---|---|---|---|
| 資料版本 | Gaia DR3 | Gaia DR3 | Gaia DR2 |
| 天區範圍 | 5° 錐形（人工指定中心） | 全天盲搜 | 全天，以已知星團為中心 |
| 樣本篩選 | G < 18，視差 > 4 mas | Rybizki+2022 品質旗標 ≥ 0.5（與星等無關） | G < 18 |
| 分群空間 | 3 維：pmRA*, pmDE, Plx | 5 維：位置 + pmRA*, pmDE, Plx | 3 維：pmRA*, pmDE, Plx |
| 位置是否入分群 | 否（另用 Ripley's K 做空間集中度檢定） | 是（直接混入 5 維） | 否（用最小生成樹做空間檢定） |
| 座標前處理 | gnomonic 切平面投影，可選扣除整體運動的透視效應 | 各天區重新定心、依 IQR 縮放，未做透視修正 | 未提及透視修正 |
| 降維 | PCA 降到 2 維 | 無（直接用縮放後的 5 維） | 無 |
| 分群演算法 | MiniBatchKMeans（pyUPMASK 內圈） | HDBSCAN | k-means（UPMASK 內圈） |
| 隨機性來源 | 誤差重抽 + KMeans 初始化，25 輪外圈取平均 | 4 組固定參數設定（mClSize ∈ {10,20,40,80}），非重抽 | 依各星測量誤差重抽，多輪取頻率 |
| 機率定義 | KDE 概似比後驗的多輪平均：P = mean(1/(1+L_field/L_memb)) | HDBSCAN 軟分群機率（點到群核心的鄰近度） | 該星在多少比例輪次中被歸為集中群的頻率 |
| 機率是否會飽和 | 會（概似比後驗，分離度越高越快飽和到 1） | 未知（未查證飽和行為） | 不會（頻率型，上限受輪數限制） |
| 使用的成員門檻 | P ≥ 0.7（依自身分布穩定谷地決定，見下） | P ≥ 0.5（於推導星團參數階段套用） | P > 0.7（常見引用值） |
| 離群值前處理 | ±5σ（stdregion 法），視差已先切 > 4 mas | 未見獨立的離群值前處理步驟 | 未見獨立的離群值前處理步驟 |
| 空間集中度是否獨立驗證 | 是（Ripley's K，與分群完全分開） | 否（集中度隱含在 5 維分群本身） | 是（最小生成樹） |

**三個要點，決定了為什麼機率數值不能跨方法直接比較：**

1. HR23 把位置直接混進分群空間，本研究把位置留給獨立的空間檢定 —— 兩者對「重力束縛」
   與「運動學一致」這兩件事的證據來源不同，不是同一件事的兩種算法。
2. 本研究與 Cantat-Gaudin 的機率都叫「成員機率」，但一個是密度比後驗（會飽和），
   一個是重抽頻率（有硬上限）。兩者的數值刻度不可通約，見 [METHODS.md](METHODS.md) 第六節。
3. HR23 沒有做透視效應修正；本研究額外測試了這一項，且已用「已知成員星的自行運動
   散布是否縮小」做過驗證（見下方變數表 D 區）。

---

## 二、本研究使用的完整變數表

依 pipeline 順序列出。每一項附實際數值、來源檔案、選擇理由。標「✓已驗證」的項目
代表做過敏感度測試，確認改變它不影響最終成員名單；標「〜未測」的則尚未測試其影響。

### A. 資料擷取（[fetch_gaia.py](fetch_gaia.py)）

| 變數 | 數值 | 說明 |
|---|---|---|
| 星表 | `gaiadr3.gaia_source` | ESA Gaia Archive，TAP 協定 |
| 天區中心 | RA 56.60083°, Dec 24.11389° | CDS Sesame 解析 "M45" |
| 錐形半徑 | 5.0° | 涵蓋 HR23 全天成員的 92.7%（1,595/1,721 顆），見 METHODS.md 第一節 |
| 星等上限 | G ≤ 18 | 與 Cantat-Gaudin 2020 相同切法，Gaia 天測精度在此之後明顯下降 |
| 視差下限 | Plx ≥ 4 mas | 對應 ≤ 250 pc；實測對 HR23 成員零誤殺（見下） |
| 擷取欄位 | source_id, ra, dec, pmra, pmdec, parallax, 及三者誤差, phot_g_mean_mag, bp_rp, ruwe | 後三者不參與分群，供 CMD 與品質診斷用 |
| 最終樣本數 | 6,956 顆 | — |

**〜未測**：星等上限與視差下限本身的敏感度（只驗證過視差切「不誤殺成員」，未測改變
數值本身對最終名單的影響幅度）。

### B. 座標前處理（[prep.py](prep.py)）

| 變數 | 數值 | 說明 |
|---|---|---|
| 投影方式 | gnomonic（切平面）投影 | 非直接用 RA/Dec，避免 dec=24° 處的東西向拉伸 |
| 投影原點 | 同天區中心 | — |
| 换算常數 K | 4.740470446 | mas/yr → km/s 標準換算（乘以 pc 距離） |
| 透視效應修正（`--deproject`） | 可選，✓已驗證 | 用星團整體三維速度反推每顆星「應有」的自行運動並扣除 |
| 星團整體參數（供修正用） | pmRA=19.955, pmDE=−45.457 mas/yr, Plx=7.378 mas, RV=+5.34 km/s | 取自 HR23 clusters 表 |
| 修正效果 | 已知成員自行運動合成標準差 1.517 → 1.385 mas/yr | 用 HR23 Prob≥0.5 成員當獨立對照組驗證，非循環論證 |
| 缺值處理 | 六個分群欄位（pmRA/pmDE/Plx 及三者誤差）任一缺值即整列丟棄 | 本資料集實測丟棄 0 列 |

### C. pyUPMASK 分群參數（[pyUPMASK/params.ini](pyUPMASK/params.ini)）

| 變數 | 本研究採用值 | pyUPMASK 預設值 | 常見文獻用值 | 敏感度 |
|---|---|---|---|---|
| ID 欄位 | source_id | None（自動編號） | — | — |
| 分群欄位（`data`） | pmRA, pmDE, Plx | 同 | 同 | — |
| 座標欄位（`xy_coords`） | 切平面 _x, _y | 原始 x,y | — | — |
| 誤差欄位（`uncert`） | e_pmRA, e_pmDE, e_Plx | 同 | — | — |
| 離群值排除法 `oultr_method` | stdregion（±N 標準差） | stdregion | — | 〜未測 |
| 離群值門檻 `stdRegion_nstd` | 5.0σ | 5.0 | — | ✓已驗證對 HR23 成員零誤殺 |
| 外圈輪數 `OL_runs` | 25 | 25 | 10（常見應用文獻） | ✓已驗證（見下） |
| 誤差重抽 `resampleFlag` | True | **False** | — | 必須開啟，否則完全不使用測量誤差 |
| PCA 降維 `PCAflag` | True | True | — | ✓已驗證：關閉後 P≥0.99 差 36%，但 P≥0.7 僅差 2.3%（見下） |
| PCA 維度 `PCAdims` | 2 | 2 | 3（部分應用文獻） | 〜未測（僅測過開關，未測維度數本身） |
| GUMM 清理 `GUMM_flag` | True | True | — | 〜未測 |
| GUMM 百分位 `GUMM_perc` | auto | auto | — | 〜未測 |
| KDE 機率估計 `KDEP_flag` | True | True | — | 必要（此步驟即機率之來源，見對照表機率定義列） |
| 內圈最大輪數 `IL_runs` | 25 | 25 | — | 〜未測 |
| 每群最少星數 `N_membs` | 25 | 25 | 10（部分應用文獻） | 〜未測 |
| 最大群數 `N_cl_max` | 1000 | 1000 | — | 不影響（本樣本規模遠低於此上限） |
| 分群演算法 `clust_method` | MiniBatchKMeans | MiniBatchKMeans | — | 〜未測（未與 DBSCAN/GMM 等替代演算法比較） |
| 亂數種子 `rnd_seed` | 42（另測 43, 44, 99 做雜訊底線） | None（隨機） | — | ✓已驗證：P≥0.7 時種子間差異僅 1.1% |
| 誤差重抽相關性 | 三維共用單一亂數（pyUPMASK 原始行為）；`PYUPMASK_INDEP_RESAMPLE=1` 可切換為三維獨立抽樣 | 共用單一亂數 | — | ✓已驗證：改為獨立抽樣僅造成 1.3% 差異 |

**關鍵警示（寫進論文方法章節）**：`resampleFlag` 的 pyUPMASK 預設值是 **False**，
若未手動開啟，等於完全不使用 Gaia 提供的天測誤差，機率值會失去統計意義。

### D. 成員判定門檻（本研究自行決定，非套用外部文獻值）

| 變數 | 數值 | 決定依據 |
|---|---|---|
| 成員機率門檻 | P ≥ 0.7 | 見下方穩定性測試；**不是**對 HR23 做 F1 最佳化的結果 |
| 決定依據：跨種子穩定性 | P≥0.5：離散度 1.1%；P≥0.7：1.1%；P≥0.9：1.7%；P≥0.95：9.2%；P≥0.99：48% | 四組獨立亂數種子（42/43/44/99）重跑後比較 |
| 為何不能沿用 Cantat-Gaudin 的 0.7 | 數值恰好相同純屬巧合 | 兩套機率構造不同（概似比後驗 vs 重抽頻率），數值不可通約，見對照表 |

### E. 驗證方法（[validate.py](validate.py), [investigate.py](investigate.py), [disagree.py](disagree.py)）

| 變數 | 數值 | 說明 |
|---|---|---|
| 比對目錄 | Hunt & Reffert 2023, VizieR J/A+A/673/A114/members | 同為 Gaia DR3，用 source_id 直接對，無需天球配對 |
| 參考成員門檻（敏感度雙組） | HR23 Prob ≥ 0.5 與 ≥ 0.7 | 用以檢驗 precision/recall 對參考門檻的敏感度 |
| 評估指標 | precision, recall, F1, ROC-AUC，及門檻掃描曲線 | — |
| 排除項目 | probs_final = −1（離群遮罩，非機率） | 計算 AUC 時排除，避免誤計為「高信心非成員」 |
| 獨立驗證項目 | CMD（BP-RP vs G），未參與分群 | 判定成員應自成一條主序帶 |

---

## 三、尚待決定或補強的項目

- PCA 維度數本身（非開關）、`N_membs`、`clust_method` 的替代選項尚未做敏感度測試
- 未做「排序前 N 名重合度」比對（不依賴任一方門檻的比較方式）
- 不切視差的完整對照跑（198,672 顆）因算力限制未執行；已測到 plx>2（21,810 顆）為
  實務可行的最大階
- OL_runs 是否需要超過 25（如提高到 100）以縮窄機率分布：測試進行中
