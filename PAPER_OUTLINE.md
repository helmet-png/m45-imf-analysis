# 論文大綱（範圍凍結文件）

**規則：不在此大綱上的計算不跑。** 每個圖表對應到「已有結果」或「待跑清單」的一項。
最後更新：2026-08-09

**維護規則（使用者 2026-08-09 指示）**：新的修正或補充發現，不必等使用者
明確說「加進 outline」，算出結果或推翻舊說法後就直接同步寫進本檔對應章節。

**工作標題**（暫）：Quantifying the methodological error budget of open-cluster
mass-function slopes: unresolved binaries, selection, and model choice in M45

**定位**（使用者 2026-08-07 定案）：方法學為主軸、M45 為示範案例。
金屬豐度普適性是後續（科展延伸＋教授框架），本篇只鋪路：
證明單一星團的 α 能對方法選擇穩健，跨星團比較才有意義。

---

## 新穎性聲明（2026-08-07 文獻查證後修正）

**不能宣稱的**（前人已做）：
- 前向模型／混合模型擬合星團 CMD 含雙星：Li, Shao et al. 2020（NGC 3532，
  ApJ 901, 49）、BASE-9（"Goodbye to Chi-by-Eye"，六個疏散星團）、
  Cordoni et al. 2023 本身就是光度法族群擬合。**前向模型不是我們發明的。**
- 「忽略雙星使 α 偏平」的定量預測：arXiv:2603.15779 已給出偏差公式與
  族群式概似的解法。
- 「不同方法給出不同雙星比例」：arXiv:2604.20722（2026-04，含 M45！）
  已用 Gaia CMD 與紅外-可見雙圖比較，雙星比例 0.16–0.36 vs 0.21–0.44。

**2026-08-07 撤回的兩個宣稱**：
- ~~「雙星修正使 alpha 改變 0.3–0.4」~~ —— 注入回收的 f_bin=0 對照組推翻。
  雙星真實貢獻約 **0.02**，與 arXiv:2603.15779 的光度相加預測（−0.011～−0.021）
  一致。原本量到的 −0.24 幾乎全部來自**我們自己**的質量範圍定義不一致。
- ~~「文獻上被歸因於雙星處理的差異，其實來自質量範圍定義」~~ ——
  **查不到任何文獻做過這種歸因**，這句是編造的。歸因錯的是我們自己；
  質量範圍不一致是本專案的實作 bug，不是可發表的發現。

**仍可宣稱的**（查證後仍是空缺）：
1. **四個方法家族的逐星重疊統計**（RUWE 天測 × CMD 光度 × NSS 目錄 ×
   前向模型逐星機率）：2604.20722 比較的是兩種光度法的族群數字，
   沒有逐星交叉、沒有天測法。兩兩重疊 ≤18% 是新結果。
2. **把雙星比例接回質量函數，並回報效應很小**：Cordoni 2023 量了 f_bin
   卻沒用它修正 MF；我們接回去，實測對 alpha 的影響只有約 0.02。
   這是一個**負面結果**，但仍有價值：它把「雙星修正」從待辦事項降級為
   次要項，並與 2603.15779 的理論預測獨立相符。
3. **單一偵測法剔除雙星會把偏差換成方向相反的另一種**：實測 CMD 偏移法
   剔除後 alpha 從 −0.013 變成 **+0.120**（因為它優先抓質量比接近 1 的
   雙星，主星偏重，整組移除等於系統性刪重星）；f_bin=0 時仍誤剔 14.7 顆。
4. **同一套實證誤差核算放在傳統法與前向模型兩邊**，並示範修正後的 alpha
   對等時線選擇穩健（0.28 → 0.04）。**這是本篇的主要貢獻——但用語需校準（見下）。**

**2026-08-08 用語校準（使用者質疑後查證）**：三輪目標搜尋沒找到完全相同的
組合，但也發現我方法的「注入回收」跟文獻標準詞「artificial star test」
不是同一件事——AST 測的是**照片層級的光度回收**（PHAT/PHATTER 用於銀河系外
IMF），我們測的是**整條擬合流程能不能找回已知物理參數**。這是不同層級。
「用合成資料量偏差」與「跨等時線族驗證（BASE-9 早已常規）」兩者分開看
都不新。**誠實定位：不是發明新方法，是把兩個已知實務第一次組合、
在同一個星團上逐項算出一張誤差預算表。** 論文裡改稱
「pipeline-level parameter-recovery test」，與 AST 並列引用但不混用同一詞。
貢獻聲明要降級為「核算的完整性與逐項可比性」，不是「方法本身」。

**修正後的論文主軸**（2026-08-07）：不是「雙星修正很重要」，
而是**「開放星團質量函數斜率的分析選擇誤差預算」**——逐項量化哪些選擇會動 alpha：

| 分析選擇 | 對 alpha 的影響 | 狀態 |
|---|---|---|
| **低質量段冪次固定在 1.3** | **0.248** | **已量（08-09），目前最大的單一誤差項，⚠ 2026-08-11 查出用的是完全沒精修的粗網格結果（`multi_stage_best()` bug），數字降級為「量級如此，精確值待 `p6_lowmass_v2` 重跑確認」，見 LIMITATIONS.md** |
| 測光品質選擇函數未建模 | 0.18 | 已量 |
| 統計誤差（1,078 顆） | 0.144 | 已量（注入回收 S3F） |
| 質量範圍與函數形式定義 | 約 0.2 | 已量 |
| 差異消光假設值 | 0.07–0.20 | 已量（品質待改善） |
| 等時線模型 —— 恆星演化（修正前/後） | 0.240 / **0.000** | 已量（08-09，P3） |
| 等時線模型 —— 濾光片定義（DR2/EDR3） | 0.040 | 已量（08-09，P3） |
| 雙星處理 | **0.02** | 已量（最小的一項） |

**低質量段冪次的細節（2026-08-09 新增，改變了誤差預算的排序）**：
加權迴歸給 d(alpha)/d(p) = −0.495 ± 0.111（4.5σ），傳播 Kroupa (2001)
的 alpha1 = 1.3 ± 0.5 得到 0.248。**用文獻先驗鎖住無法消除它**——
先驗寬度本身就是誤差來源，故改走「升格為自由參數」路線（P6b 驗證中）。

更麻煩的是文獻本身分歧且**對 M45 有方向性**：

| 來源 | 0.08–0.5 M☉ 的等效冪次 | 對我們 alpha 的影響 |
|---|---|---|
| **Moraux+2003（Pleiades 本身）** | **0.84** | **需 +0.23** |
| Chabrier 2003（銀盤場星） | 1.37 | 幾乎不動 |
| 我們目前固定 | 1.3 | — |

Moraux 量的正是 M45，比通用值更該採信 → 目前可能**系統性低估 alpha 約 0.2**。

**函數形式（冪律 vs 對數常態）已檢驗，可用冪律**：對數常態硬套單一冪律的
殘差 RMS 為 4.9–8.6%，而 641 顆星分 5/10/20 箱的 Poisson 雜訊是
8.8%/12.5%/17.7% —— 殘差小於雜訊，這批資料分辨不出兩種形式。
論文須聲明這是近似及其判準；**樣本大幅增加時要重算**（雜訊隨 1/√N 降、
殘差不變）。

主結論候選：**在這批分析選擇裡，最被討論的雙星處理反而是影響最小的一項。**
這對普適性研究的含意是：跨星團比較 alpha 之前，必須先統一的是選擇函數、
等時線與質量範圍定義，而不是雙星。

引用義務：Li+2020、2603.15779、2604.20722、Cordoni 2023、HR23、
Salpeter 1955、Kroupa 2001/2002、Pera+2021（pyUPMASK）、A&A 2014 aa23988-14。

**2026-08-10 新增（五種傳統雙星修正變體比較，見 PLAN_傳統法雙星修正比較.md）**：
- Lindegren et al. (2021), Gaia Early Data Release 3: Astrometric solutions,
  A&A 649, A2 —— RUWE 作為天測雙星／品質指標的標準文獻
- Gaia Collaboration, Arenou et al. (2023), Gaia DR3: Stellar multiplicity,
  a teaser for the hidden treasure, A&A 674, A34 —— Gaia non_single_star
  目錄的處理鏈
- Maíz Apellániz, J. (2008), Biases on initial mass function determinations.
  II. Real multiple systems and chance superpositions, ApJ 677, 1278,
  https://doi.org/10.1086/529041 —— 族群層級解析修正法的經典先例
- Li, L., Shao, Z., Li, Z.-Z., Yu, J., Zhong, J., & Chen, L. (2020),
  Modeling Unresolved Binaries of Open Clusters in the Color–Magnitude
  Diagram. I. Method and Application of NGC 3532, ApJ 901, 49,
  https://doi.org/10.3847/1538-4357/abaef3 —— 前向模型（統計混合模型）
  處理雙星的直接先例，質量範圍 0.5–1.5 M☉、PARSEC 等時線、emcee MCMC
- Rosen, A. L. (2026), Confidently Wrong: Why Ignoring Binaries Biases
  IMF Inference at Large Sample Sizes, arXiv:2603.15779（先前只有 arXiv
  編號沒有作者，這裡補上）
- **訂正**：`教授會談摘要.md` 把 arXiv:2604.20722 歸給「Carraro 等」，
  正確第一作者是 Mikhnevich（Mikhnevich, Plotnikova, Carraro & Seleznev
  2026），`教學_傳統法誤差核算.md` 原本就寫對了

---

## 章節 → 圖表 → 計算對照

### 1. Introduction
普適性爭議 → 方法學變異未量化 → 兩個缺口（修正版敘述）。無計算。

### 2. Data & Membership
- **表 1**：樣本與篩選（6,956 → 1,297 → 1,078）【已有】
- **圖 1**：六格驗證圖（plots.py）【已有】
- 對 HR23：recall 1.000、分歧 20 顆【已有】

### 3. Methods
- 3.1 傳統法（單星質量指派 + MLE 冪律）【已有，缺誤差核算 → P1】
- 3.2 前向模型（七參數、選擇函數、差異消光對數常態）【已有】——
  **模型 A vs 模型 C 的精確定義**（2026-08-09，取自 `fit_real.py` 的
  `CONFIGS` 字典，論文寫法必須逐字對齊這裡，不能只用「舊/修正」帶過）：

  | | 模型 A（舊） | 模型 C（修正） |
  |---|---|---|
  | 自由參數數 | 6（logage, A_V, f_bin, alpha, MH, q_gamma） | 7（+ dav） |
  | 測光品質選擇函數 | 無 —— 合成星團只套用 G 星等上下限，不模擬 1,297→1,078 那組品質篩選 | 有（`pipeline/selection.py`，訊噪比對星等的非參數迴歸 + 顏色線性項，係數由資料迴歸得出、**不進擬合**，因此不多一個自由度） |
  | 消光 | 全星團單一 A_V | A_V（族群平均）+ dav（星對星散布，對數常態參數化，見 3.2 節） |
  | dav 的角色 | 不存在 | 自由參數但**不可辨識**（注入回收證實會貼著先驗上界跑），論文只能報「必須放進模型以避免 alpha 偏差，但其自身數值不可解讀」，是 nuisance 參數 |

  兩者用完全相同的觀測資料（1,078 顆）、完全相同的搜尋演算法
  （`injection_recovery.multi_stage_best`）、完全相同的統計誤差核算
  （注入回收），**唯一的差異是上表這兩項**。這個嚴格對照設計是表 4
  （穩健性）成立的前提：若 A/C 之間還有其他沒控制的差異，等時線敏感度的
  下降就不能乾淨地歸因於這兩項修正。
- 3.3 誤差核算方法（注入回收、切半、換等時線）【已有】
- **圖 2**：CMD + 最佳合成星團疊圖【待畫，P5】
- **圖 3**：選擇函數（存活率 vs 星等×顏色，模型 vs 觀測）【資料已有，待畫 P5】

### 4. Results
- **表 2（缺口二）**：四法雙星判定 + 兩兩重疊矩陣【已有】
- **表 3（缺口一，主表）**：α 傳統 vs 前向，同一套誤差核算
  【前向目前最佳為 `p2_final2` 的 2.387±0.060，但**卡在 P6b + P10**
  未定案（見下方審查表）；**傳統法五變體比較已完成（2026-08-10）**，
  見下表 → P1/P7 完成】

  **表 3：五種傳統雙星修正變體 + 前向模型，質量範圍統一 0.50–2.50 M☉，
  isochrone 統一用 config C 最終確認值（logage=8.033、A_V=0.383）**
  （來自 `traditional_accounting.py`，20 次注入回收 + 1000 次 bootstrap，
  `results/traditional_accounting_v2.npz`）：

  | 變體 | 真實資料 α | 主誤差 | 主誤差核算 | bootstrap 誤差（統一尺，見下） | 注入回收偏差（f_bin=0.45） | 方法論類別 | 用來排優劣的依據 |
  |---|---|---|---|---|---|---|---|
  | 全當單星 | 2.395 | ±0.118 | 注入回收 | ±0.107 | −0.013 | 忽略（基準線） | 注入回收偏差（有真值可比） |
  | CMD 剔除 | 2.371 | ±0.121 | 注入回收 | ±0.108 | **+0.120（過矯正）** | 逐星剔除（Cordoni+2023／Mikhnevich+2026） | 注入回收偏差——**目前唯一被證實系統性表現差的變體** |
  | RUWE 剔除 | 2.395 | ±0.119 | bootstrap | ±0.119（就是主誤差） | 結構上測不到 | 逐星剔除（Lindegren+2021） | 只有真實資料中心值可比，不能排偏差優劣 |
  | GaiaNSS 剔除 | 2.423 | ±0.112 | bootstrap | ±0.112（就是主誤差） | 結構上測不到 | 逐星剔除（Gaia NSS 目錄） | 只有真實資料中心值可比，不能排偏差優劣 |
  | 文獻解析修正 | 2.411 | ±0.118 | 注入回收 | ＝全當單星的 ±0.107（加常數不變異） | ⚠ **不採計**（見下） | 族群層級解析修正（Rosen 2026） | **降級為案例討論，不進排序** |
  | 前向模型（`p2_final2`，待 P10 定案） | 2.387 | ±0.060 | 注入回收 | 未做（單次擬合成本高，見下） | +0.017 | 統計混合模型（Li+2020 同哲學） | 注入回收偏差（有真值可比，且是原生機制非外借公式） |

  **bootstrap 這欄是刻意加來回答「能不能統一誤差尺」這個問題的**：
  答案是不能直接統一，但可以**補一個所有變體都算得出來的共同量尺**——
  對真實 1,078 顆星 with-replacement 重抽、重跑同一套流程。文獻解析修正
  的 bootstrap 標準差**數學上恆等於全當單星的 bootstrap 標準差**（因為
  修正量是固定常數，加常數不改變重抽的變異量），不需要另外重跑。
  前向模型的 bootstrap 沒有做——`JointModel` 單次擬合要 500–3000 秒，
  1000 次 bootstrap 不現實，即使砍到 50–100 次也要數小時，目前判斷
  不值得為了這張表去跑，留白比硬跑一個雜訊很大的數字誠實。

  **不建議把全當單星／CMD剔除／文獻解析修正也「降級」成只看 bootstrap**：
  bootstrap 只能回答「換一批重抽樣本結果穩不穩」，注入回收才能回答
  「這個方法本身有沒有系統性偏差」——後者資訊量更大，為了表格好看而
  丟掉偏差資訊是本末倒置。正確做法是像現在這樣兩者並陳，各自標明
  回答的是哪個問題。

  ### 文獻解析修正法：降級為案例討論，不採計進排序（2026-08-11）

  上一輪查證出這個變體的「幾乎無偏（+0.003）」站不住腳：合成雙星生成
  邏輯跟 Rosen (2026) 修正量是同一套光度加總假設，這個測試量到的是
  套用邏輯自洽而非數字準確；且 Rosen 的偏差區間只驗證到 alpha=2.30，
  M45 現在的 alpha（2.39–2.46）已經超出範圍，套用區間中點是外推。
  **這個變體從此不計入「哪種方法比較準」的排序**，保留在表 3 裡純粹
  當一個案例：示範族群層級解析修正這個哲學存在、示範盲目套用文獻
  係數而不先確認資料落在其驗證範圍內會有什麼風險。真實資料中心值
  （2.411）仍然落在其他方法的範圍內，這件事本身沒有錯，只是不能拿來
  佐證「這個方法比較準」。完整討論見 `LIMITATIONS.md`。

  ### 現在怎麼比較六個方法的優劣（2026-08-11 訂定判準）

  沒有單一數字可以幫全部六個排名，因為手上的證據回答的是**兩個不同的
  問題**，不能合併成一個排序：

  1. **有沒有系統性偏差**——只有全當單星、CMD剔除、前向模型三個有
     真值可比（注入回收）。**CMD 剔除是目前唯一被實證系統性表現差的
     變體**（f_bin=0.45 時過矯正 +0.120，遠超統計誤差）；全當單星跟
     前向模型的偏差都小（−0.013／+0.017），前向模型的偏差來自它原生
     的擬合機制，不是外借的文獻公式，可信度更高。RUWE／NSS／文獻解析
     修正都沒有這個層級的證據，**不能說它們比較差，只能說沒有證據
     判斷它們準不準**。
  2. **真實資料答案穩不穩、彼此一不一致**——六個方法的中心值全部落在
     2.37–2.42（前向模型 2.387 也在內），彼此差距遠小於統計誤差。
     這個軸上沒有誰特別突出，結論是「在 M45 這個資料集上，方法選擇
     本身不是主要誤差來源」。

  **綜合判讀**：目前唯一能明確排序的是「CMD 剔除比較差」；其餘五個
  在真實資料上表現一致，優劣排序資訊不足，不宜在論文裡宣稱誰比較準，
  只能誠實列出各自的證據等級（見上表最後一欄）。

  **三個值得寫進論文的發現**：
  1. **五種傳統變體的真實資料 α 全部落在 2.37–2.42**，彼此差距（≤0.05）
     遠小於各自的統計誤差（~0.12）——**雙星處理方法的選擇，在對齊質量範圍
     與正確 isochrone 之後，對 M45 的 alpha 幾乎不影響**，這是本專案
     用五種業界常見做法系統性交叉比較才能得出的結論，先前兩變體版本看不到。
  2. **CMD 剔除是唯一被證實系統性過矯正的變體**（f_bin=0.45 時 +0.120），
     文獻解析修正原本以為的「幾乎無偏」已經降級為不採計的案例討論
     （見上方獨立段落），不再拿來跟 CMD 剔除比較誰準。
  3. **傳統法（2.37–2.42）與前向模型（2.387）高度吻合**——這是兩套
     完全獨立的方法論互相印證，跟表 4 的「換等時線」那種同架構換零件
     不同類別，這個穩健性沒有被 P9 的發現動搖（見上方 P9 討論）。
- **表 4（穩健性，主賣點）**：α 對等時線（PARSEC/MIST × 舊/修正模型）
  【2026-08-09 更新：P3 完成，濾光片效應與模型效應已拆解乾淨】

  | 等時線 | 模型 A | 模型 C |
  |---|---|---|
  | PARSEC-EDR3 | 2.380 ± 0.098 | 2.420 ± 0.098 |
  | PARSEC-DR2 | 2.340 ± 0.150 | 2.380 ± 0.098 |
  | MIST-DR2 | 2.100 ± 0.126 | 2.380 ± 0.160 |

  三次擬合用的是**同一批 1,078 顆觀測星、同一套搜尋程式**，只換 isochrone
  網格檔（PARSEC-EDR3 / PARSEC-DR2 / MIST-DR2），乾淨地拆成兩個獨立效應：

  - **濾光片效應**（PARSEC-EDR3 vs PARSEC-DR2，恆星演化模型相同、只換
    Gaia 濾光片定義）：模型 A 差 0.040、模型 C 差 0.040。**兩種模型設定下
    都小到可忽略**，遠低於統計誤差 0.10–0.15。
  - **恆星演化模型效應**（PARSEC-DR2 vs MIST-DR2，濾光片相同、只換模型）：
    模型 A 差 **0.240**；模型 C 差 **0.000**。

  **這個拆解比先前單看「PARSEC-EDR3 vs MIST-DR2 差 0.28」更嚴謹，也更強化
  結論**，理由：0.28 這個原始數字混著濾光片差與模型差兩個來源，若只看
  「修正後差變小」，不能排除是兩個效應剛好互相抵消的巧合（例如濾光片效應
  在修正後變大、模型效應變小，兩者加總看起來變小但個別成因沒解決）。
  拆解後才能確認：濾光片效應本來就一直很小（修正前後都是 0.04，代表它
  不是修正在處理的東西），而修正精準地把 0.24 的模型效應歸零——
  **穩健性的來源是實打實的模型差異被消除，不是巧合抵消。**

  **⚠ 這個主賣點的替代解釋已經被證實（2026-08-09 提出，2026-08-10 驗證）**：
  真實資料上 MH 跑到 +0.18，而 Gaia 光譜給 −0.02～−0.06，差 0.2 dex。
  這代表模型正用一個錯的金屬量去補償某個沒建模的效應。若那效應是
  **等時線在低質量前主序段的共同偏差**（PARSEC 與 MIST 用相似的對流與
  大氣處理，可能一起錯），那麼兩者在修正後給出相同 alpha，
  可能是**兩邊的 MH 都吸收了同樣的偏差**，而非「修正消除了模型差異」。
  **兩套模型一致不等於兩套都對。**

  **判別檢驗（把 MH 固定在兩套網格共有的格點 0.00，重跑 config C 的
  PARSEC 與 MIST 版）——已完成，答案是「分歧」，表 4 必須改寫**：

  | | logage（推算年齡） | f_bin | alpha |
  |---|---|---|---|
  | PARSEC（`P9a-redo`，10 次） | 8.0–8.1（100–126 Myr，**符合 M45 公認年齡**） | 0.35–0.75 | 2.440 ± 0.180 |
  | MIST（`P9c`，10 次） | 7.7–7.8（**50–63 Myr，明顯過輕**） | 0.65–0.85 | 2.180 ± 0.098 |

  兩者的 alpha 差 0.26，遠超個別散布；更關鍵的是**鎖住 MH 後 MIST 找不到
  合理的年齡**——50–63 Myr 只有 M45 公認年齡（100–135 Myr）的一半，
  這不是「兩套模型在同一個物理解上小幅分歧」，是 MIST 這套等時線在
  低質量前主序段對 M45 這批資料的描述本身有問題，鎖住 MH 之後
  沒有別的自由度可以掩蓋，只好跑到不合理的年齡＋偏高的 f_bin 去湊
  Hess 圖。**先前「修正模型後 PARSEC 與 MIST 給出一致的 alpha（差
  0.000）」不是模型真的一致，是 MH 這個自由參數在兩邊各自吸收了
  不同的系統偏差、湊出同一個 alpha 表面值。**

  **表 4 現在該怎麼寫**：不能再宣稱「修正後的模型對等時線選擇穩健」，
  要改成「在 MH 為自由參數的條件下，修正後的模型對等時線選擇的
  alpha 讀數穩健，但這個穩健性經判別檢驗證實是 MH 吸收系統偏差換來的
  表面現象，不是模型本身真的不敏感」——**這件事本身變成論文一個
  重要且誠實的發現**：它示範了「兩套獨立模型給出一致答案」不能單獨
  當作穩健性的證據，除非先排除自由參數吸收共同偏差的可能，這正是
  本專案在方法學上真正想貢獻的東西之一。完整過程見上方「全專案結果
  審查表」的 P9a-redo／P9c 兩列，數字來自 `results/fit_real_fixmh_parsec_redo.npz`
  與 `results/fit_real_fixmh_mist_redo.npz`。
- **圖 4**：質量分層 α(r)【已有】
- **表 5**：誤差預算總表（統計 0.10–0.16、等時線 ≤0.04–0.28、
  dav 敏感度 0.07–0.20、選擇函數 ±0.18）【彙整即可】

### 5. Discussion
PDMF vs IMF、MH 異常（P4 若解得了）、對文獻星團間變異的含意、
通往普適性檢驗的路。以 LIMITATIONS.md 為底稿。

### 6. Limitations
直接取自 LIMITATIONS.md。

---

## 全專案結果審查表（2026-08-10，使用者質問後全面重查）

**起因**：發現 P9 的 MIST logage 撞牆是網格覆蓋不足造成的簡併假象後，
使用者質問「先驗跟網格沒對上的問題，難道只可能影響到這兩批數據嗎？」
—— 原本只抽查了 `p2_final2` 與 `p9a2` 兩批，範圍太窄。這裡對全部跑過的
計算逐一核對四個已知問題，不是只查有出事的那幾個。

**四個已知問題（縮寫代號）**：
- **F**：`COARSE` 的 f_bin 網格上界曾是 0.75（`np.arange` 步長把上限吃掉），
  比先驗 0.8／物理上限 1.0 窄。2026-08-09 修好，放寬到 0.95。
- **A**：`COARSE` 的 alpha 網格曾是 1.70–3.00，比先驗 1.5–3.2 窄。
  2026-08-10 修好，放寬到 1.50–3.20。
- **G**：搜尋軸範圍超出**實際下載的 isochrone 網格涵蓋範圍**（不是先驗，是
  資料本身），造成簡併平坦區、argmax 報出邊界假象。MIST 網格（7.80–8.50）
  與 PARSEC 系列網格（實際只到 8.25，儘管檔名寫 8.3/8.4）都有這個問題。
  2026-08-10 發現，**只能靠重下更寬的網格解決，不能靠放寬搜尋軸**。
- **P**：`fit_real.py` 等七支腳本寫死 `mh_prior_sigma=0.0`，從未使用
  config.toml 記載的高斯先驗（中心 −0.03）。2026-08-10 發現，**尚未修**。

**第二輪審查新增的第五個代號（2026-08-10，使用者追問「安全數據會不會
也引用了錯誤數據」之後找到的）**：
- **M**：G 問題原本只查了 logage 軸，這次把同一套檢查套用到 MH 軸才發現
  PARSEC 系列網格（`parsec_v2.0_gaia*_logt7.7-8.3..._mh-0.6-0.6...dat`）
  檔名寫「MH −0.6 到 +0.6」，但實測 `np.unique(grid['MH'])` 只到
  **+0.33631**，上界比檔名窄了近 0.27 dex；`COARSE` 的 MH 搜尋軸
  `np.arange(-0.40, 0.401, 0.15)` 又比 config 宣告的先驗上下限
  （−0.60/+0.60）窄。三層寫死的範圍互不一致（先驗 0.60 ＞ 搜尋軸
  0.40 ＞ 網格實際 0.336），是跟 F/A/G 同一種「寫死常數彼此沒對齊」的
  失誤，只是這次剛好沒撞到——見下方逐項查核。MIST 網格的 MH 涵蓋
  −0.5 到 +0.5，與檔名一致，**沒有這個問題**。

**根本修法（已當場實作，不是留待之後）**：問題的共通點是
`pipeline/joint_fit.py` 的 `COARSE`／先驗邊界完全用寫死常數表示，
從不檢查實際載入的 isochrone 網格到底涵蓋到哪裡——這正是使用者提出的
新規則「先查底層資料涵蓋到哪裡，再決定要不要放寬搜尋軸」該長期落地的
地方，不能每次都靠人工事後稽核才發現。已在 `JointModel.__init__`
加上一段檢查：只要搜尋軸的 logage 或 MH 邊界超出當下載入網格的實際
`np.unique()` 範圍，啟動時就直接印出警告（含請求邊界與實際邊界的
數字），不必再靠人工翻 log 或 npz 才能發現簡併假象。這不會改變任何
既有結果（純粹是新增的載入期檢查），下一次任何腳本重跑都會自動繼承。

**驗證方式的誠實聲明**：以下「已直接驗證」的項目，是實際讀取該次執行的
log 或 `.npz` 逐一核對參數數值算出來的，不是推測。「同框架推定安全」
是指同一套程式與網格、且已驗證過代表性樣本安全，但沒有把該類別下每一次
重跑都個別核對——這是誠實的信心分級，不是全部都逐檔驗證過。

| 分類／代表性工作 | 代表性 log | 方法論 | F | A | G | M | P | 判定 |
|---|---|---|---|---|---|---|---|---|
| 成員判定 pyUPMASK 變因（freq_s42/43/99、deproj、nopca、ol100、seed43/44/99、indep、g20） | 各檔 | pyUPMASK，非 isochrone 擬合 | N/A | N/A | N/A | N/A | N/A | **結構上不適用**，跟本次五個問題完全無關的方法論 |
| 舊版循序擬合驅動程式 step3/4/5（`run_pipeline.py` 時代的 log：step3/4/5/step45/full345*） | step3/4/5/step45/full345* | 2D/3D/1D 分段網格搜尋，MH 固定 | N/A | N/A | 未查（更早期、範圍更窄的網格） | N/A | N/A（MH本來就固定，非自由參數） | **這批 log 本身已被後續 joint_fit 框架取代，未在目前任何結論中被引用**——但見下一行，「模組」跟「log」要分開看 |
| ⚠ 修正：`pipeline/step2_cmd.py`／`step3_age.py`／`step5_imf.py` 這幾支**模組**（不是上面那批 log） | trad_acc2.log | `traditional_accounting.py`（目前引用的 P1）直接 import 了 `step5_imf.assign_masses/mle_powerlaw` 與 `step3_age` 的常數 | N/A | N/A | N/A（見右欄） | N/A | N/A（生成假資料不受先驗影響） | **第二輪審查發現前一版判定的理由寫錯**：`step5_imf.py` 並沒有被取代，P1 現在還在用它。查證後結論不變（P1 安全）是因為 `traditional_accounting.py` 呼叫 `isomod.isochrone_at()` 時傳的是**寫死的已知安全值**（8.00/8.10），從未呼叫 `step3_age.fit()` 那個會做網格搜尋、可能撞上 G 簡併區的函式——是「用法安全」不是「模組跟舊結果無關」，兩者結論一樣但原因不同，必須訂正 |
| 四／六參數 MCMC（`run_joint.py`：joint*、joint6p*、joint_gauss、widegrid、finegrid、profile_mh/qgamma、prior_sens） | joint_gauss.log、joint6p_arm.log | emcee **連續取樣**，不經過 `COARSE` 離散網格，**正確使用高斯先驗**（P 問題不適用於這個家族） | **N/A（emcee 連續取樣沒有網格解析度，F/A 問題的成因不存在）** | 同左 N/A | **已驗證安全**（logage 8.184，網格實際涵蓋 7.6–8.35，安全落在內部） | **已直接驗證安全**（讀 log，MH 0.181/0.194，遠低於 0.336） | **N/A（這個家族本來就對）** | **已驗證安全**（本次審查修正了先前自動掃描的假警報：抓到的 0.78/0.80 其實是相關矩陣的相關係數，不是 f_bin 本身；且共用同一個 `JointModel`，新加的載入期網格範圍警告以後會自動涵蓋這個家族） |
| 概似診斷（overconf、overconf_fine、binscale、verify_bal/detached/s4_par/split4） | overconf_fine.log | 均勻先驗（診斷用途正確） | **已直接驗證安全**（讀 log，f_bin 0.46–0.60） | 未逐一查 | **已直接驗證安全**（讀 log，logAge 8.00–8.24） | **已直接驗證安全**（讀 log，MH 0.00–0.25，遠低於 0.336） | **N/A（診斷用途，關閉先驗是對的）** | 第二輪審查把 overconf_fine.log 十次切分的實際數值都讀出來核對過，不再只是「同框架推定」——確認安全 |
| 注入回收核心情境（S1/S2/S3/S3F/S4，含 inject_s123/s3f/s4/s4_lognorm/bignsyn） | inject_s3f.log 起，**第二輪已加驗 s4/s123/bignsyn/s4_lognorm 四個** | 均勻先驗（診斷正確） | **已驗證安全**（f_bin 0.417–0.483，四個新查的都在 0.417–0.483 內） | **已驗證安全**（alpha 2.167–2.500） | **已驗證安全**（logage 8.133–8.200，四個新查的都落在此區間） | **已驗證安全**（MH 0.000–0.050） | N/A | **已驗證安全，且驗證範圍比第一輪更廣**——σ_α=0.144 這個全專案最常引用的統計誤差數字經得起查，不是只查了 S3F 一個代表就外推 |
| dav 掃描（item4_davsweep、dav_sweep） | dav_sweep.log | 均勻先驗（診斷正確） | **已直接驗證安全**（讀 log，f_bin 固定於注入值 0.450） | 未逐一查 | **已直接驗證安全**（讀 log，logAge 8.15–8.20） | 未逐一查 | N/A | 這批結果本來就已被 LIMITATIONS.md 標記「品質不足，只能說量級」，本次審查不改變那個既有的保留態度，但 F/G 至少確認沒有額外貼牆 |
| P1 傳統法核算（trad_acc、trad_acc2） | trad_acc2.log | 直接質量指派+MLE，**不經過 COARSE 搜尋** | N/A | N/A | N/A | N/A | N/A（生成假資料不受先驗影響，MH 是固定注入值不是自由參數） | **結構上不適用**，缺口一／二的核心結論（雙星效應 0.02、CMD 剔除救不回缺口）不受這五個問題影響 |
| P2 最終數字（`p2_final` 失敗、**`p2_final2`**） | p2_final2.log | JointModel 7 參數搜尋 | **已驗證安全**（f_bin 0.52–0.62） | **已驗證安全**（alpha 2.30–2.50） | **已驗證安全**（logage 8.033，PARSEC 內部） | **已驗證安全**（MH 十次全部 0.000，遠低於 0.336 邊界） | **確認受影響**：均勻先驗，非文件記載的高斯先驗 | **α 本身的搜尋沒有被 F/A/G/M 污染，但先驗問題（P）未解決前不能當最終數字**（P10 待辦） |
| P3 等時線比對（item3a/b/c_mistgrid/mistfit、p3a/a2_dr2grid、**p3b_dr2fit**） | item3b/c_mistfit.log、p3b_dr2fit.log | JointModel 6/7 參數搜尋 | **已驗證安全**（f_bin 0.45–0.65） | **已驗證安全**（alpha 1.9–2.5） | **已驗證安全**（MIST 版 logage 8.1–8.3，安全落在 MIST 真實涵蓋 7.80–8.50 內部，不靠近 7.80 邊緣；DR2/PARSEC 版 logage 8.0–8.1，同樣安全） | **已驗證安全**（MIST 版 MH 0.200，在 MIST 網格 ±0.5 內部；DR2/PARSEC 版 MH 0.050/−0.100，遠低於 0.336） | 均勻先驗（同 P2） | **表 4 的核心證據（濾光片 0.04、模型效應 0.24→0.00）經查證安全**，不是簡併假象；仍受 P 問題影響但影響方向預期很小（見 P10 說明） |
| P6 低質量段冪次（**p6_lowmass**） | p6_lowmass.log / `results/profile_lowmass.npz` | JointModel 7 參數搜尋，五個冪次值 | **已直接驗證安全**（讀 npz，f_bin 最高 0.65） | **已直接驗證安全**（讀 npz，alpha 最高 2.7） | **已驗證安全**（logage 全部 8.0–8.1） | **第二輪新查，已直接驗證安全**（讀 npz，MH 全部落在 −0.10 到 +0.05） | 均勻先驗 | **0.248 系統誤差這個全專案第二重要的數字經得起查**。dav/q_gamma 貼牆是已知且被允許的 nuisance 行為，不是新問題 |
| P6b 可辨識性（p6b/p6b2/p6b3 已無法驗證，**p6b4_inject_lowmass 已完成**） | p6b/p6b2/p6b3 的 `.npz` 已被覆寫、無法回溯 ；**`results/inject_lowmass.npz`（p6b4）已直接讀取核對** | JointModel 8 參數（+p_lowmass），注入回收 | **已驗證安全**（8 筆成功試驗 f_bin 全部 0.45–0.55） | 未查 | **確認 1/8 筆貼近／超出邊界**：7 筆 logage=8.2（安全），1 筆（p_true=1.3 第 3 次）logage=8.3——PARSEC 網格實測只到 8.25，這筆很可能落入同一種簡併假象，見下方判讀 | **已驗證安全**（8 筆全部 MH=0.05） | 均勻先驗 | **p_true=1.3 那組（logage 8.3 的那一筆）標記為可疑，其餘 7 筆安全**。核心結論（可辨識性 ratio 0.92，alpha_bias −0.05〜+0.017）主要由未受污染的另外 7 筆撐起，方向不受動搖，但這組數字本身該視為初步結果，正式寫進論文前建議至少再補測 p_true=1.3 一次 |
| **P9a 系列（PARSEC 鎖 MH）** | **p9a_fixmh_parsec.log（失敗）、p9a2_fixmh_parsec.log** | JointModel 7 參數，MH 固定 0.00 | **確認受影響**：5 次裡 2 次 f_bin 精確落在舊上界 0.750 | **已驗證安全**（alpha 2.1–2.7） | **已驗證安全**（logage 8.0–8.1） | N/A（MH 已固定 0.00） | N/A（MH 已固定） | **α=2.460±0.233 這個數字不可信**——散布可能有一部分是撞牆造成的假散布，不是「鎖住 MH 後單純變不穩定」。**必須用已修好的 f_bin 範圍（0.15–0.95）重跑** |
| **P9b 系列（MIST 鎖 MH）** | p9b/p9b2/p9b3_fixmh_mist.log（全部失敗） | JointModel 7 參數，MH 固定 0.00 | N/A（在 logage 簡併之前就沒跑到 f_bin） | N/A | **確認受影響，結論全部作廢**：MIST 網格只到 7.80，搜尋軸放寬到 7.70/7.30 全部落入簡併平坦區 | N/A（MH 已固定 0.00） | N/A | **全部作廢**，需要 P9c（重下更寬的 MIST 網格）才能重新回答 |
| 本機佇列基礎設施（run_queue.py、貼牆偵測機制本身） | 各 queue_runner*.log | 不是科學結果，是工具 | — | — | — | — | — | 貼牆偵測與容錯機制本身經過本次審查證實有效（正是它們讓 F/A/G 問題被發現，而不是讓錯誤結果靜默通過） |
| Kaggle 延伸工作（profile-lowmass 擴大掃描、p9a_more_reps、p6_lowmass_ext_retry、**p9a_redo、p9c_mist_redo**） | `logs/kaggle_queue_done.txt` | 同 P6/P9a，但用今天已修好的網格範圍 | 已用修好後的範圍 | 已用修好後的範圍 | N/A（PARSEC，logage 沒被要求超出範圍） | N/A | 均勻先驗（同 P2，待 P10） | **全部以 `error` 結束，沒有一批真正跑出數字**：先前兩批（`p9a_more_reps`、`p6_lowmass_ext_retry`）失敗後，2026-08-10 又修了兩個真環境問題（isochrones 白名單漏了新網格檔、kaggle CLI 只裝在 ARM64 Python、`subprocess.run` 找執行檔用的是呼叫端自己的 PATH 不是傳給子行程的 env）重新派工 `p9a_redo`／`p9c_mist_redo`，這次 dataset/kernel push 都成功了，但兩者仍在 4 次重試（60/120/240/480s，累計約 16 分鐘）後以同一種掛載時序 `FileNotFoundError` 收場。**已改判：不再繼續在 Kaggle 上重試這個掛載時序問題，改在本機跑**（p6b4 剛好跑完釋出 8 核，`p9a2` 先例顯示本機 5 次重複只要 1427s，比繼續跟 Kaggle 奮戰快且可靠）——`p9a_redo_local`／`p9c_mist_redo_local` 已在本機佇列啟動。Kaggle 多帳號基礎設施本身（推送、白名單、PATH）已驗證可用，問題卡在 Kaggle 平台端的 dataset 掛載延遲，不是我方程式問題 |

**這次（含第二輪）沒有把每一個 log 逐行核對**（約 90 個檔案），而是：
（1）直接驗證了全專案最常被引用的關鍵數字（σ_α=0.144、0.248 系統誤差、
表 4 的 0.04/0.24/0.00），第二輪並把 σ_α=0.144 的驗證範圍從 1 個代表樣本
擴大到 5 個注入回收情境；（2）對確認出事的項目（p9a2、p9b 系列）查清楚
污染的確切位置與原因；（3）對結構上不受影響的家族（pyUPMASK、傳統法、
MCMC 用高斯先驗）給出方法論層級的理由而非逐檔驗證，第二輪並訂正了
P1「傳統法模組跟舊結果無關」這個寫錯的理由；（4）**明確回答了使用者
「安全數據會不會引用了錯誤數據」的問題**：逐一檢查了誤差預算表（表 5
六項）與表 4 的來源計算，確認它們全部直接讀自各自的 log/npz，沒有任何
一項把 p9a2 或 p9b 系列的數字當輸入去做二次計算或平均；表 4 在
PAPER_OUTLINE 正文的判別檢驗段落原本寫得像「還沒跑」，第二輪已訂正為
「跑了兩次、兩次都因 F/G 問題作廢」，避免讀者誤以為那是待辦而非已知失敗。
**尚未逐一查證的項目已在表中明確標記「未查」，不是暗示它們沒問題**——
第二輪把 MCMC 家族的 A/M 兩欄查清楚後改判為結構性 N/A（emcee 連續取樣
沒有網格解析度問題），目前表中真正還沒查完的只剩兩處，重要性都低：
dav 掃描的 A/M 兩欄（本來就已被降級為「品質不足」，不影響任何現有結論）、
舊版 `step3/4/5` log 本身的 G 欄（log 本身已不再被任何結論引用，
但這批 log 背後的 `pipeline/step*.py` 模組被 P1 現役程式重用一事已經
查清楚並訂正見上方「四」）。

---

## 待跑清單（依優先序，2026-08-09 更新）

| # | 內容 | 程式 | 估時 | 狀態 |
|---|---|---|---|---|
| P1 | 傳統法的注入回收（偏差＋散布；含「剔除偵測到的雙星」變體） | traditional_accounting.py | — | **完成**（08-07，2026-08-10 擴充成五變體）。發現真正的驅動因子是質量範圍定義不對齊，不是雙星本身（→ 催生 P7）。五變體的真實資料 α 全部落在 2.37–2.42，見表 3 |
| **P6** | **低質量段冪次輪廓測試**（0.9–1.7，六成樣本落在這段卻從未擬合） | profile_lowmass.py | — | **完成**（08-08）。跨度 0.400 = 統計誤差 2.8 倍，乾淨訊號無貼牆。**判定：必須處理，見 P6b**。**⚠ 2026-08-11：`--refines 3` 是單值，完全沒精修，`p6_lowmass_v2`（`--refines 3,3`）已排進佇列，優先度最高，見 LIMITATIONS.md** |
| **P6b** | 低質量段冪次升格為第八個自由參數 + 注入回收驗證可辨識性。**已改判走升格而非文獻先驗**：先驗寬度 ±0.5 本身就造成 0.248 誤差，鎖住無法消除 | inject_lowmass.py | ~3 hr | **完成，可辨識（2026-08-10 04:22，p6b4 跑了 302 分鐘）**：p_recovered 對 p_true 的跟隨比值 0.92（近 1 代表可辨識），alpha_bias 只有 −0.05〜+0.017，8/9 次試驗成功（1 次撞 A_V 角落解，正確地只丟該次試驗、沒拖累其他）。F/G/M 查核：1 筆（p_true=1.3）logage 落在 8.3，超出 PARSEC 真實網格 8.25，標記可疑；其餘 7 筆安全。**下一步**：把低質量段冪次正式升格為 P2 最終擬合的自由參數，但正式數字前建議先補測 p_true=1.3 一次排除那一筆的疑慮。**⚠ 2026-08-11：`multi_stage_best()` 精修 bug 確認影響這批結果（沒帶 `--refines` 雙值，等於完全沒精修），0.92 這個精確值待重跑確認，見 LIMITATIONS.md** |
| **P9** | **MH 鎖定檢驗**：把 MH 固定在兩套網格共有的格點 0.00，重跑 config C 的 PARSEC 與 MIST 版 | fit_real.py --fix-mh | — | **完成，結論明確：表 4 的穩健性主賣點不成立，必須改寫。** PARSEC（α=2.440±0.180）與 MIST（α=2.180±0.098）鎖住 MH 後**不再一致**，差 0.26，遠大於個別的統計散布；更關鍵的是**兩套等時線鎖住 MH 後給出的年齡直接矛盾**——PARSEC 落在 logage 8.0–8.1（100–126 Myr，符合 M45 公認年齡），MIST 落在 logage 7.7–7.8（**50–63 Myr，明顯不合理**，只有公認年齡的一半）。這正是 LIMITATIONS.md 第四節預先寫好的判別規則「鎖住 MH 後開始分歧 → 先前的一致是靠 MH 自由調整換來的，表 4 必須改寫」的**「分歧」那個分支**：自由 MH 時兩套等時線的 alpha 一致（表 4：修正模型差 0.000），是因為 MH 在兩邊各自吸收了不同的系統偏差去湊出同一個 alpha，不是模型真的一致。完整審查見上方「全專案結果審查表」。**⚠ 2026-08-11：`multi_stage_best()` 精修 bug 確認影響 P9a-redo/P9c（沒帶 `--refines` 雙值），α=2.440/2.180 這兩個精確數字與差距 0.26 待重跑確認——但質性結論（MIST 給出的年齡 50–63 Myr 明顯不合理）大機率不受影響，因為粗網格 logage 間距只有 0.10，精修後不可能挪回 8.0–8.1，詳見 LIMITATIONS.md** |
| **P9a-redo** | 用已修好的 f_bin/alpha 範圍重跑 P9 的 PARSEC 版 | fit_real.py --fix-mh 0.0 --repeats 10 | ~40 min | **完成（08-10 本機，91 分鐘，`results/fit_real_fixmh_parsec_redo.npz`）**：α=2.440±0.180，10 次都直接查過 f_bin（0.35–0.75，全部安全落在新範圍 0.15–0.95 內部，沒有貼牆）／logage（8.0–8.1，安全，對應 100–126 Myr）／MH（固定 0.000，符合設計）。**乾淨結論：鎖住 MH 後散布（0.180）仍比自由 MH 的 p2_final2（0.060）寬 3 倍**——排除了 f_bin 撞牆這個混雜因素後，「鎖住 MH 讓擬合變不穩定」看起來是真訊號，不是先前 p9a2 那種撞牆假象的殘留 |
| **P9c** | 重新下載涵蓋 7.30–8.50 的 MIST 網格，重跑 P9 的 MIST 版 | build_mist_grid.py --logage-lo 7.30 + fit_real.py | ~30 min 下載 + 擬合 | **完成（08-10 本機，93 分鐘，`results/fit_real_fixmh_mist_redo.npz`）**：α=2.180±0.098（10 次：2.1/2.1/2.1/2.3/2.1/2.3/2.1/2.3/2.1/2.3），logage 全部落在 7.70–7.80（**不是網格邊界 7.30**，載入期警告也沒有對 logage 觸發——這次是網格真正涵蓋到位後量出來的內部最佳解，不是簡併假象）。f_bin 0.65–0.85（安全，未貼新上界 0.95）。**這批年齡（50–63 Myr）本身就不合理**（M45 公認 100–135 Myr），代表 MIST 在鎖住 MH 之後找不到自洽解，只能用「不合理的年輕年齡＋高 f_bin」去湊 Hess 圖，這是 MIST 這套等時線模型本身有問題的直接證據，不是網格或先驗設定的問題 |
| **P10** | **MH 先驗一致性修正**：`fit_real.py` 等七支腳本全部寫死 `mh_prior_sigma=0.0`，均勻先驗，從未用過 config.toml 記載的高斯先驗（中心 −0.03，取自 Gaia 光譜）。移除 fit_real.py 那行覆寫、改回讀 config，重跑 config C 比較 alpha 有沒有變 | fit_real.py（移除覆寫） | ~30 min | **程式碼修正已完成（2026-08-12，使用者質問後確認並修好）**：`fit_real.py` 那行 `mh_prior_sigma=0.0` 覆寫已刪除，`JointModel` 現在會讀到 config 的高斯先驗（已實測確認 `cfg.joint_fit.mh_prior_sigma == 0.10`）。其餘六支診斷腳本（`profile_lowmass.py`／`injection_recovery.py`／`measure_overconfidence.py`／`traditional_accounting.py`／`final_imf.py`／`inject_lowmass.py`）**維持均勻先驗，這是對的、不用改**——先驗會污染那些測試想看的東西。**還沒做的是「重跑 config C 比較 alpha 有沒有變」這一步**——`p2_final2` 之前的所有 `fit_real.py` 產出（含目前排隊中的 `p2_final2_v3`、`radial_r1-4`、正在跑的 `p2_free_lowmass`）都要用修好後的程式碼重跑才算數，見下方 P2 與 `WORK_BOARD.md` |
| P3 | 換 MIST 等時線量系統誤差；PARSEC 的 DR2 濾光片版已建好，實測分離濾光片效應與模型效應 | build_dr2_grid.py + fit_real.py | — | **完成**（08-09）。濾光片效應 0.04（可忽略）、模型效應 0.24→0.00（修正後歸零）。見表 4 |
| P2 | 前向最終數字：C 設定 repeats 拉到 10 | fit_real.py --repeats 10 | — | **卡住，等重跑**。p2_final2 已完成（alpha=2.387±0.060）但**不能當最終數字**，同時疊了三個問題：(1) 低質量段冪次仍是未修正的固定值（見 P6b）；(2) 用均勻先驗而非文件記載的高斯先驗，**程式碼已於 2026-08-12 修好**（見 P10）；(3) `multi_stage_best()` 精修 bug 讓 `--refines 3,3` 只等於一階真精修（見 LIMITATIONS.md）。三個問題疊在同一個數字上，**必須用修好後的程式碼一次重跑**，不能只補其中一項——`WORK_BOARD.md` 待認領清單裡的 `p2_final2_v3` 已經是修好 bug①③的版本，2026-08-12 起也自動帶到 P10 的修正（同一支程式）。logage/A_V/f_bin 已驗證安全（8.033、0.33-0.43、0.52-0.62，都在合理範圍內部，未受網格簡併問題影響） |
| P4 | MH 異常追查：G<20 樣本重跑 step1-2 + C 擬合看 MH 動不動 | 既有程式串接 | 4 hr | 待確認 |
| P7 | 表 3 用對齊的 0.50–2.50 M☉ 重算傳統法與前向模型；**擴充成五種傳統變體 + 前向模型比較** | traditional_accounting.py | — | **完成（2026-08-10）**。isochrone 也換成當下 config C 最終確認值（8.033/0.383），不再用過時的 step3 舊選擇。見上方表 3 |
| **P8** | **傳統法基準驗證**：核對 `traditional_accounting.py` 的定義（質量範圍、isochrone 來源、MLE 誤差核算）跟主流做法一致，不是自己發明的定義 | 方法論核對，非計算 | — | **完成（2026-08-10，方法論核對版）**：跟 Li+2020（NGC 3532）逐項對照——質量範圍 0.5–1.5 M☉ vs 我們的 0.5–2.5（涵蓋更廣，方向一致）；isochrone 都用 PARSEC（Bressan+2012）；Li+2020 用 MCMC 對 CMD 做 single+binary 混合模型（跟我們的前向模型同哲學），我們額外多了 CMD 偏移／RUWE／NSS／解析修正四種逐星或族群層級的傳統作法，沒有偏離主流定義。**未做**（可選，成本高）：實際下載 NGC 3532 資料重現 Li+2020 的數字 |
| P5 | 論文圖（圖 2、3、4 重畫成投稿品質） | plots | 2 hr | 待確認，等 P2/P7 數字定案後才畫 |
| **P11** | **殘留場星污染比例 outlier_frac 敏感度**：寫死 0.01，從未測過敏感度，`CLAUDE.md` 待辦分類體檢後判定為「現役假設」必須量出代價 | profile_outlierfrac.py | ~30 min | **`verify_outlierfrac` 結果作廢**：12 次執行 alpha 全部精確等於 2.500，root cause 已查出是 `multi_stage_best()` 精修 bug（單值 `--refines` 等於完全沒精修，見 LIMITATIONS.md），不是 outlier_frac 真的沒影響。已修好程式碼，`p11_outlierfrac_v2`（`--refines 3,3`）已排進本機 `queue.txt`，優先度最高 |
| **P12** | **BP/RP 誤差模型 A/B 驗證**：用 G 查 BP/RP 誤差 vs 用星體自己的 BP/RP 星等查，同樣是體檢後判定的現役假設。已加 `--native-bprp-err` 旗標與 `e_bp_native`/`e_rp_native`，A/B 兩次擬合比較 alpha | fit_real.py --native-bprp-err | ~50 min（兩次） | **`verify_bprperr_off`/`on` 也中了同一個精修 bug**（未帶 `--refines` 雙值），這兩批結果的精確 alpha 待重跑確認，優先度低於 P6/P11，暫未重新排入佇列 |

MCMC 後驗（取代網格 argmax）列為**選配**：若 P2 的 10 次重複給出穩定平均，
網格版數字即可用（誤差棒來自注入回收而非概似曲率，本來就不依賴 MCMC 收斂）。

## 時程（含使用者 2026-08-07 同意的延長一個月，共五個月）

8 月：教授會談＋P1–P4；9 月：凍結數字＋全部圖表＋（可選）RNAAS 短文；
10 月：英文初稿；11 月：教授審改；12 月：submit＋科展材料。
