# 協作規範（給所有人類與所有 AI agent）

這份文件給**所有**要碰這個 repo 的人與 agent 讀——不只 Claude。若你是
Claude，先讀 `CLAUDE.md`；這份是通用規則，兩者都要遵守。

**目的**：多個 agent（Claude、Codex、其他）與多個人同時在同一個 repo
上工作，需要讓大家對資料共享保持同步、有條理地找到彼此跑過的結果、
改過的程式、以及對應的版本。

**這份文件跟其他規則類文件（`AGENTS.md`、`README.md`、`WORK_BOARD.md`、
`WORK_BOARD_DONE.md`、`LIMITATIONS.md`、`QUEUE_ALERTS.md`、`STATE.md`、
`docs/reference/*`）只寫現在的規則是什麼，不寫「哪一天、因為什麼事、
改了什麼」這種脈絡性敘事**——找不到某條規則的理由不代表沒有理由，是
理由被搬去了 [CHANGELOG.md](CHANGELOG.md)，依文件分節、依日期記錄。
編輯這些文件時，新加的規則要嘛不需要理由（規則本身夠清楚），要嘛把
理由寫進 `CHANGELOG.md` 對應章節，不要寫進規則文件本身。

---

## 零、開始新工作前——先查 `WORK_BOARD.md`

任何預期會花超過一次對話、或會碰共用檔案（`pipeline/`、
`injection_recovery.py`、`LIMITATIONS.md`、`PAPER_OUTLINE.md`、
`queue.txt`）的工作，開始前先讀 `WORK_BOARD.md`（正在做／還沒做的
工作）與 `WORK_BOARD_DONE.md`（已完成的工作），確認沒有人已經在做、
也沒有人已經做完同一件事——舊格式留下的行有時沒有明講「已完成」三個
字，兩份都查才不會誤判。看不出算不算重複就直接編輯那份文件寫清楚
疑點，不要用猜的。開始時在 `WORK_BOARD.md` 認領一行。完整格式規則見
零之四。

---

## 零之一、自動 PR 審查——CodeRabbit（免費，公開 repo 自動適用）

[CodeRabbit](https://coderabbit.ai) 對公開 repo 免費，裝好之後任何人
開 PR 都會自動觸發一次審查留言，不需要另外呼叫誰。設定檔在
`.coderabbit.yaml`（審查語言、排除 `results/`／`data/` 這類資料檔、
對 `injection_recovery.py`／`pipeline/` 等共用核心給的審查提示）。

啟用需要 repo 擁有者做一次性的手動步驟（Claude／agent 無法代勞，這是
帳號授權操作）：到 https://github.com/apps/coderabbitai 或
https://coderabbit.ai 用 GitHub 帳號登入，安裝 App 並選這個 repo。
裝好後不需要金鑰、不需要額外設定。

CodeRabbit 的審查是**輔助**，不是取代 `WORK_BOARD.md`／PR review 流程——
它抓程式面的問題（bug、重複實作、效率），跑不動這個專案的實際計算，
數字對不對還是要靠 `results/RESULTS_LOG.md` 跟人／其他 agent 交叉核對。
它也**不會自動修**——收到 CodeRabbit 留言，要照下面的流程回覆＋resolve
（或確認站不住腳就說明理由）。如果 CodeRabbit 誤判某則已經處理好的
留言、卡住不放，找不到問題就直接在 GitHub UI 手動 resolve 該討論串。

合併不強制要求 CodeRabbit 核准（`required_approving_review_count=0`）
——任何人隨時可以直接按 merge，**合併前自行判斷 CodeRabbit 的意見要
不要處理**，不是被按鈕鎖死。

**「Review completed」不能只看這個綠燈**：CodeRabbit 在 commit status
上永遠顯示 `context=CodeRabbit`／`description="Review completed"`／
`state=success`，**即使它因為額度用完（"Review rate limited"）根本
沒有送出正式 Review 也一樣**——這個綠燈只代表「CodeRabbit 的自動化
跑完了」，不代表「真的審過、審查結果可信」，肉眼從 PR 頁面或
`gh pr checks` 完全分不出來。唯一可靠的判定：拿最後一則正式 Review 的
`commit_id`，跟 PR 目前的 head SHA 比對——相等才代表這次 push 真的
被審過。已經寫成工具，合併前先跑：
```
scripts/tools/coderabbit_status.sh <PR 編號...>   # 查指定 PR
scripts/tools/coderabbit_status.sh                # 查全部 open PR
```
輸出會明確標示每個 PR 是「✅ 已審過且核准」「🔴 已審過但還有意見」
「💬 已審過但只是留言」，還是「⚠ 還沒有針對目前這個 commit 的正式
review（多半是額度用完被跳過）」——只有前三種才代表 CodeRabbit 真的
看過目前這次 push，「⚠」那一種不能當「審過沒問題」。

`@coderabbitai resolve` 這個指令**必須以 PR 的頂層留言送出**（不是在
單一討論串裡回覆），而且它會**一次 resolve 掉全部** CodeRabbit review
comments。所以它只適合「所有留言都已經處理完、只是要一次收尾」的情況；
**單一誤判請只在 GitHub UI resolve 對應的那一個討論串**，不要用這個
指令——否則會把其他還沒處理的留言一起關掉，正好製造這整個機制要防的
「看起來處理過了、其實沒有」。

---

## 零之二、寫進文件（跟對話）裡的判斷，預設是「當下的想法」不是「決議」

規劃類文件（`docs/planning/` 底下這些、`WORK_BOARD.md` 的說明文字、
PR 描述、甚至跟使用者的對話回覆）裡寫的「結論」「判斷」「建議」，
預設代表的是**寫下這段文字當下**的想法，不是這個專案的決議或不能動
的規則——這個 repo 由多個人與多個 agent 共同維護，同一份規劃文件常常
在短時間內被反覆討論、修正、甚至推翻自己稍早的判斷。

**具體怎麼做**：

1. **推理過程跟數字要完整寫清楚，只有語氣要保留**——不是叫你把內容
   寫得含糊或籠統，「保留語氣」指的是用「初步判讀」「目前傾向於」
   「還沒驗證過」取代「結論是」「必須」「不能」這類斷言式措辭，論證
   鏈本身、引用來源、算出來的數字，都要跟平常一樣完整交代。
2. **任何人／任何 agent 發現某段判斷站不住腳、有更好的做法、或被新
   資訊推翻了，可以直接改，不需要先徵求原作者同意**——這是把
   `WORK_BOARD.md`／`results/RESULTS_LOG.md` 那種「附加寫入、低衝突
   風險」的協作精神，延伸到規劃文件本身的內容判斷上。真的改動到別人
   可能同時在動的地方，還是照第一節的一般原則處理（有風險就讓對方
   看過再合併）。
3. **判斷文件內容新不新、可不可信，看文中標的日期，不要看語氣**——
   語氣本來就刻意寫得不肯定，不代表內容不可靠；反過來，舊文件裡語氣
   寫得篤定的段落，也不代表現在還適用，一樣要看日期跟後續有沒有訂正
   記錄。日期標在章節標題或段落開頭就夠，不用額外加「判斷日期／最後
   驗證」這種固定欄位。
4. **這條不取代已經在用的結構化信心分級系統**——`LIMITATIONS.md` 的
   A–D 嚴重度分級（見第五節）、規劃文件裡「已讀原文」vs「WebSearch
   摘要未讀原文」的引用可信度標記，這些是刻意設計的、用標籤而非語氣
   表達信心程度的機制，這條規則管的是規劃階段的推理與判斷該用什麼
   語氣寫，不是要重新設計那些結構化標記系統。

---

## 零之三、開始新工作前——也要查 `QUEUE_ALERTS.md`

`run_queue.py` 的 Gate B（開跑前檢查）／Gate C（跑完後驗收）沒過時，
不會讓整條佇列停下來（機器繼續跑下一項，不留著空轉），但會寫進
`QUEUE_ALERTS.md`。**任何 session 開始碰這個 repo 的工作前，先讀一次
這份文件，看有沒有「待處理」的項目**，處理完（或確認不是問題）要把
該列狀態改成「已處理」——不要刪掉那一列。完整格式與 Gate B／C 各自
該怎麼收尾寫在 `QUEUE_ALERTS.md` 檔頭。

---

## 零之四、`WORK_BOARD.md` 分成兩份：待辦 vs 已完成

- `WORK_BOARD.md`：只放正在執行或還沒執行的工作。認領規則、疑義
  處理、算力池歸屬這些「怎麼用這份文件」的說明也留在這裡。
- `WORK_BOARD_DONE.md`：只放完全完成的事——連「等待交接」「等待
  後續驗收」都不算，要嚴格到「這個任務本身沒有任何必要部分還沒做」
  才能搬過來。

判斷一個任務算不算「已完成」該搬過去：驗收標準已經達成、或使用者／
後續查證已經明確確認結案，都算——不要求「絕對沒有任何後續延伸
工作」，只要求「這一行描述的具體任務本身做完了」。文字裡如果提到
「若要升級／若要量化，還可以做 X」，那是選配的延伸工作，不影響這一
行本身歸類為已完成；但如果明講「仍待做」「目前阻塞」「還沒有」「等待
交接」「等待驗收」這種這個任務本身的必要部分沒做完，就不算，留在
`WORK_BOARD.md`，狀態標「進行中」或「尚未進行」。判不出來就當還沒
完成，留在待辦那份——錯誤地把還沒做完的事標成已完成，比錯誤地把
做完的事留在待辦，代價大得多（前者會讓人以為不用做了）。

**表格格式**：一個任務一張小表格（只有一列資料），底下緊接著一段
獨立的說明文字，然後才是下一個任務的表格——不要把所有任務塞進同一
張大表格、再把說明文字集中寫在表格結尾，那樣讀者要一直往回對照第
幾行對應哪一段。`WORK_BOARD.md` 的欄位固定五個：

| 任務名稱 | 狀態 | 開始日期／指派時間 | 輸入參數 | 輸出參數 |
|---|---|---|---|---|

- 狀態只有兩種：「進行中」與「尚未進行」。已完成的不會出現在這份
  文件——完成就整段（表格＋說明文字）搬去 `WORK_BOARD_DONE.md`
  （見下方具體操作）。
- 每個任務只有一張表格、一段說明，不是每次狀態變動就加一組新的。
  狀態從「尚未進行」改成「進行中」時，直接覆蓋那一格，開始日期也
  一併補上；不要用刪除線劃掉舊狀態再寫新的、也不要在說明文字裡疊加
  好幾輪的更新——舊的過程資訊等任務做完後會整段搬進
  `WORK_BOARD_DONE.md`，屆時只留結論（見下一節），中途更新不需要
  保留完整歷史。
- 表格欄位裡不要塞解釋性文字：任務名稱後面的括號只放對應的
  `LIMITATIONS.md` 條目代碼（例如 `A1`），不要在括號裡寫一整句話。
- 輸入／輸出參數寫物理量（符號與單位），不要寫程式裡的變數名稱或
  指令旗標——例如寫「合成星數 N = 40,000 顆」，不要寫
  「--n-syn 40000」；寫「消光量 A_V 掃描上限 1.20 mag」，不要寫
  「--dav-sweep ... 1.20」。可重現用的實際指令、旗標、檔名放到下方
  說明文字裡，欄位只給看得懂物理意義的摘要。
- 每個任務在表格下方緊接著一段獨立的說明文字（標題用任務名稱），
  寫清楚這個任務具體要解決什麼問題、用什麼方法（含可重現的實際
  指令）、目前卡在哪裡、預計要跑多久——耗時只在有實測依據時才寫
  具體數字，查不到依據就寫「未查證」，不要憑感覺湊一個數字。
- 不要用粗體強調：無論表格還是說明文字，都用平鋪直敘的句子，不要靠
  粗體標重點——重點寫進句子本身的用字就好。

**`WORK_BOARD_DONE.md` 的格式**：同樣是一個任務一張小表格＋一段說明
文字，但欄位跟內容政策不一樣，因為這裡只收完全做完的事，不需要追蹤
「現在的狀態」：

| 任務名稱 | 完成日期 | 輸入參數 | 輸出參數 |
|---|---|---|---|

- 沒有「狀態」欄——能出現在這份文件裡，狀態就只有一種（完成），
  留著這欄只會每行都寫同一個字，直接拿掉。
- 沒有「備註」欄，也不保留原始的執行過程細節（誰做的、卡在哪裡、
  改了哪些檔案、CodeRabbit 抓到什麼）——完整過程去查對應的 PR／
  commit 歷史，這份文件只留結論。輸入參數欄跟 `WORK_BOARD.md` 一樣
  寫物理量，不寫程式變數名稱。
- 說明文字只回答三件事：這次跑出了什麼（結果本身，含數字）、之後
  會在哪裡被用到（哪個還開著的任務依賴這個結果，或哪份文件引用了
  它）、對這個專案有什麼幫助（解決了什麼問題、排除了什麼可能性、
  還是純粹確立了一個後續依賴的基準線）。不要重述做的過程——「用
  什麼方法、中間卡過哪裡」屬於過程，不屬於結論，除非那個過程本身
  就是要回報的結果（例如「查證後發現原假設不成立」這種過程即結論
  的情況）。
- `WORK_BOARD_DONE.md` 不受 CI 的 append-only 檢查約束（見
  `scripts/tools/check_append_only.py` 的說明）：整理、濃縮、合併
  重複條目都是這份文件的正常維護動作，不是要被擋下來的異常刪改，
  只有 `results/RESULTS_LOG.md` 才是真正逐次執行、不能事後精簡的
  歷史紀錄。

**具體操作**：
1. 一個任務真正做完時，把 `WORK_BOARD.md` 裡那一段（表格＋下方說明
   文字）整段剪下，改寫成 `WORK_BOARD_DONE.md` 的四欄格式與純結論
   的說明文字（不是逐字搬過去——過程細節在這一步就要精簡掉），依
   完成的先後順序貼到 `WORK_BOARD_DONE.md`「完成事項」區尾端，
   `WORK_BOARD.md` 這裡全部刪掉，不要留著。
2. 只是狀態更新（尚未進行→進行中、換人接手、卡住了）不算完成，
   直接覆蓋 `WORK_BOARD.md` 裡那個任務的表格儲存格與說明文字，不用
   動 `WORK_BOARD_DONE.md`。
3. 待辦表格裡若有一整批項目同時完成，可以歸在 `WORK_BOARD_DONE.md`
   同一個小節標題下，但每個任務仍然各自一張表格＋一段說明，不要
   合併成一段混寫。
4. 兩份文件互相參照：`WORK_BOARD.md` 開頭已加一句指向
   `WORK_BOARD_DONE.md`；`WORK_BOARD_DONE.md` 開頭也指回
   `WORK_BOARD.md`。看不出某任務目前在哪一份、或算不算已完成，兩份
   都搜尋一次任務名稱，不要用猜的。

---

## 一、分支與 Pull Request——所有人都要走 PR，沒有例外

`main` 分支設了保護規則：**任何人（含 repo 擁有者）都不能直接 push 到
main**，包括修 bug、加一個檔案這種小事也一樣。流程一律是：

1. 開一個新分支，命名 `<你是誰>/<這次要做什麼>`，例如：
   - `claude/p9-redo`（Claude 工作階段）
   - `codex/estimator-v2`（Codex）
   - `<同學名字>/hyades-tier1`（人類協作者）
2. 在分支上工作、commit、push 到這個分支。
3. 開 PR 合併回 `main`。**PR 不強制要求別人 review 才能合併**——
   目的是留下「這裡發生過什麼變更」的可見紀錄，不是設審核關卡。
   除非你自己判斷這次改動有風險（例如改到別人可能同時在動的地方），
   才需要等其他人看過再合併；一般情況下開了 PR 可以自己直接合併。
4. 合併後**刪掉這個分支**，避免分支越積越多。

### 遇到合併衝突怎麼辦

**逐行文字衝突**（GitHub 直接擋下、標紅）：在自己的分支上
`git fetch && git rebase origin/main`（或 `git merge origin/main`），
git 會標出 `<<<<<<< / ======= / >>>>>>>`，逐段決定留哪邊，
`git add` + commit + push 後繼續合併。這是機制性的，不需要額外判斷。

**靜默語意衝突**（改到不同行，git 自動合併不報錯，但內容互相矛盾）——
**這是這個專案最容易踩到的坑，必須額外注意**：任何 PR 只要碰到
`LIMITATIONS.md` 或 `PAPER_OUTLINE.md`，合併前一定要重新讀一次 main
上這兩份文件**目前的完整版本**（不是只看自己這次的 diff），確認自己
新加的結論沒有跟其他人剛合併進去的東西矛盾。這兩份文件本來就有「推翻
舊說法要回頭標記原處，不能只在新處新增」的維護規則，同一個道理套用在
多人協作：**你的 PR 如果推翻或修改了某個既有結論，要在 PR 描述裡明講
改了什麼、為什麼**，讓後面合併的人看得到。

---

## 一之一、標註是誰改的——PR 標籤 + commit 前綴

Repo 已經建好三個彩色 PR 標籤：🔵 `by:claude`、🟣 `by:codex`、🟢 `by:human`。
**每個 PR 開的時候都要貼上對應標籤**，PR 列表上一眼就看得出誰做的。

Commit 標題也統一加前綴，方便 `git log`／blame 直接搜尋：

```
[Claude] 修好 XXX
[Codex] 新增 YYY
[王小明] 調整 ZZZ
```

三層一起用（標籤＋前綴＋git blame 本身）不需要額外工具就有完整的
可視化追蹤。

---

## 一之二、改既有段落時不要順手重新排版——會讓 diff 大到沒人看得懂

**問題**（2026-08-29 發現）：`LIMITATIONS.md`／`WORK_BOARD.md` 這類大文件
裡，條目常常是一整段擠在幾行的長文字。git 是逐行比對，只要改動段落
中間幾個字、卻同時把整段文字重新換行（例如為了讓行長看起來整齊、或
編輯器自動重排），**那一整段就會被判定成整段刪除＋整段新增**，即使
語意上只改了一兩句。實際案例：一次只改結論的 PR，`LIMITATIONS.md`
671 行的檔案被 git 判成一個橫跨全檔的 hunk（`@@ -1,671 +1,677 @@`），
diff 完全看不出真正改了什麼，審查等於白審。**這才是「PR 看不懂在改
什麼」最常見的成因，通常不是因為真的改了很多東西。**

**規則**：

1. **修改既有段落時，不要動到沒有實質變化那幾行的換行位置**——只在
   真的要新增/刪除文字的地方動手，其餘照抄原本的斷行，就算看起來
   長短不齊也不要「順手」重排。
2. **真的需要大範圍重新排版**（例如整份文件換新格式），要開**獨立的
   PR**，並在 PR 描述裡寫明「這是純格式調整，沒有改變任何實質內容」，
   不要跟新結果／程式碼修正混在同一個 PR——審查的人才能放心只抽查
   幾段確認語意沒變，不用逐行核對。
3. **開 PR 前檢查一下 diff 大小是否合理**：如果只改了一個結論，卻看到
   整份文件變色，先確認是不是不小心觸發了整段重排；不是的話再開，
   是的話拆成上面第 2 點的獨立 PR。

---

## 二、結果檔案規範——每次算出新結果都要記進 `results/RESULTS_LOG.md`

`results/` 資料夾本來就進版控，這是唯一該存放正式結果檔案（`.npz`／
`.csv` 等）的地方（`logs/`、`isochrones/`、`kaggle_work/` 等不進版控，
太大或可重建）。

每次產生一個新的結果檔案，在 `results/RESULTS_LOG.md` **檔案尾端加
一行**（附加寫入，不要改動既有的行——這是刻意設計成低衝突風險的
格式，兩個人同時加行幾乎不會撞在一起）：

```
| 日期 | 執行者 | 腳本+參數 | commit hash | 結果檔名 | 一句話結論 |
```

執行者填清楚是誰／哪個 agent（例如「Claude session」「Codex」
「王小明」），commit hash 填**跑這個結果當下** `git rev-parse HEAD`
的值，讓任何人事後都能精確對回當時的程式碼版本，不必另外拉 git tag。

**主控板（`status_dashboard/`，2026-08-24 新增）**：`py
status_dashboard/app.py`（或雙擊桌面捷徑）開一個本機網頁，把「傳統法／
前向模型／PDMF→IMF／穩健性診斷」四大類底下有哪些步驟、每個步驟對應
哪些腳本、目前執行到哪，整理在同一頁——取代翻十幾份 `.md` 文件才能拼
出全貌的做法。「階段 → 步驟 → 腳本」的對照表在
`status_dashboard/stage_map.py`，是**手動維護**的索引（沒有機器可讀的
來源能自動生成傳統法／PDMF→IMF／診斷類的分類結構）。**新增
`WORK_BOARD.md` 任務或新腳本時，記得回來 `stage_map.py` 加一筆**，
跟這裡「新結果要記進 `RESULTS_LOG.md`」是同一種「找不到自動生成辦法、
只能手動維護索引」的協作規則。

---

## 三、Commit 與身分標示

每筆 commit 的訊息裡要能看出是誰／哪個 agent 做的變更：

- 人類協作者：用自己的 GitHub 帳號 commit，訊息正常寫。
- AI agent：commit message 結尾加 `Co-Authored-By: <agent 名稱>
  <noreply 或該 agent 的識別 email>`，例如
  `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`。
  這樣 `git log` 隨時看得出這筆是哪個 agent 代跑的，不用去問。

---

## 四、遇到不明修改時

如果你發現檔案被改了、但不知道是誰／為什麼——**不需要主動追問或花
token 去猜**，先看 `git log`／PR 紀錄能不能查到；真的看不出來，
且你判斷這個改動可能有問題（跟已知結論矛盾、看起來不完整），
在 `RESULTS_LOG.md` 或對應的 PR 裡留言標記，讓專案擁有者決定要不要
進一步處理。這條跟 `CLAUDE.md` 裡「發現有未知修改時」那條規則是同一件事，
這裡是給非 Claude 的 agent／人看的版本。

---

## 五、`LIMITATIONS.md` 怎麼寫（任何人／任何 agent 編輯前必讀）

`LIMITATIONS.md` 是給人看的限制清單，不是工作日誌。**依嚴重程度 A–D 排序，
不依類型排序**，每一條嚴格照固定格式：標題行是編號＋問題名稱，內文只有
`**問題**：` 與 `**後果**：` 兩段。

| 級 | 意義 |
|---|---|
| A | 已引用的數字可能是錯的，或現有主張可能不成立。必須處理 |
| B | 每次計算都在用的未驗證假設。影響數字大小，不改變結論方向 |
| C | 結構性限制，修不掉。論文必須聲明 |
| D | 已知風險，尚未驗證。目前沒有污染現有結果的證據 |

**分級判準**：問「如果不理它，會不會有哪個已經寫進論文的數字其實是錯的」。
答案是「有可能」→ A。答案是「不會，但每次計算都在用這個沒驗證過的假設」
→ B。「不會，因為這件事本來就做不到」→ C。「不會，只是還沒去證實不會」
→ D。**B 跟 D 最容易混**：B 是已經在用的假設，D 是還沒做的測試。

**不要寫進 `LIMITATIONS.md` 的東西**：追查過程、修正過程、方法論心得、
基礎設施故障、已推翻的說法、作廢的數值。分別歸到本檔（心得，見下一節）、
`docs/reference/REFUTED.md`（推翻與作廢）、`docs/reference/KAGGLE_DIAGNOSIS.md`（派工基礎設施）。
**問題修好、且對應工作全部跑完確認後，那一條要搬到 `RESOLVED.md`（見
五之一），不要留在 `LIMITATIONS.md` 裡寫「已修正」，也不要直接刪掉不留
紀錄**——刪掉會讓下一輪重新踩到同一個問題時查不到「這件事我們做過了」。

## 五之一、`LIMITATIONS.md` 與 `WORK_BOARD.md` 的雙向追蹤

**目的**：`LIMITATIONS.md` 記「哪裡有問題」，`WORK_BOARD.md` 記「誰在
解決」，這兩份文件要能互相標註、彼此核對，讀者不必自己在腦中兜起來。

**規則一：`LIMITATIONS.md` 每一條的標題行後面加括號，列出解決這個問題
需要哪些工作、目前是否正在進行**：

```
### A1 精修 bug 波及的結果尚未重跑（p2_final2_v3 是、p6_lowmass_v2 否）
```

`是`＝這個工作項目此刻正在跑（本機佇列/Kaggle 派工/協作者機器上，
不是「排進佇列但還沒輪到」）；`否`＝已認領或已排隊但還沒真的開始跑，
或還沒有人認領。**還沒有任何對應工作的條目**（多數 C 類、D 類本來就是
「已知風險，尚未驗證」，D 類定義本身就允許沒有工作項目）寫
`（尚無認領工作）`，不要留白，留白會讓人以為忘記寫。

**規則二：完成一項工作、把結果上傳（commit／開 PR）後，必須：**

1. 回到 `LIMITATIONS.md`，把該工作項目從對應條目的括號裡**刪掉**
   （不是改成「已完成」——括號只列「還沒做完的」，做完的就從列表消失，
   跟 CONTRIBUTING.md 其他地方「附加而非累積」的精神一致，只是這裡是
   減法不是加法）。
2. 檢查刪掉這項之後，括號是不是空了——**空了代表這個問題所需的全部
   工作都做完了**，這時要重新讀一次 `**問題**`／`**後果**` 兩段，
   確認新結果真的解決了問題（不是「工作跑完了」就自動等於「問題解決
   了」，跑完可能發現問題還在、只是換了個樣子）。確認解決後，把整條
   從 `LIMITATIONS.md` 搬到 `RESOLVED.md`（格式仿 `docs/reference/REFUTED.md`：
   曾經的問題／怎麼解決的／解決時的結果／日期），不要留在
   `LIMITATIONS.md` 裡。**如果跑完發現問題沒解決或只解決一部分**，
   括號要換成新的工作項目，不能清空後假裝結束。

**規則三：在 `WORK_BOARD.md` 開始或更新一項工作時，任務名稱後面加括號
標明這項工作要解決 `LIMITATIONS.md` 的哪一條／哪幾條**：

```
| ... | p2_final2 重跑（A1、A2） | ... |
```

一項工作可能同時對應多條，全部列出來，不要只挑一個。跟 `LIMITATIONS.md`
完全無關的工作（環境設定、文件整理、協作流程本身）不用加括號。

**規則四：修改 `WORK_BOARD.md` 之後，一定要回頭確認
`LIMITATIONS.md` 對應條目的括號狀態是否還正確**——新認領一項工作，
去對應條目的括號裡把它加上（狀態填「否」，除非當場就開始跑）；工作
狀態從排隊變成正在跑，去把對應的「否」改成「是」；工作做完，套用
規則二處理。**這是雙向義務，不是只改一邊**：改了 `WORK_BOARD.md`
沒有回頭確認 `LIMITATIONS.md`，這條規則就形同虛設，兩份文件又會
各自漂移。

## 五之二、新發現要順手同步 `PAPER_OUTLINE.md`

`PAPER_OUTLINE.md` 自己開頭就寫了維護規則：新結果或推翻舊說法不必等
使用者要求就要同步進去。

**規則**：任何一次因為新發現而編輯 `LIMITATIONS.md` 或 `WORK_BOARD.md`
（不管是新增一條限制、把工作項目從括號刪掉、或是把某條搬去
`RESOLVED.md`），都要**當場**多做一步，去讀 `PAPER_OUTLINE.md` 對應的
章節，確認兩件事：

1. **資訊是否過時**——那個章節引用的數字、方法論描述、或既有結論，
   是不是被這次的新發現推翻或需要補充警語。
2. **方向是否需要修正**——這個新發現會不會動搖某個章節的論文主賣點
   或既有敘事（例如「模型對等時線選擇穩健」這種主張，若新發現顯示
   它其實是別的機制造成的表面現象，就要照實改寫，不能因為是主賣點
   就迴避）。

過時或需要修正的地方就直接編輯，不必等使用者開口——跟 `LIMITATIONS.md`
本身的維護精神一致。這不是每次都要通篇重讀 `PAPER_OUTLINE.md`（400 多行，
成本太高），而是針對這次新發現**邏輯上會碰到的章節**去核對，範圍不確定
時寧可多查一節，不要跳過。

## 五之三、`docs/PROJECT_TIMELINE.md` 定期更新

`docs/PROJECT_TIMELINE.md` 是逐日整理的專案進度日誌，給使用者快速回顧
「哪一天做了什麼、發現了什麼」用，不是逐 commit 的流水帳——**只記真的
改變結論、抓到 bug、或架構調整的項目，省略純粹的 PR merge、CodeRabbit
小修這類雜訊**，格式維持既有的「日期＋條列重點」。

**規則**：每累積一天有實質進度（不是每個 commit 都要更新），在當天結束前
或下次有人碰這份文件時，幫忙補上當天那一節；發現舊的某天內容已經被後續
發現推翻或過時，直接改，不必等使用者開口。不需要每次都回頭重寫更早的
日期，只補當下这一節或訂正明確過時的地方。

## 六、已經犯過、不要再犯的錯

1. **撞牆先查底層資料的實際涵蓋範圍，不要反射性放寬搜尋軸。**
   放寬搜尋軸只在「網格有資料但軸設太窄」時有用；網格根本沒有那段資料時，
   放寬只會擴大簡併平坦區，argmax 報出的「最佳值」是搜尋軸邊界決定的。
   （`pipeline/joint_fit.py` 現在會在載入時比對搜尋軸與 `np.unique()` 的
   實際涵蓋範圍並印警告，但警告要真的去看。）

2. **先驗、搜尋軸、網格實際涵蓋，三層邊界必須一致。**
   曾經三層各自寫死成 0.60 ＞ 0.40 ＞ 0.336，互不知道彼此。

3. **抽查兩三筆就下「其他應該沒事」的結論是錯的。**
   要系統性掃描全部，再對候選逐一人工核對（自動掃描本身會有假警報）。

4. **診斷腳本的設定不可直接複製到產出論文數字的腳本。**
   關掉先驗對「量概似本身的行為」是對的，對「產出最終數字」沒有同等理由。

5. **結果檔案不可被重跑覆寫。** 固定檔名 `np.savez` 會讓前一次的完整
   參數向量永久消失，事後想核對邊界時無資料可查。輸出要帶 tag。

6. **兩個現象同時出現，不要假設同一個成因。** 先個別驗證。
   （Kaggle 掛載失敗與網頁 Editor 卡死被錯誤關聯，拖慢查真正根因的速度，
   見 `docs/reference/KAGGLE_DIAGNOSIS.md`。）

7. **懷疑外部服務之前，先確認自己組出來的路徑／參數字串。**
   100% 重現的失敗是確定性的程式碼 bug，不會是機率性的平台不穩。

8. **改動搜尋／精修演算法後，要驗證每一階真的執行了。**
   看回傳值落在哪個解析度的格點上就知道——全部落在粗網格格點上代表
   精修根本沒跑。

9. **任何一次狀態查證，只要文件裡有對應的舊敘述，就當場回頭改。**
   只在新的地方補一筆會讓文件自相矛盾，下一輪讀到過時的那半會走錯方向。

---

## 七、快速檢查清單（開 PR 前）

- [ ] 分支名稱有標明是誰／哪個 agent
- [ ] 新結果檔案已經在 `results/RESULTS_LOG.md` 加了一行
- [ ] commit message 有正確的身分標示
- [ ] 若碰到 `LIMITATIONS.md`／`PAPER_OUTLINE.md`：已經重讀過 main
      上的完整版本，確認沒有跟別人的新增內容矛盾
- [ ] 若編輯 `LIMITATIONS.md`：新增／修改的條目照第五節的格式與分級
- [ ] 若編輯 `WORK_BOARD.md`：任務名稱有標對應的 `LIMITATIONS.md` 條目
      （見五之一），且已回頭同步該條目括號裡的工作狀態
- [ ] 若某條 `LIMITATIONS.md` 因為這次工作而完全解決：已搬到
      `RESOLVED.md`，不是留在原處或直接刪掉
- [ ] 若這次工作有新發現／推翻舊說法而編輯了 `LIMITATIONS.md` 或
      `WORK_BOARD.md`：已檢查 `PAPER_OUTLINE.md` 對應章節是否過時、
      方向是否需要修正（見五之二），不是只改前兩份文件
- [ ] 若這次編輯了任何規則類文件：沒有加入帶日期的「因為 X 所以改 Y」
      敘事——理由需要記錄的話寫進 `CHANGELOG.md`，不是寫在規則本身
