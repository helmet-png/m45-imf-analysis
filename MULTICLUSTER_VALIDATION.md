# NGC 3532／Praesepe：M45 IMF 程式通用性實測

日期：2026-08-11　｜　執行者：Codex　｜　分支：
`codex/ngc3532-praesepe-generalization`

## 結論先講

**目前不能說 M45 程式「全部通用」。** 實測結果可分成三層：

1. **傳統質量指派、五種雙星修正與轉折點檢查可移植。** 兩團都能從本地
   HR23 成員表重現執行，0.50–2.50 M☉ 也都低於單調主序上限。
2. **測光誤差與選擇函數的程式框架可移植，但參數不能沿用 M45。** 必須依
   星團重新向 Gaia 取回訊噪比欄位、重新校準；兩團重新校準後均通過原驗收門檻。
3. **完整聯合前向模型尚未通過。** NGC 3532 放開年齡／消光後出現
   `A_V=0`、`q_gamma=+1.5` 的角落解；Praesepe 不論固定／放開年齡、
   放開金屬量或加入差異消光，`f_bin` 仍約 0.98–1.00。這些情況下得到的
   alpha 只能當診斷值，**不能當最終 IMF**。

## 輸入資料與前處理

| 星團 | HR23 `Prob>=0.7` | Gaia crossmatch | `G<=18` | 測光品質後 | 選擇函數整體誤差 | 逐星等最大誤差 |
|---|---:|---:|---:|---:|---:|---:|
| NGC 3532 | 756 | 756 | 683 | **647** | +0.006 | 0.020 |
| Praesepe / NGC 2632 | 434 | 434 | 394 | **381** | -0.001 | 0.010 |

原 M45 驗收門檻是整體誤差小於 0.02、逐星等最大誤差小於 0.08；兩團皆通過。
這證明 `pipeline/selection.py` 的形式可重用，但不是證明 M45 的
`selection.npz` 可以直接複製給別的星團。

## 傳統法：使用與前向模型相同的乾淨樣本

固定 HR23 年齡／消光、`[M/H]=0.00`、質量範圍 0.50–2.50 M☉：

| 方法 | NGC 3532 alpha | 擬合星數 | Praesepe alpha | 擬合星數 |
|---|---:|---:|---:|---:|
| 全當單星 | 1.508 ± 0.090 | 591 | 2.150 ± 0.155 | 227 |
| CMD 剔除 | 1.534 ± 0.093 | 552 | 2.283 ± 0.177 | 180 |
| 文獻解析修正 | 1.524 ± 0.090 | 591 | 2.166 ± 0.155 | 227 |
| RUWE 剔除 | 1.523 ± 0.084 | 560 | 2.210 ± 0.161 | 202 |
| Gaia NSS 剔除 | 1.532 ± 0.083 | 579 | 2.205 ± 0.150 | 221 |

NGC 3532 五法只跨 0.026；Praesepe 跨 0.133，主要來自 CMD 剔除。Praesepe
有 179/381 顆被 CMD 規則標記，比例過高，且前向模型同時出現 `f_bin=1`；
兩者共同指向「目前等時線／年齡／CMD 展寬描述不合」，不能把 0.133 直接
解釋成真實雙星修正效應。

轉折點檢查已通過：NGC 3532 的單調主序上限為 3.684 M☉，Praesepe 為
3.042 M☉，都高於分析上限 2.50 M☉。

## 前向模型實測

下表的誤差是更換模型端共用亂數後的重現性散布，不是完整天文誤差棒。
「注入偏差／散布」是以該設定的平均最佳解生成假資料再回收 alpha。

| 星團與設定 | logAge | A_V | f_bin | [M/H] | alpha | 注入偏差／散布 | 判定 |
|---|---:|---:|---:|---:|---:|---:|---|
| NGC 3532，固定 HR23 年齡／消光 | 8.400 | 0.120 | 0.241 | 0.00 | 1.696 ± 0.171 | +0.015 / 0.079 | **條件式結果**；未撞牆，但對模型亂數敏感 |
| NGC 3532，年齡／消光自由 | **8.700** | **0.000** | 0.208 | 0.00 | 1.550 ± 0.050 | -0.017 / 0.067 | **失敗**；年齡上界與 A_V 下界 |
| NGC 3532，年齡擴至 9.00 | 8.700 | **0.000** | 0.250 | 0.00 | 1.300（1 次） | 未跑 | **失敗**；q_gamma=+1.5 上界，非年齡窗造成 |
| NGC 3532，固定年齡／消光、金屬量自由 | 8.400 | 0.120 | 0.35／1.00 | -0.10／-0.20 | 1.700 ± 0.000 | +0.083 / 0.083 | **不穩**；f_bin 隨模型亂數跳動 |
| NGC 3532，加入差異消光 | 8.400 | 0.120 | 0.242 | 0.00 | 1.817 ± 0.117 | -0.083 / 0.033 | **不穩**；dav 一次撞 0.60 上界 |
| Praesepe，固定 HR23 年齡／消光 | 8.550 | 0.159 | **1.000** | 0.00 | 1.867 ± 0.094 | +0.081 / 0.131 | **失敗**；f_bin 物理上界 |
| Praesepe，年齡／消光自由 | **8.900** | 0.083 | **1.000** | 0.00 | 1.683 ± 0.083 | +0.067 / 0.250 | **失敗**；f_bin 物理上界 |
| Praesepe，老年齡、金屬量自由 | 8.900 | 0.083 | **0.983–1.000** | +0.10 | 1.633 ± 0.033 | -0.083 / 0.050 | **失敗**；金屬量不能解除 f_bin 貼牆 |
| Praesepe，老年齡、加入差異消光 | 8.900 | 0.083 | **0.983–1.000** | 0.00 | 1.617 ± 0.117 | +0.050 / 0.067 | **失敗**；差異消光也不能解除貼牆 |

Praesepe 的年齡自由擬合從 HR23 的 8.539 移到網格 8.900，這是有用的診斷：
固定 346 Myr 並不符合目前 PARSEC CMD 的偏好。但年齡修正後 f_bin 仍為 1，
所以不能把問題全部歸因於 HR23 年齡。

注入回收只回答「假資料由同一模型生成時，搜尋器能否找回答案」，不能證明
模型能描述真實星團。這次正好看到：部分注入回收尚可，但真資料參數仍貼牆，
因此不能用注入回收通過來掩蓋真實 CMD 的模型失配。

## 實測中修掉的通用性問題

1. `cluster_imf_tier1.py` 原本明明已有本地 HR23 快照，仍強制連 VizieR；
   現在預設讀本地，只有 `--refresh` 才連網。
2. 原腳本把消光係數寫成 `(2.740, 3.374, 2.035)`，註解卻說與
   `config.toml` 相同；實際設定是 `(0.83, 1.08, 0.63)`。修正後相對舊值，
   在相同乾淨樣本上，全當單星 alpha 移動約 -0.014（NGC 3532）／
   +0.097（Praesepe），Praesepe 的 CMD 剔除 alpha 更移動約 +0.427，
   足以改變方法比較。
3. M45 的聯合先驗 `logAge<=8.30` 無法涵蓋兩個新星團；多星團入口現在依
   輸入網格與命令列動態設定。
4. M45 的 `alpha>=1.5` 在 NGC 3532 快速試跑直接撞牆；多星團入口預設改為
   0.8–3.5，`f_bin` 也完整涵蓋 0–1。
5. 年齡／金屬量是離散等時線格點。舊精修會回報格點中間的數字，實際卻
   吸附回最近格點；新入口在粗搜尋後固定這兩個離散軸，避免假精度。
6. 前向模型的資料檔名、誤差模型與選擇函數不再固定指向 M45；每個星團有
   自己的 `cluster_<name>_*` 檔案。

## 最終通用性判定

| 模組 | 判定 | 理由 |
|---|---|---|
| HR23 成員表讀取 | 通過 | 本地可重現；兩團 Gaia source_id 100% crossmatch |
| 傳統質量指派 | 通過（有條件） | 轉折點涵蓋 2.5 M☉；仍依賴外部年齡／消光／金屬量 |
| 五種傳統雙星修正 | 程式通過、科學解讀需保留 | 都能跑；Praesepe 的極高 CMD 旗標揭露模型不合 |
| 測光誤差模型 | 通過 | 每團可獨立建立 native BP/RP 誤差曲線 |
| 選擇函數 | 通過（必須重校準） | 兩團通過 M45 原驗收門檻 |
| JointModel 固定外部參數 | 部分通過 | NGC 3532 可得條件式結果；Praesepe f_bin=1 |
| JointModel 全聯合擬合 | **未通過** | 兩團皆有角落／貼牆解，且多項敏感度修正無法解除 |

## 下一步（在報最終 IMF 前必做）

1. 畫真資料與最佳模型的 CMD 殘差圖，定位造成 f_bin／q_gamma 貼牆的是哪一段
   主序；分開測試轉折點、低質量端、白矮星／巨星與離群成員。
2. 取得每團獨立的光譜金屬量先驗；不能再用「太陽金屬量方便」當最終設定。
3. 檢驗目前沒進模型的展寬來源：星團深度、旋轉、殘留場星、差異消光形式，
   以及質量相依的雙星比例／質量比分布。
4. 重做 cluster-specific 注入回收，真值不要取自已貼牆的真資料最佳解；應在
   多個內部點建立 bias map。
5. 在上述問題解開前，論文可引用本文件的「通用性否定結果」與傳統法
   Tier 1 表，但不要把 Praesepe／NGC 3532 的前向 alpha 當定案數字。

## 主要入口

- `prepare_cluster_tier2.py`：Gaia crossmatch、G<=18、測光品質、誤差模型、
  星團專屬選擇函數與驗收。
- `cluster_imf_tier1.py --prepared`：在相同乾淨樣本上跑五種傳統法。
- `cluster_forward_validation.py`：固定／自由年齡消光、金屬量、差異消光、
  多次模型亂數與注入回收。

等時線網格放在被 `.gitignore` 排除的 `isochrones/`，可由
`pipeline.isochrones.download_grid()` 重新下載；正式小型結果與輸入快取
則保存在 `data/`、`results/`。
