# 已解決的限制

從 `LIMITATIONS.md` 分出來的一份，格式仿 `docs/reference/REFUTED.md`。**兩者的差別**：
`docs/reference/REFUTED.md` 記的是「這個說法本來就是錯的」，這裡記的是「這曾經是真的
問題，後來修好了」——問題本身在提出當下是對的，不是誤判，只是後來
透過實際工作解決掉了。

新增規則（見 `CONTRIBUTING.md` 五之一）：`LIMITATIONS.md` 某條需要的
工作全部做完、且重新核對過問題確實解決後，把整條搬到這裡，不要留在
`LIMITATIONS.md` 裡寫「已修正」，也不要直接刪掉不留紀錄。

格式：曾經的問題／怎麼解決的／解決時的關鍵結果／日期。

---

## 已解決清單

### 原 D8：PR #11 多星團驗證的四項程式正確性問題

**曾經的問題**：貼牆偵測曾被過度放行、選擇函數缺暗端紅藍檢查、SNR
校準缺獨立控制場，另有 Tier 1 的 NSS null 風險；而且程式修正合併後尚未
用真實多星團資料重跑，無法確定安全機制真的生效。

**怎麼解決的**：PR #11 合併後，以 `main` commit `d9a09f4` 和已保存的
NGC 3532／Praesepe Gaia、控制場資料重跑 selection；只有通過 selection
的 Praesepe 才執行固定年齡／消光的前向診斷。新輸出使用 `_postmerge`
檔名，不覆寫歷史結果。

**解決時的關鍵結果**：NGC 3532 的 G≥17 紅藍差誤差 −0.199，gate 正確
阻止前向；Praesepe selection 通過，但 B 設定 2/2 次 `f_bin=1.000` 貼牆，
均被標成 `diagnostic_only`，有效擬合數 0 並跳過注入回收。這解決的是程式
正確性，不是星團科學模型：兩個殘留問題已分為 LIMITATIONS D16、D17。

**日期**：2026-08-23

---

### 原 B4：Hess 圖各格當成獨立 Poisson

**曾經的問題**：`poisson_loglike()`（`pipeline/step3_age.py`）把 Hess 圖
每一格當獨立 Poisson 分布計算概似，而各格的觀測次數聯合分布嚴格來說是
多項分布（總星數固定 1,078，格間負相關），直覺上像是多算了一個「總數
也是自由參數」的自由度，論文必須聲明這是近似。

**怎麼解決的**：查證發現這個疑慮不成立，不是「近似但可接受」而是**代數
上完全等價**。原因是 `hess()`（同檔案）回傳直方圖前一定做 `h / h.sum()`，
所以合成星團的 Hess 圖（`mod_h`）與離群成分混合後的 `mix` 恆等於機率
分布、總和永遠精確等於 1，不管參數 theta 是什麼。推導：
`LL_poisson(theta) = LL_multinomial(theta) + n_obs*log(n_obs) - n_obs`，
後面那項是跟 theta 完全無關的常數（見證明與數值驗證腳本
`check_poisson_vs_multinomial.py`）。也就是說目前的實作雖然寫成「逐格
獨立 Poisson」，數學上等同真正的多項分布概似，argmax／梯度／後驗形狀
完全相同，不是近似。這個等價性**依賴 `hess()` 强制正規化到總和為 1**
這個實作細節，`check_poisson_vs_multinomial.py` 留著，之後改動 `hess()`
可以重跑確認等價性還在。

**解決時的關鍵結果**：`check_poisson_vs_multinomial.py` 對 20 組隨機
theta 數值驗證，`LL_poisson - LL_multinomial` 的差值在全部 20 組間的
變化幅度僅 ~1e-12（浮點誤差量級），且精確等於理論常數
`n_obs*log(n_obs) - n_obs`。

**日期**：2026-08-13

---

### 原 D9：一顆亮星在三個測過的質量範圍下都被顏色一致性檢查標記

**曾經的問題**：查證 A6（白矮星混入）時新加的顏色一致性檢查
（`assign_masses()` 觀測顏色偏離同 G 星等主序色 >0.4 星等回傳 NaN），
在三個測過的質量範圍設定下（`alpha_naive` 0.30–2.50、
`traditional_accounting.py` 主表 0.50–2.50、附錄範圍 0.30–2.50）都一致
標記剔除 `source_id=68409590552589184`（G=7.386 的亮星，偏離主序色
1.3–1.4 星等）。確認影響一個目前實際引用的頭條數字——
`traditional_accounting.py` 主表「全當單星」變體剔除的唯一一顆星就是它，
alpha 從 2.3949 位移到 2.4083（+0.0134，遠小於統計誤差 0.118），是
「已經在影響、只是影響量小」的已確認事實，不是假設性風險——按照
`LIMITATIONS.md` 自己的分級判準（已引用的數字受影響才算 A 類），這條
本來就不該留在 D 類（尚未驗證、沒有污染現有結果的證據），2026-08-14
CodeRabbit review 也指出了這個分類錯位；查證完成後直接解決，不需要
先搬去 A 類再搬來這裡。

**怎麼解決的**：先查本機可查的資料排除幾個假說——RUWE=1.098（遠低於
1.4，非天測雙星／壞解特徵）、`non_single_star=0`（Gaia NSS 管線未標記）、
`phot_bp_rp_excess_factor=1.262` 落在 Evans+2018 標準測光品質帶
`[1.032, 1.426]` 內（不是測光異常，排除「BP/RP 測光污染導致顏色偏紅」
這個假說）。真正解決是換角度查：查證 C20 時重建出原始「20 顆判定分歧」
集合，發現這顆星就在裡面——HR23 (Hunt & Reffert 2023) 用他們自己獨立的
成員判定流程，完全沒有把這顆星收進 M45 成員表（不是機率低，是根本不在
名單上）。順著這條線查 `data/radial_velocity.csv`（C3 已抓的 Gaia 官方
徑向速度）：**RV = 53.062 ± 0.136 km/s**（13 次 transit，非單次量測
雜訊），跟星團整體值 `bulk_rv=5.343 km/s` 差了近 48 km/s，**偏離達
350σ**——三個獨立證據（顏色遠離主序、HR23 完全沒收、RV 偏離 350σ）方向
一致，確認 `68409590552589184` 不是 M45 成員，不需要外部目錄交叉比對
（這台機器對 SIMBAD／Vizier 的 HTTPS 連線曾遇到 SSL 憑證驗證失敗，做不了
外部比對，但靠內部三條獨立證據已經足夠確定）。已把這顆星加進
`pipeline/step5_imf.CONFIRMED_NON_MEMBER_IDS`（跟 A6 的 RV 離群星同一個
排除機制），作為顏色檢查之外的第二層防護。

**解決時的關鍵結果**：加進排除名單前後，`check_wd_exclusion_impact.py`
三個設定「兩種排除都套用」那一列的 alpha 完全沒變（1.9603／2.4083／
2.0809）——因為顏色檢查在這三個設定下本來就已經抓到這顆星，這次只是把
排除理由從「顏色偏離的統計判準」升級成「有 RV／HR23 硬證據的確認」，
數字沒有變化，但排除的正當性從統計推論變成三重獨立觀測證據。

**日期**：2026-08-13

---

### 原 A6：`cmd_members.csv` 混進至少 2 顆非成員天體

**曾經的問題**：`assign_masses()` 沒檢查觀測顏色跟等時線預測的主序顏色
是否一致，若混入白矮星或巨／次巨星會用錯誤的質光關係算出錯誤質量。
用 Gentile-Fusillo et al. (2021) 的 Gaia EDR3 白矮星目錄比對現役
`data/cmd_members.csv`（1,078 顆），查到 1 顆命中（`source_id=
66697547870378368`，`Pwd=0.999`、`bp_rp=-0.403` 明顯偏藍）；另外用
`logg_gspphot` 偏低（<4.3）且徑向速度偏離 bulk_rv 達 >5σ 兩個獨立訊號
交叉比對，再查到 1 顆（`source_id=64895139073954944`，RV 偏離
bulk_rv 達 19.5σ，顏色本身正常、只有 RV／logg 抓得到）。逐一核對三個
實際引用的擬合設定後，發現這兩顆星混入的影響比原本以為的更窄，但
確實已經在影響（不是假設性風險），依 `LIMITATIONS.md` 自己的分級判準
屬於 A 類。

**怎麼解決的**：(1) 幫 `assign_masses()` 加可選的 `obs_color` 參數，
用同一條 isochrone 反查每個 G 星等對應的主序顏色，觀測顏色偏離
>0.4 星等就回傳 NaN（門檻理由：這顆白矮星偏離主序色達 1+ 星等，遠超
未解析主序雙星的典型偏移 <0.2 星等）；(2) 加一個小型、有清楚出處的
`pipeline/step5_imf.exclude_confirmed_non_members()` 已確認非成員名單
（RV 離群星，附上判定依據），不是手動從 `cmd_members.csv` 挖掉資料列。
`run_pipeline.py` 第 4、5 步與 `traditional_accounting.py` 真實資料段
都已接上這兩個機制。**CodeRabbit review 抓到一個真的分析錯誤並訂正**：
第一版只測了一個質量範圍就直接宣稱「只剔除白矮星、A6 解決」，後來逐顆
核對才發現白矮星的假質量（0.276–0.307 M☉）剛好卡在 0.30–0.50 這個
邊界帶，在對齊主表的範圍（0.50–2.50）下根本不在樣本裡；那個範圍下
顏色檢查真正剔除的其實是另一顆星（`source_id=68409590552589184`，
另開 D9 追蹤，已於 2026-08-13 併案解決，見上一則）。

**解決時的關鍵結果**（`check_wd_exclusion_impact.py`，三個實際引用的
擬合設定，兩個排除機制一起套用 vs 都不排除）：

| 設定 | 基準（都不排除） | 兩個排除都套用 | 差異 |
|---|---|---|---|
| `alpha_naive`（0.30–2.50） | 1.9797 (n=694) | 1.9603 (n=680) | −0.0194 |
| 對齊主表（0.50–2.50） | 2.3949 (n=424) | 2.4083 (n=423) | +0.0134 |
| 附錄較寬範圍（0.30–2.50） | 2.0916 (n=770) | 2.0809 (n=759) | −0.0106 |

三個設定下的位移都遠小於各自的統計誤差（0.067–0.118），兩顆非成員
天體混入對已引用頭條數字的影響可忽略。前向模型對齊的主表設定完全沒
被這兩顆星影響過（不管排不排都一樣）。

**日期**：2026-08-13

---

### 原 C20：20 顆判定分歧，定義已重建、逐顆核對完畢

**曾經的問題**：與 HR23 有 20 顆真正的判定分歧（`my_prob>=0.99` 且
完全不在 HR23 的 M45 成員表裡），這 20 顆的自行運動與視差跟公認成員
無法區分。曾經嘗試用 `gaia_radial_velocity.py` 查這批星的徑向速度，
但發現 `comparison.csv` 重新算出的「判定分歧」有 367 顆，跟原始的
「20 顆」對不上——兩者用的母體（全部候選星 vs 已篩過的 `cmd_members.
csv`）與門檻不同，一度搞不清楚原始的「20」是怎麼定義的。

**怎麼解決的**：從 `_archive/C_membership_phase_analysis/investigate.
py` 的 docstring 找回原始定義，用 `results/baseline.dat`（pyUPMASK
全部候選星，6,956 顆）＋本機已有的 `data/hr23_Melotte_22.csv` 精確
重建，重現「20 顆」，逐顆列出 `source_id`。逐顆核對現役樣本後發現：
18 顆已被後續的測光品質篩選或 `membership_threshold` 濾掉，不在目前
任何頭條數字的樣本裡，這部分的分歧已自然消失；剩下 2 顆逐顆查證——
一顆（`63534390354375040`）有 RV 資料，只偏離 bulk_rv 0.1σ，運動學
完全正常，維持成員身分；另一顆就是 D9／A6 那顆亮星
（`68409590552589184`，RV 偏離 bulk_rv 達 350σ），已確認不是成員、
已排除。

**解決時的關鍵結果**：20 顆判定分歧裡 18 顆自然消失於現役樣本、
2 顆逐顆查完（1 顆維持成員、1 顆確認排除），不再有懸而未決的判定
分歧存在於目前任何頭條數字使用的樣本中。

**日期**：2026-08-13

---

### 原 C18：測光品質篩選的顏色相依殘留，查證後是統計雜訊量級

**曾經的問題**：測光品質篩選從 1,297 顆砍到 1,078 顆（砍 219 顆），
G≥17 紅星砍 59%、藍星砍 20%，模型化後仍有 +0.014 的殘留紅藍落差
沒有被目前的星等相依一維選擇函數模型解釋，懷疑顏色本身（而非只有
星等）也影響存活率，可能是星雲汙染造成的系統性殘留。

**怎麼解決的**：重用 `scripts/diagnostics/selection_probe.load()` 的
資料，扣掉已經被訊噪比切掉的星（避免跟 BP/RP 超額那一刀重複計入），
依星等分箱、箱內再依顏色細分成三份，直接把存活率對顏色的關係擬合成
連續的線性函數（不是只看一個紅藍落差常數），並換用更大樣本（全星等
範圍 n=1,163，取代原本只看 G≥17 的 n=174）提高檢定力。

**解決時的關鍵結果**（`check_selection_color_residual.py`）：

| 樣本 | 擬合結果 | 斜率顯著度 |
|---|---|---|
| G≥17（原比較基準，n=174） | survival = 1.002 − 0.0113×bp_rp | 0.1σ，不顯著 |
| 全樣本（n=1,163，檢定力較高） | survival = 0.939 − 0.0054×bp_rp | 0.8σ，不顯著 |

換較大樣本增加檢定力後，顏色對存活率的線性斜率依然跟 0 統計上無法
區分（0.8σ）。現有的星等相依一維近似**目前沒有被數據推翻**——這是
這個樣本量下的結論，不是永久定論，樣本大幅增加時（例如放大搜尋半徑）
應該用 `check_selection_color_residual.py` 重跑確認結論是否還成立。

**日期**：2026-08-14
